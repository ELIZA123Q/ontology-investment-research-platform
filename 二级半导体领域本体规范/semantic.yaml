# v2 兼容入口：只读。
schema_name: semiconductor_domain_semantic
schema_version: 1.0.0
status: deprecated_read_only
description: 半导体 v1 semantic 入口转发到严格领域语义本体。
depends_on: [common.yaml, ../一级通用本体规范/semantic.yaml]
authority_targets:
- ../语义本体/二级半导体/semantic.yaml
