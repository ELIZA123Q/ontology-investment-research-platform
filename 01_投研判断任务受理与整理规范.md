# 01_投研判断任务受理与整理规范

## 1. 文档定位

本文档规范 01 阶段如何把研究员提出的问题、事件观察、材料线索、数据变化或初步假设，整理成一个可以进入 02 判断结构梳理和本体视图生成的投研判断任务。

01 的核心目标不是把原始问题改写得更长，而是把自然语言输入收敛为一个**有判断价值、边界清楚、可验证、可被反面证据推翻、可重复执行**，并且足以交给 02 搭建判断结构的研究任务。

01 应回答四个问题：

1. 研究员到底要判断什么；
2. 判断对象、范围、时间和用途是什么；
3. 哪些歧义会改变后续任务结构，是否已经消除；
4. 当前任务是否达到正式交给 02 的质量门槛。

研究员不需要使用本体术语，也不需要填写对象类型、关系类型、关键变量或证据规则。研究员使用正常业务语言表达，01 负责整理为后续阶段可承接的判断任务。

01 不读取、不引用也不校验任何本体文件。正式本体暂时无法表达研究员需求时，01 仍应如实保留需求，不能要求研究员为了适配本体而改变真实意图。本体承接和缺口判断从 02 才开始。

`ready_for_matching` 只表示 01 阶段状态已经满足进入 02 的条件，不代表本体一定能够承接、证据已经充分或判断已经成立。正式交付时，还必须满足 `00A_高质量产出判别标准.md` 中对 01 的质量要求。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

01 负责确认并整理以下内容：

1. **核心判断问题**：本次到底要判断什么，是否不预设答案；
2. **判断落点**：本次最终要形成状态定位、趋势判断、阶段切换、影响强弱、路径成立性，还是假设验证与更新；
3. **研究对象与判断起点**：围绕谁或什么判断，从事件、状态、材料、数据还是已有假设开始；
4. **研究范围边界**：时间、地域、产业链、对象集合和不研究事项；
5. **观察维度**：哪些业务变化可能影响判断；
6. **初步线索**：支持方向、削弱方向、反面证据方向和其他可能解释的需求层面提示；
7. **研究用途与交付要求**：产物用于行业跟踪、材料更新、假设验证、复盘还是后续证据准备；
8. **关键歧义处理**：判断是否需要追问，是否可以采用可回滚假设，是否必须判定为不适用；
9. **质量门槛检查**：判断本阶段产物是草稿、最低通过，还是达到正式交付质量。

01 可记录研究员提供的关注点和必要假设，但这些内容只作为 02 的输入线索，不能提前写成正式判断路径、本体映射、证据要求或结论。

### 2.2 本阶段不负责

01 不做以下事情：

1. 不从零替研究员批量生成研究选题；
2. 不查询事实数据、行情、公告、新闻或研报；
3. 不判断材料真假、证据强弱或结论方向；
4. 不匹配正式本体类型、关系、关键变量或推理规则；
5. 不编写正式判断路径、传导环节、传导链或本体视图；
6. 不编写正式证据要求、取数规则、证据槽位或证据准备清单；
7. 不执行推理，不形成投研结论；
8. 不直接回答“买不买”“涨不涨”“目标价多少”等交易问题；
9. 不把行情查询、资料总结、文本润色等请求强行改造成判断任务。
10. 不输出本体 ID、对象类型、关系类型、状态变量、证据 profile、本体引用或本体缺口判断。

正式判断结构由 02 生成，证据准备要求由 03 生成，路径推理和研究员报告由 04 生成。01 只确认研究任务，不提前替后续阶段完成结构、证据或判断。

只有研究员需求本身仍有歧义时，任务才退回 01。02 发现本体无法承接已经明确的需求时，应记录本体候选与缺口，不得以本体限制为由退回 01 或改写研究问题。

直接买卖问题只有在能够收敛为具体、可验证、可被反面证据推翻的投资假设时才进入本流程；改写后仍缺少对象、范围、判断动作或验证条件的，应继续澄清或判定为不适用。

---

## 3. 哪些问题可以受理

### 3.1 主任务类型

每项任务必须明确一个主类型。复杂问题可以包含辅助分析角度，但不能同时设置多个彼此竞争的主线。

| 主任务类型 | 要回答的问题 | 典型输入 |
|---|---|---|
| 状态与趋势判断 | 某个行业、产品、材料、公司或资产当前处于什么状态，未来可能如何变化 | “存储芯片现在处于什么周期？” |
| 事件或变化影响判断 | 某个政策、公告、事故、价格、供需或竞争变化是否影响目标对象 | “出口管制会不会影响光模块产业链？” |
| 传导路径判断 | 某项变化是否会通过产业链、供应链、竞争关系、成本、价格或预期传导 | “材料紧缺会不会传导到公司业绩？” |
| 假设验证与更新判断 | 某个已有判断、投资假设或研究逻辑是否仍成立，是否需要修正 | “AI 需求支撑高景气这个逻辑还成立吗？” |

### 3.2 常见输入形态

以下输入可以进入 01，但必须先收敛为上述主任务类型之一：

1. **事件观察**：政策、公告、事故、价格变化、产业新闻；
2. **材料线索**：研究员给出的新闻、研报、财报、会议纪要或数据片段；
3. **已有假设**：已有投资逻辑、行业判断、公司判断或跟踪结论；
4. **证据更新**：新公告、新数据或新材料触发对旧判断的更新；
5. **历史复盘**：按当时信息边界复盘原判断，并用后续事实检查是否兑现。

证据更新和历史复盘不是独立主任务类型。证据更新通常触发“假设验证与更新判断”，历史复盘需要先明确当时复盘的是状态、影响、路径还是假设。

### 3.3 不应直接进入本流程的问题

以下问题不应直接进入 01—04 判断流程：

1. 纯事实查询，如“某股票今天涨了多少”；
2. 纯资料罗列，如“帮我整理几篇新闻”；
3. 纯文本处理，如润色、翻译、排版；
4. 无条件价格、收益率或短期涨跌预测；
5. 直接交易建议，如“现在能不能买”；
6. 要求内幕信息或未授权信息；
7. 范围过大，无法落到对象、范围、观察维度和反面证据；
8. 无法改写成可验证、可被反面证据推翻判断的请求。

不适用问题可以转交其他流程；如果能够收敛为具体投资假设，可在说明边界后进入 01。

---

## 4. 如何整理自然语言输入

01 不要求研究员按固定格式提问。系统应从自然语言中整理以下九项，并据此形成规范化判断任务。

### 4.1 原始输入与研究意图

保留研究员原始输入，并说明系统对其研究意图的理解。

整理时应区分：

1. 研究员明确说出的内容；
2. 研究员提供的材料或线索；
3. 系统为完成任务整理出的表达；
4. 系统补充的可回滚假设。

不得把系统推断伪装成研究员原话。

### 4.2 核心判断问题

核心判断问题必须是不预设答案、可验证、可被反面证据推翻的问题。

好的核心问题应包含：

1. 判断对象；
2. 判断动作；
3. 时间窗口；
4. 主要范围；
5. 支持与削弱方向。

例如，原始问题：

```text
存储芯片目前处于什么周期，本轮周期什么时候结束？
```

不应只整理为：

```text
研究存储芯片行业周期变化。
```

应整理为：

```text
截至每次运行的数据截止时点，全球存储芯片中 HBM、非 HBM DRAM 与 NAND 分别处于什么供需与价格周期阶段；未来 6—24 个月内，哪些需求、库存、有效供给、价格和产能信号将支持周期延续，或表明周期转入见顶、正常化或下行？
```

### 4.3 判断落点

01 必须明确本任务最终要形成哪类判断落点（YAML 字段 `judgment_landing`；正文 §2.3 称「问题类型」）。常见落点包括：

| 判断落点 | 含义 |
|---|---|
| 状态定位 | 判断当前处于什么状态或阶段 |
| 趋势判断 | 判断未来方向、斜率或持续性 |
| 阶段切换 | 判断何时或在什么条件下从一个阶段切到另一个阶段 |
| 影响强弱 | 判断某个事件、变化或政策影响是否显著 |
| 路径成立性 | 判断某条产业链、供应链、成本、价格或预期传导是否成立 |
| 假设验证与更新 | 判断已有逻辑是否仍成立、需要增强、削弱还是推翻 |

不得只把任务整理成宽泛研究主题。若无法判断落点，必须追问。

### 4.4 核心对象

明确围绕谁或什么判断。对象可以是行业、细分赛道、公司、产品、材料、技术、资产、投资假设、事件或政策。

业务名称可以先保留，但必须足以让 02 判断其可能角色。如果同一名称可能对应不同层级或不同判断路径，应先确认研究员关注的是哪个层级。

例如，“存储芯片”可能需要进一步拆分为 HBM、非 HBM DRAM、NAND；“先进封装”可能需要区分 CoWoS、HBM 封装、ABF 载板、测试或设备材料。

### 4.5 判断起点

判断起点说明问题从哪里开始，可以是：

1. 一个事件或政策；
2. 一份公告、新闻、材料或数据变化；
3. 一个行业现象；
4. 一个已有投资假设；
5. 一个当前状态或未来趋势问题。

01 不强制所有问题都从事件开始。对状态类问题，应写清“以当前状态为判断起点”；对假设更新类问题，应写清被验证的原假设是什么。

### 4.6 判断范围

范围应覆盖会影响 02 结构匹配的边界。

| 范围维度 | 需要说明 |
|---|---|
| 时间 | 当前、近半年、未来 6—12 个月、固定历史区间或复盘时点 |
| 地域 | 中国、海外、全球、特定国家或特定市场 |
| 产业链 | 材料、设备、制造、封测、产品、客户、终端或资产 |
| 对象 | 行业、产品、公司、资产或特定集合 |
| 不研究事项 | 明确本次不覆盖什么，防止后续扩散 |

“当前”“最新”“近半年”“未来 6 个月”“代表性龙头”等相对时间或动态对象，不能被固化为文档生成日对应的一次性日期或名单。01 应写清它们相对于什么时点变化、按什么范围或口径选择，以及后续运行是否需要重新确定。

### 4.7 观察维度

观察维度说明需要关注哪些业务变化。常见维度包括：

- 需求；
- 订单；
- 库存；
- 产能；
- 价格；
- 成本；
- 毛利率；
- 交期；
- 资本开支；
- 市场预期；
- 竞争格局；
- 国产替代；
- 技术替代。

01 使用业务语言记录观察维度，由 02 再映射为正式关键变量。观察维度应具有判别力，不能只是罗列变量。

例如，“价格”应说明它可能用于判断涨价斜率、周期阶段、需求破坏还是供需紧张；“库存”应说明它可能用于区分真实需求、补库或需求前置。

### 4.8 初步线索：支持、削弱、反面证据和其他可能解释

01 应记录需求层面的初步线索，帮助 02 判断后续如何拆解问题。至少包括：

1. **支持方向**：哪些现象可能支持某个判断；
2. **削弱方向**：哪些现象可能降低判断强度；
3. **反面证据方向**：哪些现象出现时可能推翻或逻辑失效判断；
4. **其他可能解释**：是否存在其他路径可以解释同一现象。

这些线索只表达“研究需求上需要留意什么”，不是正式判断路径、传导环节、证据规则或反面证据规则。

不得在 01 中写成：

```text
节点 N-01 → N-02 → N-03
```

也不得写成正式证据表：

```text
证据 E-01 支持关键变量 X，反面证据 C-01 逻辑失效路径 P-01。
```

应写成研究员可理解的业务语言，例如：

```text
需要关注 AI 服务器需求是否是真实使用拉动，还是由客户提前采购和库存前置放大；如果后续出现订单取消、库存回升或终端需求下修，可能削弱周期延续判断。
```

### 4.9 用途与交付要求

01 必须说明产物准备用于什么研究动作。常见用途包括：

1. 行业状态跟踪；
2. 新材料或新数据更新；
3. 已有投资假设验证；
4. 研究逻辑复盘；
5. 后续数据与证据准备；
6. 为 04 形成研究员报告提供任务边界。

用途不清时，02 难以判断分析深度、对象边界和证据需求。若用途会影响任务结构，应追问。

### 4.10 必要假设

只有在不改变任务结构的情况下，01 才可以记录可回滚假设。

每条假设必须说明：

1. 假设内容；
2. 采用原因；
3. 来源类型：研究员原话确认、需求澄清后确认、系统补充；
4. 失效条件；
5. 对后续阶段的影响。

如果某项假设会改变主要对象、判断类型、时间范围、产业链边界、主辅轴或交付用途，不能作为系统假设，必须追问。

---

## 5. 改写与澄清原则

### 5.1 改写原则

改写问题时应遵循以下原则：

1. 保留研究员原始意图，不偷换问题；
2. 不预设结论，不写成利好、利空、改善或恶化；
3. 使用“是否”“处于什么阶段”“在什么条件下”“哪些信息支持或削弱”等表达；
4. 明确判断对象、判断动作、时间窗口、范围和用途；
5. 将未经验证的事实前提改写为待验证命题；
6. 区分研究员原话、用户材料、系统整理和系统补充；
7. 保留相对时间和动态对象的业务语义，不替换成一次运行的绝对日期或临时名单；
8. 避免为了覆盖全面而扩大问题范围。

例如，“近半年 HBM 已经上涨，原因是什么”应整理为：

```text
近半年是否存在具有代表性的 HBM 价格上涨；若存在，其主要驱动来自 AI 需求、供给约束、客户提前采购、产品结构变化，还是其他因素？
```

### 5.2 关键歧义校验

01 在每次项目运行中都必须先与用户完成真实交互确认，不能因为原始输入看似完整而跳过。正式生成需求说明前至少提出并获得三个问题的用户回答，必须覆盖：

1. 核心判断对象及其层级；
2. 事件口径、判断时点和时间窗口；
3. 交付形态、用途及需要拆分的对象维度。

上述三问是进入 02 的硬门，不是“有歧义时才问”的可选步骤。系统默认、上下文推断、批处理话术和“无需追加澄清”均不能代替真实用户回答。用户已经在连续对话中明确回答的，可以直接记录为本次三问结果，但必须保留问题、回答、确认时间和主题字段。

完成三问后，仍需继续检查会改变任务结构的其他歧义；如有，必须追加追问。

### 5.3 必须追问的情况

以下情况必须追问，不得直接输出 `ready_for_matching`：

1. 核心对象不清，不同理解会产生不同判断结构；
2. 判断动作不清，无法判断是状态、趋势、影响、路径还是假设验证与更新；
3. “影响”含义不清，无法判断关注供给、需求、成本、业绩、估值还是预期；
4. 时间、地域或产业链范围不清，且会改变主路径；
5. 判断用途不清，且会影响分析深度或对象边界；
6. 研究员在多个实质不同方向中没有选择；
7. 问题过大，必须先收窄才能进入 02；
8. 原始输入包含明显方向性结论，但缺少验证对象和反面证据方向。

追加追问时，每次只问最关键的问题，并说明该问题会影响什么。三问硬门之外，能从用户已明确回答的内容中可靠提取的信息不重复询问。

### 5.4 可以不阻塞 01 的情况

以下情况可以记录为可回滚假设或下游待解析项，不阻塞 01：

1. 不改变任务结构的名称规范化；
2. 后续 02 可以处理的对象类型归属；
3. 后续 03 才能确定的具体数据截止时点；
4. 后续 03 才能确定的实际样本名单；
5. 不影响主问题的补充对象；
6. 不影响判断落点的表达风格或交付格式。

可回滚假设不得替代研究员对核心意图和范围的选择。

---

## 6. 准入状态与质量状态

01 必须区分阶段状态和产出质量。

### 6.1 阶段状态

阶段状态回答：01 是否可以继续流转。

| 状态 | 是否生成文件 | 处理方式 |
|---|---:|---|
| `ready_for_matching` | 是 | 生成完整《投研需求说明》，可交给 02 |
| `needs_clarification` | 否 | 只在对话中逐项追问，不创建待澄清版文件 |
| `out_of_scope` | 是 | 生成简短《不予受理说明》，不进入 02 |

#### `ready_for_matching`

核心检查通过，不存在会改变任务结构的关键歧义，可以进入 02。该状态不代表本体已匹配、证据充分或判断成立。

#### `needs_clarification`

存在会改变任务结构的关键歧义。不得进入 02，也不生成正式文件；只在对话中说明影响并逐项追问。澄清完成后再一次性生成正式文件，不能先生成待澄清版再追加答案。

#### `out_of_scope`

问题不属于本项目，或无法改写为可验证、可被反面证据推翻的投研判断任务。不进入 02；生成简短不予受理说明，写明原因及可行的改写或转交方向。

### 6.2 产出质量状态

产出质量回答：01 产物是否达到正式交付标准。

| 状态 | 含义 | 处理 |
|---|---|---|
| `draft` | 草稿，尚未完成完整校验 | 不得正式交付 |
| `minimum_pass` | 满足最低流程要求，可以试运行进入 02 | 可内部试跑，但不得作为高质量交付 |
| `high_quality_pass` | 通过 01 质量门槛，可正式交给 02 | 可正式交付 |
| `return_required` | 存在关键缺陷 | 退回澄清或重写 |
| `stop_with_gap_report` | 当前无法形成有效判断任务 | 输出缺口说明或不予受理 |

正式发布的 01 产物原则上应达到 `high_quality_pass`。如果只达到 `minimum_pass`，必须说明限制，不得包装成正式高质量产出。

《投研需求说明》与《不予受理说明》的 YAML `quality_status` 及正文质量结论取值，以 `输出模板/01_投研需求说明模板.md` 和 `01_不予受理说明模板.md` 为准。`stop_with_gap_report` 用于全链缺口说明场景，不写入上述两类 01 产物。

---

## 7. 质量门槛与返工规则

本节承接 `00A_高质量产出判别标准.md` 中关于 01 的要求，但只保留 01 执行所需的阶段质量门。

### 7.1 01 最低准入检查

进入 02 前，至少满足以下检查：

| 检查项 | 通过标准 |
|---|---|
| 用户交互确认 | 已完成至少三个真实问答，覆盖核心对象、事件口径与时间窗口、交付形态与拆分方式 |
| 核心问题 | 已形成一个不预设结论、可验证、可被反面证据推翻的问题 |
| 判断落点 | 已明确任务最终要形成哪类判断 |
| 核心对象 | 已知道围绕谁或什么判断 |
| 判断起点 | 已知道从事件、状态、假设、材料还是数据开始 |
| 判断范围 | 时间、地域、产业链和对象边界足以限定任务 |
| 观察维度 | 已知道需要关注哪些业务变化 |
| 初步线索 | 已记录支持、削弱、反面证据和其他可能解释方向 |
| 研究用途 | 已知道产物用于什么研究动作 |
| 关键歧义 | 不存在会改变任务结构的未决问题 |
| 可重复执行 | 相对时间和动态对象没有被固化为一次性结果 |
| 上下文一致 | YAML、摘要和正文表达一致 |

### 7.2 高质量语义审阅

达到高质量 01，还必须通过以下语义审阅：

1. 规范化问题是否真的回答“要判断什么”，而不是变成宽泛主题；
2. 判断落点是否足够清楚，能指导 02 形成判断结构；
3. 对象和范围是否既不过宽，也没有漏掉影响主判断的必要边界；
4. 初步线索是否有判别力，而不是只罗列变量；
5. 反面证据和其他可能解释是否出现；
6. 必要假设是否可回滚，且没有替代研究员选择；
7. 正文是否使用研究员能理解的业务语言；
8. 是否没有越界写入 02 的判断路径、03 的证据规则或 04 的结论。

语义审阅不能只写“通过”，必须说明通过依据或返工原因。

### 7.3 01 不得出现的问题

以下问题出现时，01 不能判定为 `high_quality_pass`：

1. 把材料线索写成结论；
2. 把研究员的方向性判断直接写成系统结论；
3. 只改写语句，不明确判断对象和判断动作；
4. 没有判断落点，只形成宽泛研究主题；
5. 支持方向、削弱方向、反面证据方向或其他可能解释缺失；
6. 用“默认已澄清”代替真实歧义消除；
7. 将过宽问题硬塞进后续流程；
8. 在 01 阶段提前设计正式本体、路径、证据规则或推理结论；
9. 正文和结构化字段不一致；
10. 用文档生成日固化动态时间或动态对象。

### 7.4 返工条件

出现以下任一情况，01 不得进入 02，必须追问、重写或不予受理：

1. 核心问题仍含糊；
2. 判断落点不清；
3. 对象边界不清；
4. 时间窗口不清；
5. 研究用途不清；
6. 反面证据方向或其他可能解释缺失；
7. 问题被偷换；
8. 结构化摘要与正文不一致；
9. 仍存在会改变任务结构的关键歧义；
10. 请求本身超出本项目范围，且无法合理改写为投研判断任务。

返工时必须说明：

1. 当前问题是什么；
2. 会影响哪个任务要素；
3. 需要研究员补充什么；
4. 补充后如何重新判断是否可进入 02。

---

## 8. 输出文件规则

正式产出文件名必须以对应阶段前缀开头：`01-`、`02-`、`03-`、`04-`、`05-`。格式为 `<阶段>-<核心主题><产物类型>-<YYYYMMDD>-<当日序号>`。模板维护文件（如 `01_投研需求说明模板.md`）保留目录内编号，不用于正式产出。

### 8.1 通过版文件

`ready_for_matching` 且质量状态达到 `high_quality_pass` 时，输出完整《投研需求说明》。

文件名使用：

```text
01-<核心主题>投研需求说明-<YYYYMMDD>-<当日序号>.md
```

《投研需求说明》定义研究需求，不是推理报告，也不是某次运行的数据快照。它不应包含正式本体映射、正式传导环节、证据规则、推理结果或投资建议。

YAML 头部字段以 `输出模板/01_投研需求说明模板.md` 为准，正式通过版至少应包含：

```yaml
---
document_type: judgment_task
schema_version: "1.0.0"
task_id: "<任务稳定标识>"
requirement_version: <版本号>
generated_at: "<文档生成时间，ISO 8601；仅用于审计>"
status: ready_for_matching
status_reason: "<一句话：核心对象、判断落点、范围、用途和关键歧义已确认，可进入 02>"
quality_status: high_quality_pass
quality_gate_ref: "00A_高质量产出判别标准.md#5.1"
deterministic_check_status: checked
semantic_review_status: reviewed
return_required: false
return_stage: null
user_confirmation:
  status: confirmed
  confirmed_at: "<用户完成确认的时间，ISO 8601>"
  timezone: "Asia/Shanghai"
  required_question_count: 3
  questions:
    - topic: core_object
      question: "<与用户核对核心判断对象的问题>"
      answer: "<用户原意回答>"
    - topic: event_scope_and_time_window
      question: "<与用户核对事件口径和时间窗口的问题>"
      answer: "<用户原意回答>"
    - topic: delivery_landing
      question: "<与用户核对交付形态和拆分方式的问题>"
      answer: "<用户原意回答>"
original_input: |
  <研究员原始输入>
normalized_question: "<不预设结论、可验证、可被反面证据推翻的规范化判断问题>"
judgment_landing: "<状态定位 | 趋势判断 | 阶段切换 | 影响强弱 | 路径成立性 | 假设验证与更新>"
task_type:
  primary: "<状态与趋势判断 | 事件或变化影响判断 | 传导路径判断 | 假设验证与更新判断>"
  secondary: []
scope_summary: "<时间、地域、产业链、对象和不研究事项摘要>"
---
```

`quality_status` 在通过版中取值为 `draft`、`minimum_pass`、`high_quality_pass` 或 `return_required`。标记为 `high_quality_pass` 时，`deterministic_check_status` 必须为 `checked`，`semantic_review_status` 必须为 `reviewed`。

正文必须包含以下章节，结构与 `01_投研需求说明模板.md` 一致：

1. 研究需求确认；
2. 研究目标与核心问题；
3. 研究对象；
4. 研究范围与边界；
5. 已澄清的问题；
6. 重点看什么；
7. 初步线索：支持、削弱与反面情形；
8. 工作假设；
9. 研究边界提醒；
10. 01 质量门槛检查。

正文只写研究需求。`status`、`readiness_checks`、`next_question` 等机器字段不应进入正文；第 10 章「01 质量门槛检查」可保留人工审阅表和质量结论，但不得重复抄写完整 YAML。

### 8.2 待澄清状态

`needs_clarification` 不生成正式文件。只在对话中说明：

1. 当前缺少什么信息；
2. 为什么该信息会改变任务结构；
3. 需要研究员回答的一个最关键问题；
4. 回答后如何继续受理。

不得生成待澄清版《投研需求说明》后再补答案。

### 8.3 不予受理版文件

`out_of_scope` 时输出简短《不予受理说明》。不保留通过版正文第 1—10 章，也不保留完整任务结构字段。字段与章节以 `输出模板/01_不予受理说明模板.md` 为准。

文件名使用：

```text
01-<核心主题>不予受理说明-<YYYYMMDD>-<当日序号>.md
```

YAML 头部至少应包含：

```yaml
---
document_type: judgment_task
schema_version: "1.0.0"
generated_at: "<文档生成时间，ISO 8601；仅用于审计>"
status: out_of_scope
status_reason: "<不予受理原因>"
quality_status: high_quality_pass
quality_gate_ref: "00A_高质量产出判别标准.md#5.1"
original_input: |
  <研究员原始输入>
out_of_scope_category: "<纯事实查询 | 文本处理 | 直接投资建议 | 范围过大 | 非投研判断 | 未授权信息 | 其他>"
can_be_rewritten: "<true | false>"
recommended_route: "<数据检索/资料整理/文本处理/继续澄清/改写为投研判断任务/拒绝>"
---
```

不予受理版同样使用 `document_type: judgment_task`，通过 `status: out_of_scope` 与通过版区分。

正文必须包含以下章节：

1. 原始输入；
2. 不予受理原因；
3. 可行改写方向；
4. 建议转交流程；
5. 质量检查。

### 8.4 模板说明不得进入正式输出

模板文件中可以保留维护说明、字段说明、示例和生成规则，但正式生成文件时不得输出模板维护说明，也不得把模板说明作为正文内容。

---

## 9. 动态范围与正式输出检查

### 9.1 动态范围处理

正文应先用研究员能够直接理解的语言写清范围，YAML 再保存同一含义的执行字段。两者不得一个写相对范围、另一个写一次运行的绝对结果。

`generated_at` 和文件名日期只表示文档何时生成，不是滚动窗口锚点。01 保留研究员提出的相对时间和动态对象口径；03 在每次运行中记录实际采用的起止日期、对象名单和数据截止时点。

例如：

| 原始表达 | 01 应处理 | 不应处理 |
|---|---|---|
| 当前 | 写明以每次运行数据截止时点为锚点 | 固化为文档生成日 |
| 近半年 | 写明回看运行日前最近六个月或最近两个季度 | 固化为某一次绝对日期 |
| 代表性龙头 | 写明选择口径和后续运行需刷新 | 直接写死临时名单且不说明口径 |
| 最新公告 | 写明后续运行需按数据截止时点重新确认 | 只引用当前看到的一份公告 |

### 9.2 正式输出检查

通过版输出前，至少完成以下检查：

1. 文件名、YAML 和正文是否对应同一任务；
2. `status` 与正文处理方式是否一致；
3. `user_confirmation` 是否记录至少三个真实问答，且主题覆盖 `core_object`、`event_scope_and_time_window`、`delivery_landing`；
4. `quality_status` 或第 10 章质量结论是否体现已通过质量门槛；
5. 原始输入是否保留；
6. 规范化问题是否不预设答案；
7. 判断落点是否明确；
8. 对象、时间、地域、产业链和不研究事项是否一致；
9. 初步线索是否同时包含支持、削弱、反面证据和其他可能解释方向；
10. 必要假设是否标明来源和失效条件；
11. 是否没有越界写入 02、03、04 的内容。

---

## 10. 与 02 的交接

02 使用 01 输出的以下内容：

1. 规范化核心判断问题；
2. 判断落点与主任务类型；
3. 研究对象与判断起点；
4. 时间、地域、产业链和对象边界；
5. 观察维度；
6. 支持、削弱、反面证据和其他可能解释的初步线索；
7. 研究用途和交付要求；
8. 必要假设和下游待解析项。

02 应保留 01 的研究意图、主要对象和判断范围，不得擅自改变。01 写的是“当前”“近半年”“截至运行时的代表性公司”等动态范围时，02 应保留其业务含义和选择口径，不得改写成文档生成日对应的绝对日期或一次性名单。

如果 02 发现仍有会改变判断结构或本体匹配的关键歧义，应退回 01。如果 02 发现正式本体缺少对象、关系、变量或规则，应由 02 记录本体缺口或任务候选，不视为 01 受理失败。

```text
研究员输入
↓
01 整理判断任务并检查关键歧义
├─ needs_clarification：对话追问，不生成正式文件
├─ out_of_scope：输出不予受理说明，不进入 02
└─ ready_for_matching + high_quality_pass：输出《投研需求说明》并交给 02
```

---

## 附录：常见输入处理

| 研究员输入 | 处理方式 |
|---|---|
| 明确判断问题 | 直接整理成投研判断任务，并检查质量门槛 |
| 行业状态问题 | 收敛为状态与趋势判断 |
| 事件或政策线索 | 收敛为事件或变化影响判断；涉及多环节作用时检查是否需要传导路径判断 |
| 新公告、新数据、新材料 | 作为证据更新输入，通常收敛为假设验证与更新判断 |
| “这个逻辑还成立吗” | 收敛为假设验证与更新判断 |
| “当时判断对不对” | 采用历史复盘方式，并明确对应主任务类型 |
| 纯资料总结 | 只有用于更新或验证判断时才进入本流程 |
| 纯行情查询或文本润色 | 转交其他流程，不进入 01—04 |
| 直接买卖建议 | 尝试收敛为具体投资假设；仍不具体则追问或不予受理 |
| 问题过大 | 先收窄对象、范围和观察维度 |
| 相对时间或动态对象 | 保留业务语义和刷新规则，不固化一次性结果 |
