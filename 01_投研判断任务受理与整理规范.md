# 01_投研判断任务受理与整理规范

## 1. 文档定位

本文档规范 01 阶段如何把研究员提出的问题、事件观察、材料线索或初步想法，整理成一个可以进入 02 判断结构梳理和本体匹配的投研判断任务。

01 阶段只负责把研究员的自然语言输入整理为一个**不预设结论、范围清楚、可验证、可反证、可重复执行**，并且足以交给 02 的投研判断任务。它不负责正式本体匹配、数据证据准备、推理执行或结论输出。

研究员不需要使用本体术语，也不需要填写对象类型、关系类型或状态变量等技术字段。研究员使用正常业务语言表达，系统负责整理。

`ready_for_matching` 只表示问题已经清楚到可以进入 02，不表示本体一定能够承接、证据已经充分或判断已经成立。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

本阶段负责把自然语言输入整理为投研判断任务，核心确认：

1. 研究对象与判断起点；
2. 研究范围边界（时间、地域、产业链、对象）；
3. 主辅轴（如适用）；
4. 前瞻窗口；
5. 交付要求。

为支撑 02 接手，01 还可记录观察维度、需求层面的初步线索（关注哪些业务连接、哪些信息方向可能影响判断）和必要假设。这些内容用业务语言表述即可，**不是**正式判断结构、本体映射或证据规则。

本阶段还负责识别关键歧义，判断任务是否可以输出《投研需求说明》并进入 02。

### 2.2 本阶段不负责

1. 不从零替研究员批量生成研究选题；
2. 不匹配正式本体类型、关系、变量或规则；
3. 不编写正式判断路径、路径节点、传导链或本体视图；
4. 不编写证据要求、取数规则、证据槽位或证据准备清单；
5. 不查询数据、创建实例或评价证据质量；
6. 不执行推理或形成投研结论；
7. 不把行情查询、资料总结或文字润色强行改造成判断任务；
8. 不直接回答“买不买”“涨不涨”等确定性问题。

正式判断结构、本体映射由 02 生成；证据准备要求由 03 生成。01 只确认研究需求，不提前替后续阶段完成结构或证据设计。

直接买卖问题只有在能够收敛为具体、可验证的投资假设时才进入本流程；改写后仍缺少对象、范围或验证条件的，应继续追问或判定为不适用。

---

## 3. 哪些问题可以受理

### 3.1 主任务类型

| 主任务类型     | 要回答的问题                            |
| --------- | --------------------------------- |
| 状态与趋势判断   | 某个行业、产品、材料、公司或资产当前处于什么状态，未来可能如何变化 |
| 事件或变化影响判断 | 某个政策、公告、事故、价格、供需或竞争变化是否影响目标对象     |
| 传导路径判断    | 某项变化是否会通过产业链、供应链、竞争关系、成本、价格或预期传导  |
| 假设验证与更新判断 | 某个已有判断、投资假设或研究逻辑是否仍然成立，是否需要修正     |

每项任务应明确一个主类型。复杂问题可以包含辅助分析角度，但不能同时设置多个彼此竞争的主线。

### 3.2 常见输入形态与研究方式

证据更新和历史复盘不是独立主类型：

1. **证据更新**是常见输入形态。新材料、新公告或新数据通常触发“假设验证与更新判断”，也可能触发与其内容对应的其他主类型。
2. **历史复盘**是研究方式。历史问题仍需先明确对应的主任务类型，再以当时的信息边界检验原判断，并用后续事实检查是否兑现。

以下问题不应直接进入本流程：

1. 只要求查询行情、罗列资料或润色文本；
2. 要求无条件预测价格、收益率或短期涨跌；
3. 要求内幕信息或未授权信息；
4. 范围过大且无法落到具体对象、观察维度、证据和反证；
5. 无法改写成可验证、可反证判断的其他请求。

### 示例

原始问题：

```text
存储芯片现在产能过剩吗？未来供需会怎么变化？
```

可以整理为：

```text
公开信息是否表明存储芯片行业当前存在供给过剩、库存压力或价格承压？
未来 6—12 个月，DRAM、NAND 与 HBM 的供需、价格、库存和产能利用率可能如何变化？
哪些需求修复、产能收缩或产品结构变化会支持供需改善，哪些新增产能、库存去化不及预期或终端需求走弱会削弱这一判断？
```

---

## 4. 如何把自然语言问题整理成判断任务

01 不要求研究员按固定格式提问。系统应从自然语言中整理以下六项，并据此形成一条不预设结论的规范化判断问题。

### 4.1 核心对象

明确围绕谁或什么进行判断。对象可以是行业、细分赛道、公司、产品、材料、技术、资产、投资假设、事件或政策。

业务名称可以先保留，但必须足以让 02 判断其可能角色。如果同一名称可能对应不同对象类型或判断路径，应先确认研究员关注的具体层级。

### 4.2 判断起点

明确问题从哪里开始。判断起点可以是：

1. 一个事件或政策；
2. 一份公告、新闻、材料或数据变化；
3. 一个行业现象；
4. 一个已有投资假设；
5. 一个当前状态或未来趋势问题。

01 不强制所有问题都从事件开始。

### 4.3 观察维度

明确需要观察哪些业务变化。常见维度包括供给、需求、库存、产能、价格、成本、毛利率、订单、交期、资本开支、市场预期、竞争格局、国产替代和技术替代。

01 使用业务语言记录观察维度，由 02 再映射为正式状态变量。

### 4.4 判断范围

范围应覆盖会影响 02 结构匹配的边界：

1. 时间：当下、一个季度、未来 6—12 个月或固定历史区间；
2. 地域：中国、海外、全球或特定辖区；
3. 产业链：材料、制造、产品、客户或终端；
4. 对象：行业、产品、公司、资产或特定集合。

“近半年”“当前”“未来 6 个月”“最新”“代表性龙头”等相对时间或动态对象，不能被固化为文档生成日对应的一次性日期或名单。生成《投研需求说明》时，应写清它们相对于什么时点变化、按什么范围或口径选择，以及后续运行时是否需要重新确定。

### 4.5 需求层面的初步线索

01 可用业务语言记录两类需求线索，供 02 梳理判断结构时参考：

1. **关注线索**：研究员希望重点跟进的业务连接或分析角度（如主辅轴关系、全球—本地映射等）；
2. **验证与反证关注点**：哪些信息方向可能支持或削弱判断、是否存在竞争解释。

这些线索只表达“研究需求上需要留意什么”，**不是**正式判断路径、路径节点、验证规则或反证规则。不得写成箭头传导链、节点清单或“对判断的作用”式证据逻辑表。

例如，判断供需改善时，可关注库存、价格、产能利用率和订单等方向；新增产能过快或终端需求走弱可作为削弱方向记录——但不在 01 定义它们如何验证、如何取数或如何阻断推理。

研究员未主动说明时，系统可补充不改变任务结构的研究方法假设，但必须标注为系统补充。补充内容若会改变主要对象、判断类型、范围或主辅轴，应先追问。

---

## 5. 改写与追问原则

### 5.1 改写原则

改写问题时应：

1. 保留研究员原始意图；
2. 不预设结论，不直接写成利好或利空；
3. 使用“是否”“在什么条件下”“哪些信息支持或削弱”等表达；
4. 明确核心对象、判断起点、观察维度、范围与需求层面的初步线索；
5. 区分研究员原话、用户材料和系统推断；
6. 将未经 03 验证的事实前提改写为待验证命题；
7. 保留相对时间和动态对象的原始语义，不替换成一次运行的绝对日期或临时名单。

例如，“近半年 HBM 已经上涨，原因是什么”应整理为“近半年是否存在具有代表性的 HBM 价格上涨；若存在，其主要驱动是什么”。

### 5.2 必须追问的情况

如果未确认信息会改变主要对象、判断类型、时间范围、产业链边界、主路径或交付用途，必须追问。常见情况包括：

1. 对象不清，不同理解会产生不同判断结构；
2. “影响”含义不清，无法判断关注供给、需求、成本、业绩、估值还是预期；
3. 时间、地域或产业链范围不清，且会改变主路径；
4. 问题过大，必须先收窄才能进入 02；
5. 研究员需要在多个实质不同的研究方向中作出选择。

所有需求**至少问 3 个问题**，用来确认问题范围。此项为硬性要求，不得以批处理、默认假设或可回滚假设替代追问流程；未完成至少 3 轮有效澄清前，不得输出 `ready_for_matching`。
每次只问一个最关键的问题，并说明该缺口会影响什么。研究员回答后重新检查剩余歧义；能从原话、已提供材料或可靠上下文提取的信息不重复询问。

### 5.3 可以不阻塞 01 的情况

如果未确认信息只是可由 02/03 按统一规则处理的执行细节，不改变任务结构，可以记录为可回滚假设或下游待解析项，不应阻塞 01。上述例外**不适用于**第 5.2 节规定的至少 3 轮澄清要求。

系统假设不得替代研究员对核心意图和范围的选择。每条假设应记录采用原因、来源和失效条件。

采用原因只写「研究员原话确认」「需求澄清后确认」或「系统补充」，**不得**保留对话选项编号（如「选项 C」「选项 D」）或其他对话痕迹。

---

## 6. 准入判断与处理状态

### 6.1 核心检查

| 检查项    | 通过标准                 |
| ------ | -------------------- |
| 判断问题   | 已形成一个不预设结论、可验证的问题    |
| 核心对象   | 已知道围绕谁或什么进行判断        |
| 判断起点   | 已知道问题从事件、状态、假设还是材料开始 |
| 观察维度   | 已知道需要观察哪些变化          |
| 判断范围   | 时间、地域、产业链或对象边界足以限定任务 |
| 需求层面初步线索 | 已记录关注方向及验证/反证关注点（不要求正式路径或证据规则） |
| 待确认项   | 不存在会改变任务结构的未决问题      |
| 可重复执行  | 相对时间和动态对象没有被固化为一次性结果 |

### 6.2 三种处理状态

| 状态                    | 是否生成文件 | 处理方式                      |
| --------------------- | -----: | ------------------------- |
| `ready_for_matching`  |      是 | 生成完整《投研需求说明》              |
| `needs_clarification` |      否 | 只在对话中逐项追问，不创建待澄清版文件       |
| `out_of_scope`        |      是 | 生成简短《不予受理说明》，不保留通过版任务结构字段 |

#### `ready_for_matching`

核心检查全部通过，可以进入 02。该状态不代表本体已匹配、证据充分或判断成立。

#### `needs_clarification`

存在会改变任务结构的关键歧义。不得进入 02，也不生成正式文件；只在对话中说明影响并逐项追问。澄清完成后再一次性生成正式文档，不能先生成待澄清版再追加答案。

#### `out_of_scope`

问题不属于本项目，或无法改写为可验证、可反证的判断任务。不进入 02；生成简短的不予受理说明，写明原因及可行的改写或转交方向。

---

## 7. 输出文件规则

### 7.1 通过版文件

`ready_for_matching` 时输出《投研需求说明》。

《投研需求说明》定义研究需求，不是推理报告，也不是某次运行的结果快照。YAML 头部必须包含 `status: ready_for_matching` 和 `status_reason`，用一句话说明核心检查已通过、需求确认已完成、可进入 02。

正文固定包含以下 8 章：

1. 研究目标与核心判断问题；
2. 研究对象与判断起点；
3. 核心观察维度；
4. 初步关注线索（需求层面，非正式判断路径）；
5. 研究范围与边界；
6. 初步线索：验证与反证关注点（需求层面，非证据规则）；
7. 必要假设；
8. 交付要求。

正文只写研究需求。`status`、`status_reason`、`readiness_checks`、`next_question` 只写在 YAML 头部，不出现在正文；不设置确认按钮、下一步流程、正式判断路径节点或已经成立的本体推理链。原始输入只保留在 YAML 中，不在正文机械重复。

第 4、6 章若出现箭头传导链、路径编号、「对判断的作用」式表格或类似 02/03 产物的内容，视为越过 01 边界，应收敛为业务语言的需求线索。

通过版文件名使用：

```text
<核心主题>投研需求说明-<YYYYMMDD>-<当日序号>.md
```

### 7.2 不予受理版文件

`out_of_scope` 时输出简短《不予受理说明》，不保留通过版正文第 1—8 章，也不保留通过版完整任务结构字段。YAML 结构与正文要求见 `输出模板/01_不予受理说明模板.md` 文首「模板使用说明」。

### 7.3 文件命名与 YAML 结构

文件命名规则、YAML 头部字段、动态范围填写规则、正文章节与完成检查，统一放在对应模板文首的「模板使用说明」中：

- 通过版：`输出模板/01_投研需求说明模板.md`
- 不予受理版：`输出模板/01_不予受理说明模板.md`

本规范优先规定处理逻辑、受理边界和生成规则；模板承接字段顺序、正文格式和示例。

### 7.4 模板说明不得进入正式输出

模板文件中可以保留维护说明、字段说明、示例和生成规则，但正式生成文件时不得输出模板维护说明，也不得把模板说明作为正文内容。

---

## 8. 动态范围与正式输出检查

正文先用研究员能够直接理解的语言写清范围，YAML 再保存同一含义的执行字段。两者不得一个写相对范围、另一个写一次运行的绝对结果。

`generated_at` 和文件名日期只表示文档何时生成，不是滚动窗口锚点。01 保留研究员提出的相对时间；03 在每次运行中记录实际采用的起止日期。01 不依据当前印象把动态集合写成固定名单。

动态范围填写规则见本节上文。正式输出完成检查见 `输出模板/01_投研需求说明模板.md` 文首「模板使用说明」第 4 节。

---

## 9. 与 02 的交接

02 使用 01 定义的规范化问题、研究对象、判断起点、观察维度、范围边界、主辅轴、前瞻窗口、需求层面初步线索和必要假设，梳理正式判断结构并匹配本体。

02 不得改变 01 的研究意图、主要对象和判断范围。01 写的是“近半年”“截至运行时的代表性龙头”等动态范围时，02 应保留其原意和选择口径，不得改写成文档生成日对应的绝对日期或一次性名单；需要在某次运行中确定实际日期和对象时，由 03 记录本次取值，不能反向覆盖 01 的需求定义。

如果 02 发现仍有会改变匹配结果的关键歧义，应退回 01；如果发现正式本体缺少对象、关系、变量或规则，应由 02 记录本体缺口或任务候选，不视为 01 受理失败。

```text
研究员输入
↓
01 改写并检查关键歧义
├─ needs_clarification：对话追问，不生成文件
├─ out_of_scope：输出不予受理说明，不进入 02
└─ ready_for_matching：输出《投研需求说明》并交给 02
```

---

## 附录：常见输入处理

| 研究员输入       | 处理方式                         |
| ----------- | ---------------------------- |
| 明确判断问题      | 直接整理成判断任务                    |
| 行业状态问题      | 收敛为状态与趋势判断                   |
| 事件或政策线索     | 收敛为事件或变化影响判断；涉及多环节作用时也检查传导路径 |
| 新公告、新数据、新材料 | 作为证据更新输入，通常收敛为假设验证与更新判断      |
| “这个逻辑还成立吗”  | 收敛为假设验证与更新判断                 |
| “当时判断对不对”   | 采用历史复盘方式，并明确对应的主任务类型         |
| 纯资料总结       | 只有用于更新或验证判断时才进入              |
| 纯行情查询或文本润色  | 转交其他流程，不进入                   |
| 直接买卖建议      | 尝试收敛为具体投资假设；仍不具体则追问或不进入      |
| 问题过大        | 先收窄对象、范围和观察维度                |
| 相对时间或动态对象   | 保留业务语义和刷新规则，不固化一次性结果         |
