# 01_semantic/03_knowledge_graph

- **回答什么：** 实例图/关系图的合同、稳定投影定义与端点约束（「图长什么样」）。
- **不放什么：** 单次 run 的图物化结果、UI 临时布局、研究步骤。
- **当前权威资产：** 本目录 `contracts/`；Runtime 边界在 `07_runtime/src/semantic/graph-contracts.ts`。
- **读面策略（wave D）：** 正式图存在且指纹一致时，Stage02–04 **从图单向投影**阶段 JSON；禁止阶段 JSON ↔ 图双向手工同步。
- **status:** migrating

索引：`registry.yaml`。
