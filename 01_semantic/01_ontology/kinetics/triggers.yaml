schema_name: ontology_trigger_catalog
schema_version: 4.0.0
status: active
namespace: core.kinetic
triggers:
  JudgmentChangeConditionMet:
    version: 1.0.0
    label_zh: 改判条件命中
    description: MonitoringRule 条件命中后提交判断重评动作并创建局部更新任务。
    observes: [MonitoringRule, StateVariable, Observation, Event]
    condition: monitoring_rule_matches_new_state
    action_ref: TriggerJudgmentReassessment
    run_as: monitoring-system
    enabled_by_default: false

