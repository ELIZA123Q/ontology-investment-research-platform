# Ontology (Semantic)

机器可读本体权威目录。

- **status:** active
- **source_of_truth:** `01_semantic/01_ontology/`
- **registry:** [`registry.yaml`](./registry.yaml)
- **compat:** `01_semantic/01_ontology/`（仅桥接与迁移账本）

写模型 YAML 只进 `models/`；词典正文在 [`../02_dictionary/`](../02_dictionary/)。
