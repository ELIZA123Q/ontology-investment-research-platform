---
description: 本机工作台唯一访问入口与启动约定
alwaysApply: true
---

# 工作台唯一入口

本仓库 Runtime 工作台**只认**这个地址：

**http://127.0.0.1:3000**

## 必须遵守

- 打开、走查、粘贴链接、浏览器自动化：一律用 `http://127.0.0.1:3000/...`
- **不要**用 `http://localhost:3000`（易被系统/VPN 代理劫持）
- **不要**用 `3001` / `3002` 等漂移端口；出现占用时先清端口再启，不要改端口凑合
- 启动开发服务：在 `runtime/` 下用 `npm run dev:singleton`（固定 `:3000`、清代理、杀掉旧进程）
- 后台任务：另开终端 `npm run worker`（worker 启动时会清掉继承的代理变量）
- 不要在受限沙箱里长期跑 `dev`/`worker` 去打 DeepSeek；模型连不上时优先在本机普通终端重启上述两个进程

## 路径示例

- 首页：`http://127.0.0.1:3000/`
- 某次运行 Stage3：`http://127.0.0.1:3000/runs/<runId>/stages/3`
