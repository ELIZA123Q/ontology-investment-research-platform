# 04_推理输出规范

## 1. 文档定位

本文档规范 04 如何综合 01 已确认的研究需求、02 已确定的判断结构与本体视图、03 已冻结的数据与证据，完成路径推理并输出两份配对产物：

1. **研究员推理报告**：回答“怎么看、为什么、接下来盯什么”。
2. **机器推理审计**：保存证据准入、观点分级、不确定性、本体命中、路径状态、状态变量和数据逻辑的可复核留痕。

04 必须先判断证据能支持到哪里，再执行路径推理。03 的覆盖率和准入状态是**过程完整性与可用范围**，不是业务结论强度。覆盖率高不等于关键判断条件已满足，也不等于可以确认完整阶段。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

1. 继承 01—03 的任务、对象、范围、判断时点和版本。
2. 检查输入是否配对，且未发生未说明的范围漂移。
3. 审阅 03 证据包的准入范围、使用上限、反证、冲突和缺口。
4. 使用 02 已选定的路径、状态变量、模板和规则逐段推理。
5. 形成观点级判断、整体系统等级、关键不确定性和改判条件。
6. 将业务表达与系统留痕分开，分别生成 Markdown 报告和 YAML 审计。
7. 将边界、本体或证据问题反馈给 01、02 或 03。

### 2.2 本阶段不负责

1. 不重新定义研究问题、对象范围或时间边界。
2. 不新增判断路径、状态变量、传导模板或推理规则。
3. 不搜索新材料、替换来源、创建新实例或改变 03 取数口径。
4. 不把未找到材料写成事实不存在，也不把证据不足写成反向结论。
5. 不输出个股评级、买卖建议、目标价、收益率预测或仓位建议。
6. 不将单次结果直接写回正式本体。

---

## 3. 正式输入与配对检查

04 必须同时读取同一任务链的：

1. 01 投研需求说明；
2. 02 研究逻辑；
3. 02 判断结构与本体视图；
4. 03 数据与证据准备文档；
5. 03 冻结快照摘要与 CSV；
6. 已引用的正式本体版本。

开始推理前必须确认：

- `task_id`、运行标识、02 文件指纹、判断时点和对象范围可解析且一致；
- 02 选中的每个状态变量都出现在 `state_variable_coverage.csv`；
- 02 路径、节点、规则、本体引用可解析；
- 03 准入状态和置信度上限可继承；
- 04 不对 01—03 冻结产物原地修改。

任务身份冲突、关键引用断裂、02 文件漂移、状态变量覆盖缺失或范围不一致时，相关路径不得进入正式推理。

---

## 4. 证据准入与推理边界

### 4.1 准入状态

| 03 准入状态 | 04 处理 |
|---|---|
| `normal_pass` | 在快照覆盖范围内执行完整路径验证 |
| `restricted_pass` | 只对允许范围执行受限验证，继承置信度上限 |
| `incomplete_pass` | 只使用 `evidence_gate_status=counted` 的变量—锚点单元；未计入单元保持不可推理，整体置信度上限为低 |
| `failed` | 不形成方向性业务观点，只说明失败原因、关键缺口和退回要求 |

### 4.2 证据覆盖与结论充分性必须分开

- **证据覆盖**回答：本次要求的变量—锚点中，有多少完成可追溯证据闭环。
- **结论充分性**回答：某个具体观点所需的关键前提、口径、时间序列、反证和传导条件是否已满足。

两者不得混成一个等级或一句结论。例如，大部分锚点已覆盖，仍可能因多层库存、取消率、有效产能或成交价缺失，无法确认完整周期阶段。

### 4.3 缺口影响

| 缺口类型 | 处理 |
|---|---|
| 阻断性 | 关键前提、关键观测、语义锚点或必要反证缺失；相关路径停止 |
| 限制性 | 主方向可判断，但强度、持续时间、对象或范围不清；缩小观点范围或降低等级 |
| 补充性 | 不改变当前方向，只影响解释完整度；放入审计或后续跟踪 |

---

## 5. 推理执行与观点形成

### 5.1 路径推理

每个关键路径节点都必须在审计 YAML 中记录：

1. 待验证问题；
2. 对象、关系、状态变量和观测；
3. 推理输入、支持证据、反证、冲突和缺口；
4. 应用的传导模板和规则；
5. 适用条件、削弱条件和阻断条件；
6. 路径状态与对后续节点的影响。

路径状态使用：`established`、`partially_established`、`weakened`、`blocked`、`insufficient_evidence`、`contested`、`not_applicable`。03 的路径就绪状态与 04 的路径推理状态必须分开。

### 5.2 观点级标签

正文不使用一个整体等级压平所有子结论，而是为每个可读观点选择下列标签：

| 正文标签 | 使用条件 |
|---|---|
| `已确认` | 对象、时点和口径内的事实或变化得到直接、可追溯支持 |
| `倾向判断` | 多个方向性信号指向同一方向，但强度、持续性或部分条件仍受限 |
| `条件判断` | 对未来窗口、情景或转折点的判断，必须同时写明触发条件和改判方向 |
| `暂不可判断` | 关键前提或必要数据缺失，不形成方向性结论 |

每个正文观点必须在 `claim_register` 中有对应记录。审计中仍保留标准 `conclusion_level`：`insufficient_evidence`、`observation`、`supported`、`contested`、`blocked`。

### 5.3 不确定性分级

| 等级 | 定义 | 展示位置 |
|---|---|---|
| `decisive` | 可能改变主观点方向、核心时间窗口或主要对象范围 | 正文第 4 部分，最多 4 项 |
| `boundary` | 只影响强度、幅度、局部范围或置信度 | 报告附录摘要或审计 YAML |
| `audit_only` | 只影响过程完整性、映射或复核 | 仅审计 YAML |

同一项不确定性在正文只定义一次。产品或环节段落不再重复整段限制说明，只在需要时说明该不确定性会如何改变对应观点。

---

## 6. 正式输出契约

04 每次必须生成两份同主题、同日期、同序号的配对产物：

```text
<核心主题>推理报告-<YYYYMMDD>-<当日序号>.md
<核心主题>推理审计-<YYYYMMDD>-<当日序号>.yaml
```

04 契约版本为 `1.3.0`。报告 front matter 必须保留任务、运行、01—03 引用、判断时点、范围、整体结论等级和置信度，并新增 `audit_ref`。报告与审计 YAML 必须双向引用。

### 6.1 研究员报告五段结构

1. **核心判断**
   - 第一句必须直接回答用户的核心问题，并给出统领后文的总框架。
   - 分别列出“已经可以判断”与“还不能判断”。
   - 不展示覆盖率、整体系统等级或流程状态。

2. **主导机制**
   - 用业务语言说明本次变化的本质、主要起点、传导机制和时间节奏。
   - 产品或对象分化前，必须先给出共同的总体机制。

3. **分对象/环节判断**
   - 只按业务机制真正不同的对象拆分。
   - 每个对象写“当前怎么看—为什么—什么变化会改判”，不复制该对象的审计记录。

4. **决定性不确定性与情景**
   - 只展开 `decisive` 不确定性，最多 4 项。
   - 每项写明它当前限制的观点、可观察信号、触发条件和改判方向。
   - 如需 base/upside/downside，在本节合并表达，不另建审计式章节。

5. **跟踪与改判**
   - 优先列出 3—6 项能够确认、削弱或推翻当前观点的变量。
   - 每项包含当前基线、信号与来源、改判触发条件、判断变化。
   - 调研问题可作为本节子节，最多 5 项，必须直接对应决定性不确定性。

正文后只设置**附录 A：证据边界与审计索引**，保存判断时点、范围、证据准入摘要、整体系统等级、关键缺口对观点的影响以及审计文件定位。

### 6.2 正文语言边界

在“附录 A”之前，禁止出现：

- `restricted_pass`、`incomplete_pass`、`normal_pass`、`failed`；
- `counted`、`blocked`、`profile`、`inference_allowed`；
- 本体 ID、路径 ID、节点 ID、覆盖单元 ID 或 CSV 字段名；
- “本体基座”“命中对象与关系”“路径状态与证据留痕”等审计章节；
- 证据覆盖率、整体 `conclusion_level` 或整体置信度。

正文可以使用具体数据和来源支持业务观点，但不得用证据数量或覆盖率替代关键条件的实质判断。

### 6.3 机器审计 YAML

审计 YAML 使用 `输出模板/04_推理审计模板.yaml`，必须包含：

- `metadata`：任务、运行、版本、判断时点、报告及 01—03 引用；
- `evidence_admission`：03 准入状态、覆盖计数、覆盖率和置信度上限；
- `overall_judgment`：整体系统结论等级、置信度、适用范围和降级原因；
- `claim_register`：正文观点、观点级标签、标准结论等级、路径、证据、不确定性和失效条件；
- `uncertainty_register`：不确定性等级、受影响观点、当前缺口、观察信号和改判方向；
- `ontology_context`：本体版本、本次使用的对象、关系、模板和规则引用；
- `path_results`：覆盖 02 所有关键路径节点；
- `state_variable_results`：覆盖 02 所有状态变量，并引用其全部 03 覆盖单元；
- `data_logic`：可复核的指标、公式、口径和时滞；
- `compliance_check`：输出边界和引用完整性。

审计 YAML 只保存对 03 快照记录的 ID 引用，不复制整张证据表或另建事实记录。

---

## 7. 降级、退回与完成标准

### 7.1 退回规则

| 情形 | 退回阶段 |
|---|---|
| 判断对象、问题范围或时间边界仍有关键歧义 | 01 |
| 关键本体定义、路径、变量或规则不适用 | 02 |
| 关键实例、关系、证据引用或口径错误 | 03 |
| 需要新增事实、来源或新快照版本 | 03 |
| 03 准入为 `failed` | 不进入方向性推理，退回 03 |

### 7.2 完成标准

只有同时满足以下条件，报告和审计状态才能标记为 `complete`：

1. 01—03 输入配对完整，范围未漂移。
2. 证据准入与业务结论充分性已分开记录。
3. 审计 YAML 覆盖所有关键路径、状态变量和正文观点。
4. 每个正文观点都可回到路径、证据和不确定性引用。
5. 正文第一句回答了核心问题，并明确区分“已经可以判断”与“还不能判断”。
6. 产品或对象分化之前已给出统一的主导机制。
7. 正文只展示最多 4 项 `decisive` 不确定性，且没有逐章重复。
8. 正文没有出现审计 ID、系统状态码、覆盖率或完整本体/路径表。
9. 报告附录说明“覆盖率高不等于周期阶段可确认”的具体原因，并引用完整审计。
10. 未新增 03 未冻结的事实或超过 `inference_allowed` 的判断。
11. 未输出交易建议、目标价、收益率预测或仓位建议。

---

## 8. 流程关系

```text
01 明确问题、对象和范围
↓
02 形成研究逻辑、本体视图和推理路径
↓
03 获取、归一并冻结实例、观测、证据、反证和缺口
↓
04 分开判断证据准入与观点充分性，执行路径推理
↓
研究员推理报告 Markdown + 机器推理审计 YAML
```
