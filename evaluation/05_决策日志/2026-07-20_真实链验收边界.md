# 2026-07-20 真实链验收边界（决策摘要）

**结论：** 两条真实工作台研究链已闭环并通过结构化导出校验；这证明 Runtime 能诚实停判，也能在受控证据下形成有边界的方向观察。当前结果**不等于**四案例 `formal_full` 研究增益评测完成，但**可以**用 DeepSeek 两模型跑 `pilot_light`（`single_vendor`）。

## 已证明

1. 模型失败不会迫使系统编造事实；失败分类与受控降级可回放。
2. Source → EvidenceFact → Judgment → Expression 存在事实级引用。
3. 同来源组多文档不会被误算为多个独立来源；强度可被确定性限制在 J1。
4. Stage05 过度表达会被已批准 Judgment 投影覆盖。

## 未证明 / 边界

1. `formal_full` 跨家族可靠率尚未验证；`single_vendor` 的 S/C 只能作区分能力参考。
2. 不发布正式 R/U/delta/S/C 的跨模型外推；不把工作台 A/B 分数外推为系统可靠率。
3. 复杂冲突链 `e79e5e8f-…` 仍待 Stage04—05 续跑。

## 决策（2026-07-20 更新）

- **评测硬门已下调**：`evaluation/03_执行/model_profiles.yaml` 使用 `run_mode: single_vendor`，只需 `deepseek-v4-pro` + `deepseek-v4-flash` 即可跑 `pilot_light`。
- 架构：不再横向扩本体；以真实失败归因驱动修改。
- 复杂冲突链续跑见 `evaluation/03_执行/续跑_e79e5e8f_冲突链.md`。
