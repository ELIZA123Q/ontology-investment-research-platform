# 复杂冲突链续跑清单

**Run ID：** `e79e5e8f-8282-4f40-9717-99ae2e5f7d6f`  
**问题：** 以 2025H1 公开信息裁决非 HBM DRAM 的价格上修、库存回补、政策抢运和现货谨慎是否足以支持可持续改善。  
**当前状态（截至 2026-07-20 验收报告）：** Stage01/02 完成；Stage03 三份 TrendForce 原文已抓取并对象级批准；Stage04—05、独立审阅、同证据基线、导出未完成（曾被 API 配额门拦截）。

## 续跑步骤（本机工作台）

1. 确认 `RESEARCH_MODEL_PROVIDER` / `REVIEW_MODEL_PROVIDER` 与 `.env.local` 配额可用；建议生产 DeepSeek、审阅切到 `openai_compatible` 以分散额度。
2. 打开 `/runs/e79e5e8f-8282-4f40-9717-99ae2e5f7d6f`，确认 Stage03 最新 approved attempt 仍绑定三条已批准事实。
3. Stage04：优先尝试模型生成；若超时，使用判断页「受控判断投影」逐单元提交，再经对象级批准。
4. 独立审阅：使用与生产不同的 `REVIEW_MODEL_PROVIDER` 或有独立性声明的人类审阅（禁止自审）。
5. 同证据基线 + 盲评；必要时 `recompute-metrics`（仅在冻结 hash 一致时）。
6. `POST /api/runs/:id/publish` 导出，跑 `validate_workbench_package.py`。
7. 将结论摘要写入 [`evaluation/05_决策日志/`](../05_决策日志/README.md)，不要只留在 gitignore 的 `04_报告/`。

## 成功标准

- Stage01—05 + 独立审阅 + 基线盲评 + 导出校验通过
- 不得把 forecast/likely 来源伪装为已实现事实
- 方法语义前置条件不得因「抓取成功」自动记为 pass

## 说明

本清单不代替真实 API 调用。在配额或密钥未就绪时，本项保持「工具化就绪、执行待凭证」。
