# 00A_高质量产出判别标准

## 1. 文档定位

本文件只规定 01—05 共用的质量定义、检查方式、正式发布条件和返工原则。每个阶段具体怎样执行、什么情况下通过以及失败后如何修正，以对应阶段规范为唯一权威：

- [`01_投研判断任务受理与整理规范.md`](01_投研判断任务受理与整理规范.md#7-质量门槛与返工规则)
- [`02_判断结构与本体视图规范.md`](02_判断结构与本体视图规范.md#8-质量门槛与返工规则)
- [`03_数据与证据准备规范.md`](03_数据与证据准备规范.md#14-质量门槛与返工规则)
- [`04_推理输出规范.md`](04_推理输出规范.md#10-质量门槛与返工规则)
- [`05_投研表达与交付规范.md`](05_投研表达与交付规范.md#9-质量门槛与返工规则)

本文件不重复阶段检查清单、字段要求、文件格式和模板说明。能够明确判断真假的规则由 Python 校验器执行；需要研究判断的内容由独立内容审阅完成。

## 2. 高质量的总体定义

高质量产出必须同时满足以下原则：

1. **不偷换问题**：对象、时间、范围、用途和最终要回答的问题与已确认需求一致。
2. **有判断价值**：产出能够识别关键分歧、主导机制、竞争解释和影响判断的信号，不是材料堆砌或模板拼贴。
3. **证据不越权**：每条重要观点不超过对应证据最多能支持的强度，反面证据、冲突和限制得到处理。
4. **读者可使用**：研究员不阅读 YAML 或审计字段，也能理解当前判断、依据、边界和下一步跟踪。
5. **可复核、可返工**：上游输入、证据、路径、观点、表达和审阅记录能够对应，失败时可以定位到具体阶段。

高质量不等于必须形成方向性结论。证据不足时，边界清楚、缺口明确、没有强行下结论的“暂不可判断”可以是高质量结果。

## 3. 三类状态必须分开

### 3.1 阶段状态

阶段状态只回答产物能否进入下一阶段，例如 `ready_for_matching`、`restricted_pass` 或 `complete`。它不证明产出质量，也不代表具体观点可靠。

### 3.2 产出质量

| 状态 | 含义 | 正式发布 |
|---|---|---|
| `draft` | 尚未完成检查或审阅 | 不允许 |
| `minimum_pass` | 达到最低内部流转条件 | 不允许 |
| `high_quality_pass` | 规则校验和独立内容审阅均通过 | 允许进入全链发布检查 |
| `return_required` | 存在必须修正的问题 | 不允许 |
| `stop_with_gap_report` | 当前条件不足，只能形成缺口说明 | 仅可发布高质量缺口结果，不得包装成方向性判断 |

内部过程可以出现 `draft` 或 `minimum_pass`，正式交付只能接受经全链校验确认的高质量结果。

### 3.3 结论强度

结论强度回答具体观点能够说到什么程度，与阶段状态和产出质量无关。04 必须为每条重要观点单独确定强度，不能用整体通过替代逐条判断，也不能用结论方向倒推证据质量。

## 4. 双层质量检查

### 4.1 规则校验

规则校验处理能够无歧义执行的要求，包括：

- 文件、版本、字段和枚举是否合法；
- 任务 ID、运行 ID、时间、对象和范围是否一致；
- 上下游引用、证据文件和内容哈希是否可解析；
- 证据、判断和表达是否超过上游允许范围；
- 是否缺少必需产物、审阅记录或正式交付引用；
- 应返回哪个阶段重新处理。

规则校验由 `validate_01_outputs.py` 至 `validate_05_outputs.py` 和 `validate_publish.py` 执行。规则未通过时，不进入正式内容审阅，也不得发布。

### 4.2 独立内容审阅

独立内容审阅处理不能仅靠程序判断的问题：

- 是否真正回答本阶段的问题；
- 是否有判断价值和研究增量；
- 是否存在逻辑空转、材料堆砌或模板拼贴；
- 是否充分处理反面证据、竞争解释和限制；
- 是否能被下一阶段或最终读者直接使用。

审阅必须使用 `independent_review_register / 1.1.0`，并记录产出责任人、独立审阅者、时间、无利益冲突声明、逐项准则结果、具体理由、结论和所审阅文件的内容哈希。审阅者不得与 `producer_id` 相同；不能只写“通过”。内容发生实质修改后，原审阅自动失效。

| 阶段 | 必须逐项审阅的内容 |
|---|---|
| 01 | 问题一致性、研究价值、范围与竞争解释、进入 02 的可交接性 |
| 02 | 判断结构、可证伪性、本体交接、03 的可执行性 |
| 03 | 证据适配性、来源可追溯性、反证与限制、允许的推理边界 |
| 04 | 结论与证据一致性、竞争解释、可复盘的改判信号、研究员可读性 |
| 05 | 与 04 结论一致性、论证质量、素材可追溯性、最终读者可用性 |

校验器验证的是已声明的产出者—审阅者分离、完整审阅准则和当前内容哈希；审阅者身份的真实授权与利益冲突事实仍须由团队治理机制保证，不能由仓库内的 YAML 自证替代。

## 5. 正式发布条件

正式发布必须同时满足：

1. 01—05 所需产物完整，且属于同一次任务和运行；
2. 每阶段规则校验通过；
3. 每阶段达到 `high_quality_pass`，或形成符合阶段规范的高质量缺口结果；
4. 独立内容审阅完整，审阅哈希与当前产物一致；
5. 03 明确证据能够支持到什么程度；
6. 04 每条观点未超过 03 的证据上限；
7. 05 未新增未经 03/04 支撑的事实、判断或更强表达；
8. 全链不存在未说明的范围漂移、引用断裂或越权结论。

`validate_publish.py` 是正式发布状态的唯一执行入口。产物中填写的 `quality_status` 只是阶段记录，不能单独证明可以发布；`minimum_pass` 只能用于内部流转，不能通过命令参数、函数参数或人工改写发布状态绕过。最终只有全链校验结果 `PUBLISHABLE` 才代表正式发布通过。

## 6. 返工原则

质量问题必须回到最早产生问题的阶段，不得只在下游改写措辞掩盖上游缺陷。

| 问题来源 | 返回阶段 |
|---|---|
| 问题、用途、对象、时间或范围不清 | 01 |
| 核心判断、问题拆解、变量、路径或竞争解释有误 | 02 |
| 数据、来源、口径、证据质量、反证或展示素材不足 | 03 |
| 证据已经具备，但推理、观点强度、分化判断或审计有误 | 04 |
| 上游判断正确，但报告结构、表达、图表组织或追溯关系不合格 | 05 |

返工记录必须说明具体问题、影响对象、返回阶段、需要修改的内容和重新检查方式。返回上游后，下游依赖产物必须重新校验；内容发生变化时，相关独立审阅必须重新执行。

## 7. 唯一发布原则

项目可以保存草稿、内部流转件和历史版本，但不能把它们作为正式结果交付。正式目录中一次运行只能有一份正式 05 交付物；任何未达到高质量要求的环节都必须继续修正、返回上游或形成高质量缺口说明，不能通过降低发布标准结束流程。
