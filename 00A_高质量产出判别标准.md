# 00A_高质量产出判别标准

## 1. 文档定位

本文档定义 01—05 各阶段产物何时可以视为**高质量产出**，以及何时必须返工、降级或停止发布。

本标准不替代 01—05 的阶段规范。01—05 负责说明“每一步怎么做”，本文负责说明“做到什么程度才算可以正式交给研究员使用”。

本项目中的产出分为两类：

1. **流程合格**：格式完整、引用链清楚、范围没有明显漂移，可以继续流转。
2. **高质量可用**：不仅流程正确，而且对研究员有判断价值，能够支持后续研究、证据准备、推理输出或复盘。

“合格”不等于“好用”。
“能进入下一阶段”也不等于“可以正式发布”。

高质量产出的目标是让研究员清楚知道：

* 当前到底要判断什么；
* 哪些逻辑能够区分不同解释；
* 哪些证据足以支持或削弱判断；
* 当前能判断到什么程度；
* 还不能判断什么；
* 下一步应该看什么；
* 什么信号会触发改判。

---

## 2. 总体原则

01—05 的高质量产出必须同时满足以下五项原则。

### 2.1 不偷换问题

每一阶段都必须承接上游已经确认的对象、范围、时间口径、判断含义和使用目的。

不得为了让后续步骤更容易执行，而悄悄改变问题。

例如：

* 不得把“当前处于什么周期阶段”偷换成“有哪些行业变化”；
* 不得把“未来什么时候结束”偷换成“未来有哪些风险”；
* 不得把“是否影响目标公司”偷换成“行业整体是否有变化”。

如果发现原问题过宽、对象不清或判断含义不足，应退回上游澄清，而不是在本阶段自行改写为另一个问题。

### 2.2 有判断价值

产出不能只是复述常识、罗列变量或总结材料。高质量产出必须说明：

* 真正需要辨别的核心矛盾是什么；
* 哪些解释可能同时成立，哪些解释互相竞争；
* 哪些信号能够区分主逻辑、反证和替代解释；
* 哪些条件满足后，判断可以增强；
* 哪些条件出现后，判断必须削弱、阻断或改判。

判断价值来自“能区分”，不是来自“写得全面”。

### 2.3 证据不越权

证据只能支持它实际能够支持的结论。

不得把以下内容直接包装成方向性判断：

* 材料存在；
* 单一来源观点；
* 局部事实；
* 弱相关信号；
* 未经口径对齐的数据；
* 没有反证检查的支持证据；
* 预测、展望或渠道判断。

证据不足时，可以正式输出“暂不可判断”或“只能形成条件判断”，但不能用风险提示、限制条件或模糊措辞掩盖证据不足。

### 2.4 读者可使用

研究员拿到产出后，应能直接理解本阶段的实际价值。

高质量产出必须回答：

* 当前完成了什么；
* 当前没有完成什么；
* 为什么这样处理；
* 对下一阶段意味着什么；
* 哪些内容可以使用；
* 哪些内容不能使用；
* 哪些地方需要返工或补证据。

不得把内部状态码、字段堆叠或机器校验结果直接当作业务表达。

### 2.5 可复核、可返工

每个阶段都必须留下足够清楚的判断依据，使后续能够复核：

* 为什么通过；
* 为什么未通过；
* 为什么只能受限通过；
* 应该退回哪个阶段；
* 需要补什么信息；
* 补完后如何重新校验。

高质量不是一次生成的结果，而是“生成—校验—审阅—返工—再校验”之后的结果。

---

## 3. 三类状态必须分开

项目中必须严格区分三类状态。

### 3.1 阶段状态

阶段状态回答的是：本阶段是否可以继续流转。

例如：

* 01 是否可以进入 02；
* 02 是否可以交给 03 准备证据；
* 03 是否可以进入 04 推理；
* 04 是否完成报告和审计输出。

阶段状态只说明流程是否可继续，不代表研究结论可靠。

### 3.2 产出质量

产出质量回答的是：本阶段产物是否达到正式发布或交付标准。

建议使用以下状态：

| 状态                     | 含义              | 处理                   |
| ---------------------- | --------------- | -------------------- |
| `draft`                | 草稿，尚未完成校验或审阅    | 不得正式发布               |
| `minimum_pass`         | 达到最低流程要求，可以继续流转 | 可进入下一阶段，但不得作为高质量产物发布 |
| `high_quality_pass`    | 通过确定性校验和独立语义审阅  | 可以正式交付或发布            |
| `return_required`      | 存在关键缺陷，必须返工     | 按返工路由退回对应阶段          |
| `stop_with_gap_report` | 当前条件不足以继续形成有效判断 | 输出缺口说明，不硬生成方向性结论     |

正式发布只允许 `high_quality_pass`。

### 3.3 结论强度

结论强度回答的是：04 的具体观点能判断到什么程度。

可以使用以下标签：

| 标签      | 含义                        |
| ------- | ------------------------- |
| `已确认`   | 关键前提、证据、反证和口径均满足，可以形成明确判断 |
| `倾向判断`  | 主方向有较强证据支持，但仍存在限制条件       |
| `条件判断`  | 只有在特定条件成立时，判断才可使用         |
| `暂不可判断` | 关键证据不足，不得形成方向性结论          |
| `被削弱`   | 原判断受到反证或冲突证据影响            |
| `被阻断`   | 关键前提不成立，后续路径不得继续推导        |

不得用阶段状态冒充结论强度。
不得用结论方向倒推证据质量。
不得因为包级通过，就默认所有观点都可以方向性输出。

---

## 4. 双层门禁机制

每个阶段都必须经过两层门禁。

### 4.1 第一层：确定性校验

确定性校验检查硬规则是否满足，重点包括：

* 文件版本是否正确；
* 必要字段是否完整；
* 引用链是否可解析；
* 任务 ID、运行 ID、判断时点和范围是否一致；
* 上下游文件是否配对；
* 对象、时间、地域、产业链边界是否发生漂移；
* 是否存在越权结论；
* 是否存在缺少来源、缺少证据或证据口径不匹配；
* 是否存在机器可识别的结构错误。

确定性校验未通过时，不进入独立语义审阅。

### 4.2 第二层：独立语义审阅

独立语义审阅检查产物是否真的好用，重点包括：

* 是否清楚回答了本阶段应回答的问题；
* 是否有判断价值；
* 是否存在逻辑空转、堆字段或模板化拼贴；
* 是否把线索、事实、推论和结论混在一起；
* 是否对反证、竞争解释和限制条件处理充分；
* 是否能被下一阶段直接使用；
* 是否符合研究员阅读习惯。

独立语义审阅必须说明具体依据，不能只写“通过”。

内容发生实质修改后，原审阅自动失效，必须重新审阅。

---

## 5. 四阶段高质量标准

## 5.1 01：投研判断任务受理与整理

### 5.1.1 高质量目标

高质量 01 应该把研究员的自然语言输入，整理成一个**有判断价值、边界清楚、可验证、可反证、可交给 02 搭建判断结构**的投研任务。

01 不只是把问题写长，而是要让人清楚：

* 到底要判断什么；
* 为什么现在要判断；
* 判断对象是谁；
* 判断范围到哪里；
* 结果准备用来支持什么研究动作；
* 哪些歧义已经消除；
* 哪些内容不在本次研究范围内。

### 5.1.2 高质量 01 必须做到

1. **核心问题不预设答案**
   规范化问题必须保持开放，不得预设“已经改善”“已经见顶”“一定影响”“不会影响”等方向性结论。

2. **判断落点明确**
   必须说明本次任务最终要形成哪类判断落点，例如：

   * 状态定位；
   * 趋势判断；
   * 阶段切换；
   * 影响强弱；
   * 路径成立性；
   * 假设验证与更新。

   不得只把问题整理成宽泛研究主题。

3. **对象和范围清楚**
   必须明确时间、地域、产业链、对象集合和不研究事项。
   如果存在产品、公司、地区、时间窗口或产业链环节的内部差异，应说明是否纳入本次范围。

4. **观察维度有判别力**
   初步观察维度不能只是罗列变量，而要说明这些变量可能如何影响判断。
   应至少覆盖：

   * 支持方向；
   * 削弱方向；
   * 反证方向；
   * 竞争解释。

5. **澄清有效完成**
   01 必须完成关键歧义校验。
   如果原始输入已经足以确认对象、范围、时间、用途和关键歧义，可以直接说明无需追加澄清的理由。
   如果仍存在会改变任务结构的歧义，必须继续追问，不得用形式化追问代替真实澄清。

6. **用途明确**
   应说明本次产物用于行业跟踪、假设验证、材料更新、投资逻辑复盘、后续证据准备，还是其他研究用途。
   用途不清会导致 02 难以判断分析深度和对象边界。

7. **结构化摘要与正文一致**
   YAML、摘要表、正文叙述中的对象、范围、问题、状态和不研究事项必须一致。

### 5.1.3 01 不得出现

* 把材料线索写成结论；
* 把研究员的方向性判断直接写成系统结论；
* 只改写语句，不明确判断对象和判断动作；
* 用“默认完成澄清”代替真实歧义消除；
* 将过宽问题硬塞进后续流程；
* 在 01 阶段提前设计正式本体、路径或证据规则；
* 正文和结构化字段不一致。

### 5.1.4 01 返工条件

出现以下任一情况，01 不得进入 02：

* 核心问题仍含糊；
* 对象边界不清；
* 时间窗口不清；
* 研究用途不清；
* 支持方向、反证方向和竞争解释缺失；
* 问题被偷换；
* 结构化摘要与正文不一致；
* 仍存在会改变任务结构的关键歧义。

---

## 5.2 02：判断结构与本体视图

### 5.2.1 高质量目标

高质量 02 应该把 01 的研究任务拆成**最小可判断结构**，并转化为 03 可以准备实例和证据的任务本体视图。

02 的重点不是展示框架知识，而是讲清楚：

* 这个问题真正要区分什么；
* 总问题应拆成哪些子问题；
* 为什么按这个顺序判断；
* 主路径如何成立；
* 什么会削弱、阻断或替代主路径；
* 每个关键判断需要什么证据；
* 本体能否承接这套结构。

### 5.2.2 高质量 02 必须做到

1. **形成判断脊柱**
   必须用一段话说明本次判断的核心矛盾和主导机制。
   判断脊柱应能指导后续 03 和 04，而不是停留在行业常识层面。

2. **形成最小问题树**
   子问题必须服务于总判断。
   每个子问题都应能够被证据支持、削弱、阻断或暂时搁置。
   不影响判断的问题不应纳入。

3. **明确主路径、反证和竞争解释**
   不能只有主逻辑。
   必须说明：

   * 主路径成立需要什么条件；
   * 哪些信号会削弱主路径；
   * 哪些信号会阻断主路径；
   * 是否存在能够解释同一现象的替代路径；
   * 如何用证据区分不同解释。

4. **明确停止条件和降级规则**
   必须说明什么时候可以停止补充证据，什么时候必须继续找。
   停止条件不能是“材料足够多”，而应是“关键判断单元达到最低验证条件”。
   如果最低条件不满足，应说明后续应降级为倾向判断、条件判断还是暂不可判断。

5. **证据需求可执行**
   每个关键判断单元都必须转化为 03 可执行的实例需求、状态变量、观测要求、证据要求或反证要求。
   不得停留在“关注需求、供给、价格”这类宽泛表达。

6. **本体映射服务研究逻辑**
   本体视图应承接研究逻辑，而不是用技术字段替代研究逻辑。
   研究逻辑 Markdown 应让研究员能读懂，本体 YAML 应让系统能执行。

### 5.2.3 02 不得出现

* 只拼贴框架库内容，没有本次问题的判断脊柱；
* 只有行业逻辑，没有最小问题树；
* 只有主线，没有反证和竞争解释；
* 把所有相关变量都纳入，导致问题树过宽；
* 本体字段看似完整，但无法支撑 03 证据准备；
* 路径节点之间没有前置、并列、竞争或阻断关系；
* 用技术字段掩盖研究逻辑不清。

### 5.2.4 02 返工条件

出现以下任一情况，02 不得进入 03：

* 判断脊柱不清；
* 最小问题树缺失；
* 主路径、反证和竞争解释无法区分；
* 关键判断没有最低验证条件；
* 证据需求无法执行；
* 本体视图无法承接研究逻辑；
* 发现 01 的研究问题或范围仍有关键歧义。

---

## 5.3 03：数据与证据准备

### 5.3.1 高质量目标

高质量 03 不看“材料多不多”，而看**关键判断是否取得足够质量的证据**。

03 应把 02 的判断结构转化为一次可复核的数据与证据准备过程，并明确告诉 04：

* 哪些判断单元证据充分；
* 哪些只能受限使用；
* 哪些不能推理；
* 哪些反证已经检查；
* 哪些竞争解释仍未排除；
* 本次证据包最多能支持什么强度的结论。

### 5.3.2 高质量 03 必须做到

1. **按判断单元准备证据**
   证据准备必须绑定 02 的路径、节点、状态变量、观测和反证要求。
   不得按主题泛搜，也不得用材料堆砌替代关键证据。

   每个关键判断单元应形成 `evidence_requirements`，并匹配 `evidence_recipes`、`evidence_baskets`、`source_profiles`、`acquisition_channels`、必要的 `proxy_indicators` 和 `evidence_readiness_assessments`。没有证据需求和取证配方，不得标记为高质量通过。

2. **区分核心结论证据门槛和覆盖率**
   覆盖率只能说明准备过程是否完整，不能直接说明结论是否充分。
   03 必须对每个核心判断单元单独判断：

   * 是否满足最低证据要求；
   * 是否满足口径要求；
   * 是否满足时间要求；
   * 是否完成反证检查；
   * 是否允许 04 输出方向性判断。

3. **来源质量可解释**
   必须说明来源是否直接、独立、可复核、足够新。
   不能用同源转述冒充交叉验证。
   不能把搜索结果、网页摘要或模型生成内容当作证据来源。

4. **口径匹配**
   对象、时间、地域、价格、产能、库存、订单、收入、利润等口径必须与 02 判断单元匹配。
   口径不匹配时，不得强行计入有效证据。

5. **主动检查反证和竞争解释**
   03 不只找支持证据，也必须查找削弱、阻断、冲突和替代解释。
   没有反证尝试的证据包，不得作为高质量通过。

6. **准入范围清楚**
   必须明确本次证据包允许 04 判断什么，不允许判断什么。
   `restricted_pass` 必须列出具体可使用的判断单元，不能只写“受限通过”。04 使用上限以 `evidence_readiness_assessments.csv` 为观点门禁来源，`judgment_unit_readiness.csv` 只作为汇总。

7. **搜索停止条件清楚**
   应说明为什么停止继续补充材料。
   停止原因必须与关键判断门槛有关，而不是因为材料数量已经很多或时间已经用完。

### 5.3.3 03 准入状态

建议使用以下准入状态：

| 状态                | 含义                  | 04 处理            |
| ----------------- | ------------------- | ---------------- |
| `normal_pass`     | 关键判断最低证据要求和关键路径全部满足 | 可在范围内执行完整路径推理    |
| `restricted_pass` | 只有部分判断单元达到方向性推理门槛   | 只允许对明确列出的单元方向性推理 |
| `incomplete_pass` | 证据不足以方向性判断，但足以说明缺口  | 只能生成高质量“暂不可判断”报告 |
| `failed`          | 关键证据、来源或引用链不足       | 不进入 04，输出返工要求    |

### 5.3.4 03 不得出现

* 覆盖率高，但核心 profile 最低要求没有满足；
* 两条所谓独立证据实际来自同一来源组；
* 没有反证尝试；
* 必需证据篮子缺失却输出方向性判断；
* 代理指标未披露，或把代理指标写成直接事实；
* 证据口径与 02 判断单元不匹配；
* 搜索仍在进行，却冻结正式准入；
* 用材料数量替代证据质量；
* 用弱信号支持强结论；
* 把预测、观点、展望和已发生事实混为一类。

### 5.3.5 03 返工条件

出现以下任一情况，03 不得进入方向性推理：

* 关键判断单元证据门槛未满足；
* 反证检查缺失；
* 来源不可复核；
* 口径不匹配；
* 时间窗口不匹配；
* 证据冲突未处理；
* 准入范围没有明确列出；
* 缺少 evidence readiness assessment，或 04 使用上限未绑定 readiness；
* 证据包无法说明 04 的使用上限。

---

## 5.4 04：推理输出

### 5.4.1 高质量目标

高质量 04 应该在证据边界内，形成研究员能直接使用的判断报告。

04 必须做到：

* 先回答怎么看；
* 再说明为什么；
* 再说明边界和不能判断的部分；
* 最后说明下一步盯什么、什么会改判。

04 不能用大量限制条件替代判断，也不能为了锋利而超过证据上限。

### 5.4.2 高质量 04 必须做到

1. **answer first**
   报告开头必须直接回答核心问题。
   不得先堆过程、背景、证据表和限制条件，让读者看完仍不知道当前怎么看。

2. **观点逐条接受证据门禁**
   每个方向性观点都必须单独对应证据门槛。
   某个观点证据充分，只放行该观点；不能因为包级通过，就连带放行其他观点。

3. **观点标签与证据强度一致**
   已确认、倾向判断、条件判断、暂不可判断、被削弱、被阻断必须与证据状态匹配。
   不能把条件判断写成确定结论，也不能把暂不可判断藏在风险提示里。

4. **事实、推论和未决问题分开**
   报告正文必须区分：

   * 已发生事实；
   * 来源观点或预测；
   * 系统推论；
   * 证据缺口；
   * 后续跟踪问题。

5. **对象分化清楚**
   如果不同产品、公司、地区或环节机制不同，必须分开判断。
   不得用整体平均掩盖结构分化。

6. **改判条件可观察**
   改判条件必须能被后续跟踪验证。
   不得只写“需求不及预期”“供给超预期”这类不可执行表述。
   应说明具体观察对象、变量、方向和触发含义。

7. **正文对研究员友好**
   报告正文应使用业务语言。
   机器状态码、内部 ID、字段名、路径编号和审计细节应放在审计文件或附录，不应干扰正文阅读。

8. **不越过合规边界**
   不输出个股评级、买卖建议、目标价、收益率预测或仓位建议。
   如涉及资产影响，只能在项目允许范围内表达研究含义和跟踪重点。

### 5.4.3 高质量 04 的基本结构

04 报告原则上应包含以下七项功能：

1. **一页摘要**
   不超过 500 字，回答：

   * 当前状态；
   * 方向或阶段；
   * 主导原因；
   * 下一验证窗口；
   * 主要分化；
   * 改判信号。

2. **核心落点**
   按“当前怎么看—为什么—下一步看什么”组织。

3. **主导机制**
   说明本轮判断由什么机制主导，哪些机制不是主导因素。

4. **对象分化**
   分产品、分环节、分公司或分区域说明不同判断。

5. **演进路线**
   说明当前阶段可能如何向下一阶段切换。

6. **改判闸门**
   列出触发维持、增强、削弱、阻断或改判的信号。

7. **可执行跟踪**
   明确后续应跟踪什么、看什么来源、用什么口径、何时复核。

### 5.4.4 04 不得出现

* 用包级准入状态支持所有观点；
* 在最低证据不满足时输出方向性判断；
* 把局部事实写成整体方向；
* 把材料摘要伪装成推理结论；
* 只写风险，不写改判条件；
* 只写限制，不给核心落点；
* 只写结论，不说明证据边界；
* 正文充斥内部 ID 和机器字段，研究员难以阅读。

### 5.4.5 04 返工条件

出现以下任一情况，04 不得发布：

* 开头没有直接回答核心问题；
* 核心观点超过证据上限；
* 观点标签与证据强度不一致；
* 对象分化被整体结论压平；
* 改判条件不可观察；
* 暂不可判断被包装成方向性判断；
* 报告不能让研究员知道下一步看什么；
* 审计文件无法复核正文观点来源。

## 5.5 05：分型研报正文

### 5.5.1 高质量目标

高质量 05 应该把 04 已批准的判断，按 01 指定的输出原型生成一份**内部研究稿正文**。

05 的表达质量应参考优秀外发研报：观点鲜明、主线清楚、结构完整、图表意识强、风险具体。但 05 的合规定位仍是内部研究稿，不输出外发评级、目标价、收益率预测、仓位建议或交易建议。

05 必须做到：

* 先让读者知道当前怎么看；
* 结构与 01 `delivery_archetype.primary` 匹配；
* 再把该原型需要的预期差、证据推进、强弱排序、测算、对象分化或演进路径写完整；
* 用图表和表格服务判断，而不是堆材料；
* 把不确定性收束为风险、改判闸门和跟踪项；
* 05 文件只保留研报正文，不放系统留痕；
* 所有核心判断和图表数据都能通过配对 04 审计与 03 快照回溯。

### 5.5.2 高质量 05 必须做到

1. **只消费 01—04 推理链**
   05 不得新增 04 未确认的判断，不得引入 03 未记录的新证据、来源或数据。缺失内容只能降级、标缺口或退回上游。

2. **内部稿定位清楚**
   文首只保留标题与副标题，先进入一页摘要和核心观点；判断时点、数据截止日、报告性质等口径信息放在文末说明。报告性质必须说明是内部深度研究稿，不是外发研报、评级报告或交易建议。

3. **观点锋利但不越权**
   标题、摘要和核心观点应有清晰判断句；但表达强度不得超过 04 的 `handoff_to_05` 或 claim 审计结果。正文层不得写入这些审计字段。

4. **原型结构匹配**
   05 不是一个万能模板。事件点评、行业动态点评、行业周期判断、公司业绩点评和主题深度研究应使用各自模板；主原型必须与 01 `delivery_archetype.primary` 一致。产业链映射收敛为行业/主题局部模块，政策影响收敛为事件点评或主题深度。

5. **预期差和叙事推进清楚**
   正文必须解释市场常见理解或误读、本文不同判断、证据如何一步步压实判断。不得只把 04 短报告扩写成材料摘要。

6. **图表服务判断**
   每张图表都必须说明服务哪个核心观点，正文只展示图表结论、制图数据、来源名称和限制说明；对应的 03 记录或 `display_data_candidates.csv` ID 由 04/03 外部校验承担。

7. **风险和改判具体**
   风险提示必须与当前判断相关，改判闸门必须可观察、可触发、可复核。

8. **对象落点明确**
   行业周期必须有强弱排序；事件点评、行业动态、主题深度和公司业绩点评必须有对象、环节、业务或影响变量的清晰落点。

9. **前台纯净**
   正文层不得出现 `C-01`、`JU-05`、`EV-01`、`RI-06`、`DDC-01`、`SRC-01`、`claim_id`、`source_id`、`quality_status`、`restricted_pass`、`full_reasoning_ready` 等本体、审计、流程和质量门槛字段。

10. **图表密度达到原型门槛**
   high_quality_pass 必须满足对应原型的图表/表格门槛。行业周期判断至少需要两个研报级量化图表，或一个研报级量化图表加一个研报级强弱排序表；其他原型按 05 模板库的最低图表要求校验。

11. **不越过合规边界**
   不输出个股评级、买卖建议、目标价、收益率预测或仓位建议。

### 5.5.3 高质量 05 的基本结构

05 内容稿只包含研报正文，但结构必须按输出原型分化：

| 输出原型 | 高质量结构重点 |
|---|---|
| 事件点评 | 事件事实、市场理解、本文判断、影响机制、对象影响、影响测算、改判信号 |
| 行业动态点评 | 最近变化、关键观察、预期差、数据验证、产业链影响、演进判断、跟踪指标 |
| 行业周期判断 | 周期位置、预期差、主导机制、证据推进、强弱排序、结束条件、改判信号 |
| 公司业绩点评 | 业绩与指引、超预期来源、分业务拆解、预测调整或经营弹性、后续验证 |
| 主题深度研究 | 为什么现在、空间测算、产业链拆解、竞争格局、演进路径、核心风险 |

### 5.5.4 05 不得出现

* 新增 04 未审计判断；
* 新增 03 未记录来源、证据或数值；
* 用外部常识填补项目推理链缺口；
* 把观察项、假设或证据不足写成确认结论；
* 出现本体编号、证据编号、审计字段、流程字段、质量门槛字段或系统留痕；
* 图表数据无法通过 03/04 外部校验追溯；
* 为了图表完整补造数据；
* 只写风险，不写改判闸门；
* 只写图表清单，不写图表服务的判断；
* 输出评级、目标价、收益率预测、仓位建议或交易建议。

### 5.5.5 05 返工条件

出现以下任一情况，05 不得作为高质量产物交付：

* 标题或摘要放大 04 判断强度；
* 正文层出现 `claim_id`、`evidence_id`、`source_id`、`quality_status` 或任何 `C- / JU- / EV- / RI- / DDC- / SRC-` 编号；
* 正文包含系统留痕、追溯表、质量自检或生成结论；
* 预期差无法追溯到 04 handoff 或 02/04 竞争解释；
* 图表数据无法通过 03 快照和 04 handoff 外部校验；
* 图表密度不足以支撑 high_quality_pass；
* 用缺失数据生成图表结论；
* 分化结构与 02/04 不一致；
* 风险提示与本次判断无关或过于空泛；
* 禁止写入内容没有被删除、降级或标为待补证；
* 出现外发评级、目标价、收益率预测、仓位建议或交易建议。

---

## 6. 返工路由

当产出未达到高质量标准时，应按问题类型退回对应阶段，而不是在当前阶段硬修。

| 问题表现                  | 返工阶段            |
| --------------------- | --------------- |
| 研究问题、用途、对象或范围不清       | 退回 01           |
| 判断脊柱不清、问题树混乱、竞争解释缺失   | 退回 02           |
| 本体无法表达关键机制            | 退回 02 或进入本体优化   |
| 证据覆盖高但核心结论证据不足        | 退回 03           |
| 证据口径不匹配、来源不可复核、反证缺失   | 退回 03           |
| 结论有依据但表达不清、结构不适合研究员阅读 | 重写 04           |
| 04 判断已成立但研报成稿、图表组织或追溯不清 | 重写 05           |
| 05 需要的可展示数据未在 03 冻结快照中登记 | 退回 03 或降级为缺口说明 |
| 05 需要表达的判断未在 04 中审计通过 | 退回 04 或删除该表达 |
| 04 观点超过 03 使用上限       | 退回 04 并必要时退回 03 |
| 全链条引用、版本或范围不一致        | 按断裂位置退回对应阶段     |

返工时必须说明：

* 具体问题是什么；
* 影响哪个判断单元；
* 应退回哪个阶段；
* 需要补充或修改什么；
* 修改后如何重新校验。

---

## 7. 发布规则

正式发布必须同时满足以下条件：

1. 01—05 使用同一套已生效规范版本；
2. 如规范发生破坏性变更，相关阶段产物已重新生成或重新校验；
3. 每阶段确定性校验通过；
4. 每阶段独立语义审阅通过；
5. 审阅绑定的内容哈希与当前产物一致；
6. 03 明确给出 04 的使用上限；
7. 04 每条方向性观点没有超过对应证据单元的使用上限；
8. 05 内容稿、04 审计文件、03 快照和引用链一致；
9. 没有未说明的范围漂移；
10. 没有越权结论。

全链发布校验最终只允许两种结果：

| 结果                | 含义        |
| ----------------- | --------- |
| `PUBLISHABLE`     | 可正式发布     |
| `RETURN_REQUIRED` | 输出分阶段返工清单 |

以下表述不得作为正式发布状态：

* “基本可以”；
* “建议人工复核后发布”；
* “格式通过但语义存疑”；
* “证据不完整但先发”；
* “后续再补”。

只要未达到 `PUBLISHABLE`，产物就只能作为草稿、返工件或缺口说明，不得作为正式研究员交付物。

---

## 8. 最小验收清单

每次全链发布前，至少完成以下检查。

### 8.1 01 检查

* 核心问题是否不预设答案；
* 判断落点是否明确；
* 对象、范围、时间和用途是否清楚；
* 支持、反证和竞争解释方向是否出现；
* 是否消除了会改变任务结构的关键歧义；
* 正文和结构化摘要是否一致。

### 8.2 02 检查

* 是否有清晰判断脊柱；
* 是否有最小问题树；
* 是否区分主路径、反证和竞争解释；
* 是否有最低验证条件；
* 是否有停止条件和降级规则；
* 本体视图是否能支撑 03 执行。

### 8.3 03 检查

* 证据是否绑定关键判断单元；
* 核心结论证据门槛是否单独判断；
* 来源是否直接、独立、可复核；
* 口径是否匹配；
* 是否主动检查反证；
* 是否明确 04 的使用上限；
* 准入状态是否与证据质量一致。

### 8.4 04 检查

* 开头是否直接回答核心问题；
* 每条观点是否单独通过证据门禁；
* 观点标签是否与证据强度一致；
* 是否区分事实、推论和未决问题；
* 对象分化是否清楚；
* 改判条件是否可观察；
* 研究员是否能知道下一步看什么。

---

## 9. 一句话标准

高质量产出不是“看起来完整”，而是：

> 问题没有被偷换，逻辑有判别力，证据不越权，结论有边界，读者能使用，后续可复核。
