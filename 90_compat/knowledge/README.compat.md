# knowledge (compat)

- mode: compat
- target_authority: 90_compat/methods/ + five domains
- write_policy: deny_new_writes
- migration_ref: 05_governance/01_架构/compat_policy.yaml

说明：本目录仅旧入口兼容桥；正文实体已删除。权威在 `90_compat/methods/`（及五域 registry）。
