# Ontology 3.0 迁移基线

本目录保存 Ontology 3.0 的只读迁移基线，供版本演进审计使用，**不是**当前正式权威，也不是 Runtime 兼容层。

| 文件 | 说明 |
|---|---|
| [`model_registry.yaml`](./model_registry.yaml) | 3.0 模型集合历史指针 |

当前正式入口：

```text
01_semantic_knowledge/01_ontology/platform_registry.yaml
```
