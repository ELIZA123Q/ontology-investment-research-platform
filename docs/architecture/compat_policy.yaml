schema_name: repository_compat_policy
schema_version: 2.0.0
status: active
policy: no_runtime_compat_tree
rules:
  - 历史样例和 eval 可保留旧 schema 用于只读回放
  - 旧 Runtime、Agent、Skill、Workflow 实现不得保留兼容副本
  - 所有新执行只进入 06_runtime
  - 迁移 adapter 必须读取资产权威，不复制资产正文
validator: 06_runtime/scripts/audit-cutover.ts
