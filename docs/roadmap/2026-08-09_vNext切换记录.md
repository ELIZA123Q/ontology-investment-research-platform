# 2026-08-09 vNext 全量切换记录

## 结果

vNext 已成为项目唯一 App 与 Runtime。旧平台代码不再留在项目树中，研究资产继续保留并通过 adapter 接入。

## 替换范围

- 旧 `06_runtime` 的 stage controller、agents、skills、workflow、runner、storage 与后台管理 UI 已退出项目。
- 原 `vnext/` 提升为新的 `06_runtime/`。
- `.claude/skills`、`03_agent_capability/01_agents`、`02_skills`、`03_tools` 的重复正文已清理并重建为治理索引。
- `07_workspace` 从路由空壳更新为真实产品登记。
- Deep Research 01—05 降级为 `template_only`，执行权威转移到 typed node catalog 与 planner。
- 根 README、CLAUDE、Cursor 入口和一键启动脚本已指向新 Runtime。

## 保留范围

- `01_semantic_knowledge` 的本体、词典、证据语义与图资产。
- `02_scenario_task` 的场景、任务定义、模板与历史合同。
- `03_agent_capability/02_skills` 和 MCP/A2A 协议资产。
- `05_control_evaluation/04_verifiers/fixtures/reference_runs/research_runs/` 的历史示范案例，以及 `05_control_evaluation/04_verifiers/fixtures/regression/` 的回归夹具。
- `05_control_evaluation/05_evals` 等规则、评测和迁移基线。

保留不等于继续按旧路径执行。所有保留资产必须通过 adapter、Context Builder、Tool 或 Eval 读取，不能重新成为第二套 Runtime。

## 恢复信息

- 完整源码/资产快照：`/private/tmp/ontology-platform-pre-vnext-cutover-20260809.tar.gz`
- SHA-256：`9f1f43d121db6449596170549d27b60bafeba49f5c48cabdae1f8d78e8d76d08`
- 旧 Runtime 临时恢复目录：`/private/tmp/ontology-vnext-cutover-20260809/06_runtime-pre-cutover`
- 旧镜像临时恢复目录：`/private/tmp/ontology-vnext-cutover-20260809/*-pre-cutover`

## 防回退规则

以下路径或模式不得重新出现：

- `06_runtime/agents`、`skills`、`workflow`、`runner`、`storage`
- `.claude/skills/*/SKILL.md`
- 固定执行 01→02→03→04→05 的 controller
- Agent、Skill 或 Tool 的第二份可执行 manifest
- 把 ContextPackage 或 TraceSpan 建成与 Event 平行的首版业务中心

验证命令：`cd 06_runtime && npm run audit:cutover`。
