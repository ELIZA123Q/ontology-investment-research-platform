# 研究角色

**Role = 一个任务中的责任位置。**  
**Agent = 承担某个 Role 的能力主体**（见 [`03_agent_capability/01_agents`](../../03_agent_capability/01_agents/README.md)）。

本目录只定义职责与 `eligible_actor_types`，不写死 `maps_to_agent`。vNext.1 可执行 Agent 仍仅 `research-lead`。
