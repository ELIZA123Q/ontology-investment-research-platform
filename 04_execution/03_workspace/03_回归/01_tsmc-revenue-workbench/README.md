# 真实工作台链回归夹具

来源运行：`536a5f6f-84ed-423a-9a52-be3bd0b25291`。

该目录冻结一条已通过校验的真实 `workbench_export`，用于持续回归来源血缘、
确定性规则、独立审阅、同证据基线和 A/B 绑定。`sources.json` 删除了大段
正文快照，只保留正文 hash、逐字引文和定位；它不是 `formal_pack`，也不证明研究增益。
