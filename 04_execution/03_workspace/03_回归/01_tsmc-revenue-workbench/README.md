# 真实工作台链回归夹具

冻结一条已通过校验的真实 `workbench_export`，用于持续回归来源血缘、确定性规则、独立审阅、同证据基线和 A/B 绑定。

## 给谁看

维护回归的工程师；研究员可跳过。

## 材料从哪来

- **来源运行 ID：** `536a5f6f-84ed-423a-9a52-be3bd0b25291`
- 本目录为只读冻结快照；`sources.json` 删除了大段正文，只保留 hash、逐字引文和定位

## 怎么用

作为 Runtime / 导出 / 校验的回归输入；阅读时把它当「当时导出」，不是最新研究结论。

## 怎么维护

需刷新时走上级 [`../README.md`](../README.md) 的 snapshot 脚本；刷新后注明新的 run id。
本夹具不是 `formal_pack`，也不证明研究增益。
