# v2 兼容入口：只读。Evidence 是应用运行模型，不再属于本体。
schema_name: ontology_evidence
schema_version: 2.0.0
status: deprecated_read_only
description: v1 evidence 合并视图；对象合同与执行规则分别由新权威维护。
depends_on: [common.yaml, semantic.yaml]
authority_targets:
- ../研究运行合同/evidence.yaml
- ../研究规则/evidence.yaml
