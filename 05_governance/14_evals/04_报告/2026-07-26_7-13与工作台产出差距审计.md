# 7/13 与重构工作台产出差距审计

> 历史差距审计：`7:13/` 对比快照已经退役，本文保留用于解释当时发现的工程根因，不是当前样例入口。当前可复现样例见 [`../../04_execution/03_workspace/README.md`](../../04_execution/03_workspace/README.md)。

日期：2026-07-26  
对比基准：`7:13/` 存储芯片周期完整产物  
Runtime 样本：`ac9e6a56-46fc-4f32-88a8-71cb22540d9c`

## 一、结论先行

差距不是单一模型问题。便宜模型会放大退化，但本次最主要的损失发生在工程链路：

1. 核心 Markdown 的确没有完整落到 prompt。旧加载器把每个文件正文里的第一个 `##` 错认成“下一个文件”的边界；一旦总上下文超预算，每份规范只剩标题和导语，却仍登记为已加载。
2. Stage03 把“联网取证、工具调用、来源登记、证据结构化、方法执行”压成一个超大任务。样本输入一度达到 236,943 字，先后遇到 Zod 不兼容、工具调用上限、取消，最终用全缺口 fallback 代替研究。
3. Stage04 输入已经没有事实，只能生成全 J0 fallback。Stage05 此时不可能凭写作恢复洞见，只能把缺口改写成报告。
4. 同一方法正文曾同时经 knowledge 与 selected method guidance 注入，02/03/04 上下文重复、注意力稀释，并增加超时概率。
5. 质量门偏重固定章节、字段、表格和字数，研究价值审查没有成为 Stage05 的直接通过条件。因此“合规形态”可能盖过“研究员是否能用”。
6. MCP 曾被当作 Stage03 high-quality 的必要条件。这混淆了获取通道与来源质量：公司 IR、监管/政府官网等公开原文可以是一手来源；MCP 不是证据等级。
7. 产物的 `knowledge_version` 过去只覆盖知识文件，没有覆盖 ontology 切片和 method guidance。三库虽被维护，治理版本不能证明它们实际影响了本次运行。

因此，本次差距的实质是：研究过程在 03 断裂，04/05 继续完成了流程形态。它不是“同一方法论换了模型后文风变差”，而是方法论的关键动作没有被可靠执行。

## 二、逐阶段对比

| 阶段 | 7/13 基准 | Runtime 样本 | 判断 |
|---|---:|---:|---|
| 01 受理 | 8,681 字 | 3,008 字 | Runtime 边界更短，尚不是主要断点；需要保留真正的增量问题与决策用途 |
| 02 结构 | 4,241 字 | 7,565 字 | Runtime 更长但不代表更好；输入 225,771 字，结构复杂度与上下文重复显著 |
| 03 证据 | 10,119 字 | 169 字 | 决定性断点：0 条非 gap 事实、26 条 gap、0 个来源，使用 deterministic gap fallback |
| 04 判断 | 3,363 字 | 148 字 | 上游无事实，7 个判断全部为 J0，使用 deterministic J0 fallback |
| 05 表达 | 8,164 字 | 1,916 字 | 只有缺口转述；缺少可形成 Research Edge 的证据—机制—含义链 |

长度只是症状，不是单独的质量标准。更关键的是：

- 7/13 的 05 对 HBM、通用 DRAM、企业级 NAND 和消费端 NAND 做了对象分化；
- 有明确数字、来源交叉验证、主导机制、竞争解释、投资含义、证伪条件和跟踪信号；
- Runtime 05 的信息上限已由 03 的“全缺口”锁死，无法形成上述研究增量。

## 三、运行证据

完整 Runtime 样本的失败序列：

- Stage03 v1：结构化接口不支持 `optional` Zod 字段；
- Stage03 v3/v4：在工具调用上限内没有提交合法结构化结果；
- Stage03 v6：人工确认了 `runtime-deterministic-gap-fallback`；
- Stage04 v2：模型第 3 轮超时；
- Stage04 v3：人工确认了 `runtime-deterministic-j0-fallback`；
- Stage05 v2：在 183,692 字输入上生成 1,916 字缺口型正文。

这说明 fallback 被当作“流程继续”的出口，而不是“研究暂停并返工”的边界。对研究员而言，全 gap 和全 J0 可以是诚实的内部缺口报告，但不能被包装成已经完成的高质量行业研究。

## 四、已落地的修复

### 1. 修复核心 Markdown 实际未加载

- `loadKnowledge` 保留逐文件正文，不再从拼接字符串猜文件边界；
- 预算截断后，`files` 只记录真正进入 prompt 的文件；
- 新增 `files_omitted_by_budget`，不再把预算省略伪装成已加载；
- 每文件截断优先保留方法、质量门槛、停止条件和边界章节，而不是只截文件开头；
- 增加回归测试，明确验证文件第一个 H2 之后的“质量门槛与返工规则”仍进入 prompt。

### 2. 让 ontology / methods / workflow 成为真实治理输入

- 02 注入通用与半导体语义、判断本体规范；
- 03 注入通用与半导体证据域规范，以及完整的通用、半导体、地缘真实来源操作手册；
- 04 注入通用与半导体判断规范及裁决附录；
- 方法正文只经 selected method guidance 按任务路由，移除 knowledge 重复注入；
- 每次运行记录 knowledge、ontology、method guidance、scenario card 和 structured keys；
- `knowledge_version` 升级为组合治理版本：维护 ontology / methods / workflow 任一有效输入，运行版本都会变化。

### 3. 把“来源质量”与“调用通道”分开

- 取消“没有 MCP 调用就不能 high-quality”的硬门；
- MCP、Web、公开网页抓取只记录获取通道；
- 来源质量由生产者权威性、正文抓取、hash、逐字引用、时间口径和独立来源组决定；
- 公司 IR、监管、政府官网等公开原文不再因不是 MCP 返回而被降级。

### 4. 把研究价值变成 Stage05 通过门

- 00A 五项从布尔打勾升级为 0–4 分，总分至少 16/20；
- 检查问题是否偷换、是否有差异化观点与机制链、是否证据越权、是否可直接用于更新判断、是否在证据不足时硬撑；
- “有 Research Edge 表格”不再自动等于“有判断价值”；
- 由 reviewer 模型审查，不再让生产模型单独自评；
- 任一研究价值失败直接 `return_required`，Stage05 不能 `high_quality_pass`；
- 论点章最低密度由 280 字提高到 500 字；7/13 基准最短论点章为 889 字。

### 5. 模型按研究价值分层

- 支持 01–05 独立模型路由；
- 当前本机配置为 Stage01 使用 `deepseek-v4-flash`，Stage02–05 使用 `deepseek-v4-pro`；
- 便宜模型负责受理整理，核心结构、取证、裁决和表达使用更强模型。

### 6. 增加研究质量回归审计

新增 `npm run audit:research-quality -- --run=<run_id>`，离线检查：

- 每阶段相对 7/13 的正文密度；
- deterministic fallback；
- Stage03 非 gap 事实、gap 和来源数；
- Stage04 是否全 J0；
- Stage05 是否过薄及研究价值审查是否通过。

旧样本会被明确判为失败，不再因为五个阶段都有 approved 记录而显示“流程成功”。

## 五、质量定义：什么才算对研究员有价值

Stage05 的目标不是把字段翻译成自然语言，而是让研究员能够：

1. 一眼知道当前判断与市场常见判断有什么不同；
2. 知道差异来自哪个主导机制，而不是材料数量；
3. 看清不同产品、公司或环节为何不同；
4. 区分事实、解释和仍未解决的竞争解释；
5. 知道哪些信号出现时应上调、下调或推翻判断；
6. 知道判断对标的筛选、盈利预期、估值或风险监测意味着什么；
7. 不读 YAML 和审计字段也能直接使用正文。

合规字段和追溯链是必要的安全底座，但只证明“没有明显越权”；研究价值检查证明“这份东西值得研究员花时间读”。两者不能互相替代。

## 六、仍需通过新运行验证的风险

本轮已经堵住“03 失败仍一路包装成高质量 05”的假绿路径，但离线修改不能证明外部模型和真实来源在下一次运行中一定成功。下一次端到端验收必须满足：

- Stage03 没有 deterministic fallback，且存在可核验的非 gap 事实和独立来源；
- Stage04 至少形成与证据强度一致的实质判断，或形成真正有决策价值的高质量缺口边界；
- Stage05 研究价值得分至少 16/20，并通过固定结构、论点密度、来源 grounding 与 Research Edge 检查；
- 回归审计通过；
- 研究员盲看时，能指出至少一个相对市场共识的增量判断、一个主导机制和一组改判信号。

若 Stage03 仍频繁触发工具上限，下一步不应继续提高一轮的上下文或工具次数，而应把 26 个证据要求拆成按判断单元/证据集合的可恢复微任务，再由确定性程序汇总。这是后续可靠性优化，不应再用 fallback 05 掩盖。

## 七、2026-07-27 付费验证补充：03 的问题比“拆批”更深一层

本轮新建付费 run `f19c7d94-d6ea-4796-acbb-61d63c41257c`，没有重复创建完整研究任务。

- Stage01 已用 `deepseek-v4-flash` 生成并通过；
- Stage02 已用 `deepseek-v4-pro` 生成并通过，共形成 5 个判断单元、10 个证据要求和 17 个实际方法应用；这说明 ontology / method / workflow 的结构层落地已明显改善；
- Stage03 首次真实运行按 3 个判断单元批次执行，但 3 批全部失败，最终诚实保留 10 个 gap、0 个来源、0 条可用事实，没有批准。

Stage03 新暴露的失败不是“批次还不够小”，而是三个运行合同没有形成闭环：

1. 模型没有调用 search、fetch 或证据 MCP，却提交了类似事实的草稿；Runtime 过去只在抓取后降级，但在抓取前就先跑完整 schema，导致整批失败。
2. 模型提交的事实缺少 source binding、subject/scope/time 元数据，`semiconductor_measurement` 也缺 nullable 字段；这些近似稿没有被分为“可抓取候选”和“只能是 gap”。
3. MethodApplication 返回了合同外状态；Runtime 没有按证据可用性保守归并。

因此新增了四层工程约束：

- **取证调用门**：补证调用在至少完成一次 search/fetch/证据 MCP 前，不暴露最终 submit 工具；如果供应商仍绕过工具，Runtime 只接受 gap。
- **抓取前事实资格门**：非 gap 必须绑定一个字段完整的候选来源；否则在 schema precheck 前降为 gap，模型记忆不得进入事实层。
- **抓取后核验门**：只有正文抓取成功、quote_verified 且来源可用的绑定，才保留为事实；否则再次降为 gap。
- **补证也分批**：工作台“按缺口补证”不再把全量问题重新压成一次大调用，而是只选择覆盖不足的判断单元，按 1–2 个单元执行可恢复批次。

这次验证进一步确认：低价模型确实会放大工具不调用、字段越界和研究压缩，但决定工作台能否稳定产出研究价值的，是 Runtime 是否把“调用过工具”“拥有可核验来源”“形成可用事实”“完成方法执行”区分成四个独立状态。任何一个状态都不能由表格完整或合规字段替代。

## 八、2026-07-28 继续验证：03 不是提示词问题，而是付费边界与取证编排问题

同一 run 后续四次 Stage03 请求均在模型生成前被供应商以
`Thinking mode does not support this tool_choice` 拒绝，token 记录为 0。继续重试不会提升研究质量，
只会制造更多失败版本。修复为 `tool_choice=auto`、取证前隐藏 submit 后，新的 Stage03 请求不再返回
400，但暴露出两个更关键的工程问题：

1. 一个证据批次在 thinking 阶段耗时约 7.5 分钟，模型尚未形成 Source Registry 即进入下一批；
2. worker 在第三批中断时，前两批结果仍只在进程内存，数据库中的 Stage03 仍为 `{}`。旧恢复逻辑会从
   阶段起点重跑，可能重复执行已经付费的批次。

因此本轮不再继续付费重试，而是完成以下架构修复：

- **批次级持久化检查点**：每批调用前先写 `in_progress`，调用后立即保存证据草稿、token、工具轨迹和
  complete/failed 状态；恢复时跳过已提交批次。
- **不确定扣费保护**：若 worker 在付费模型调用开始后、批次提交前中断，该批自动标为
  `interrupted` 并保留缺口，不自动重跑；若中断发生在付费调用前，才允许安全恢复。
- **取证阶段关闭 thinking**：来源采集是工具路由任务，不应在搜索前消耗长链推理；复杂推理留给证据
  归纳与 Stage04 裁决。
- **Runtime 确定性预取**：由 Stage02 EvidenceRequirement 生成主证据/反证各一条检索式，先把候选
  正文和 hash 登记到 Source Registry，再让模型做逐字摘录与结构化。模型未调用额外搜索工具时，只能
  使用 Runtime 预取集合；越界来源拒绝登记，事实降为 gap。
- **候选二次核验不占新增来源预算**：已登记但未核验的候选允许用模型摘录的逐字引文重新抓取核验，
  不能因“新增来源预算为 0”而永久停留在 limited。

真实 Stage02 还暴露了一个“结构已经生成、执行层却取错字段”的实例：5 个 JudgmentUnit 内部的
`evidence_requirements` 是 `ER-JU01-support` 一类 ID 引用，而 Stage02 顶层已经有 10 条内容完整、
主证/反证分离且要求两组独立来源的 EvidenceRequirement。旧 Stage03 忽略了这组权威对象，反而从
JudgmentUnit 的 ID 引用重新投影，形成 15 条空泛/重复要求。现已改为优先执行 Stage02 顶层
EvidenceRequirement；只有旧产物确实缺少顶层对象时，才把占位引用展开为可执行要求并与对象化反证
方向去重。当前三个批次都固定为一条主证据查询和一条反证查询。

这进一步说明三库“进入 prompt”仍只是最低层证明。真正落地必须同时满足：

1. 文件正文进入上下文；
2. 规范中的对象和方法被投影成可执行任务；
3. 执行状态与来源/证据状态由 Runtime 留痕；
4. 失败不能被表格完整、阶段完成或合规标签掩盖。

本地证据为：72 个测试文件、426 项测试全部通过；生产构建成功；标准注册表
ontology 19/19、methods 77/77、workflow 25/25、governance 63/63，Runtime 上下文 50 个文件全部就绪。
这些只能证明工程合同和离线行为；真实来源质量仍必须由下一次受控 Stage03 外部取证验证。

## 九、取证计划与 7/13 金标证据集合对齐

仓库中的原 `7:13/` 目录已经退役；当前可追溯金标是
`05_governance/14_evals/02_案例/RV-T01/evidence.yaml` 及对应 `system_artifact.md`。用该金标反查当前 10 条
EvidenceRequirement，得到以下覆盖关系：

| 判断单元 | 当前取证篮子 | 7/13 金标可提供的验证 | 仍应保留的缺口 |
|---|---|---|---|
| JU-01 HBM | 价格、库存、AI 部署、有效产出/良率、代际切换；需求/库存/良率反证 | Samsung HBM4 量产与稳定良率、Micron 长协和产能规划 | 跨厂 HBM 库存天数、客户库存、同口径价格仍不足 |
| JU-02 通用 DRAM | 合约/现货价、三层库存、终端出货、HBM 转产、减产执行；需求与库存反证 | Micron 实现价/位出货、TrendForce 合约价、Notebook 需求下修 | 跨厂晶圆投入、渠道库存和减产执行需继续取证 |
| JU-03 企业级 NAND | 企业 SSD 价格、数据中心部署、库存、利用率；订单/库存反证 | Micron NAND 价格/位出货、TrendForce 企业 SSD 产能配置 | 企业级客户库存和订单延期公开数据较弱 |
| JU-04 消费级 NAND | SSD/UFS 报价、PC/手机出货、渠道库存与订单；需求/库存反证 | TrendForce 消费应用缩减、Notebook 出货下修 | 手机与渠道同口径库存、消费级报价序列需补 |
| JU-05 HBM 挤占 | HBM/通用 DRAM 投片、封装分配、良率和晶圆面积；“挤占有限”反证 | Samsung 良率、Micron HBM 封装扩产只能提供间接机制证据 | 缺跨厂可比投片与资源分配，不能精确声称挤出比例 |

该对齐说明当前 Requirement 本身已经接近 7/13 研究所需的证据集合；真正差距在执行可靠性和
证据许可，而不是再增加一张字段表。特别是 JU-05，即便工作流完成也应允许保留“方向性机制、
不可量化挤出比例”的边界，不能为追求表格满格而伪造精确结论。

同时修复了批次内覆盖假绿：EB-01/EB-02 各含两个 JudgmentUnit，原确定性预取只生成第一单元的
主证和反证两条查询；现改为每个单元各一条主证、一条反证，因此两个双单元批次各 4 条查询，
JU-05 单单元批次 2 条查询。4 个候选来源名额先按查询各分配 1 个，再允许同一查询取得第二候选，
避免“来源数达到 4，但第二个单元仍为 0 覆盖”。

## 十、2026-07-28 端到端验收：01—05 已完成，但不是靠一次模型调用自动变好

最终验收 run 为 `f19c7d94-d6ea-4796-acbb-61d63c41257c`。01—05 均已确认，run 状态为
`complete`。本次没有再次创建研究任务，也没有在 Stage05 失败后继续重放付费模型；模型稿之后的
问题全部采用可审计的确定性修订解决。

| 阶段 | 最终已批产物 | 正文字符 | 实质结果 | 与金标的关系 |
|---|---|---:|---|---|
| 01 | `5b0feaf4-d38a-4b16-89eb-b8d68b7009ff` v1 | 2,584 | Flash 完成范围、对象、六个月窗口与“不做个股建议”边界 | 比旧金标更短，但已覆盖决策用途和分产品要求；不是主要质量瓶颈 |
| 02 | `04dae31b-d689-4d8d-815e-4985deea81cd` v4 | 4,808 | 5 个 JudgmentUnit、10 个主证/反证 Requirement、17 个实际方法应用 | 与 RV-T01 的“分产品、库存—供给—价格—需求”主脊柱一致；结构已可执行 |
| 03 | `bf12211e-d045-402f-958c-4489027eb290` v10 | 11,802 | 10 条逐字引文核验事实、10 个冻结来源、3 个独立来源组 | 不再是全 gap；但来源组和连续时间序列仍弱于 7/13 金标 |
| 04 | `8480074c-76e8-4efd-9b28-0fa3c852b509` v4 | 5,054 | 5 个 J2 分产品判断，全部保留竞争解释、证伪条件和表达权限 | 判断结构已达到研究可用；四个 A03 时间序列方法因缺连续序列诚实标为 degraded |
| 05 | `d061f4ba-6311-4f15-9971-7feefdc1043e` v2 | 9,266 | 5 个论点章、4 组实质 Research Edge、四类竞争解释、可执行跟踪表 | 字符密度高于 8,844 字的 formal-pack 金标；最终价值审查 19/20，但证据广度仍不应宣称超过金标 |

长度仍只用于识别异常薄稿。真正的通过依据是：问题未偷换、存在增量判断、证据未抬强度、研究员
能据此更新判断、没有为了给方向而硬撑。

## 十一、付费 Stage05 实际证明：高价模型也不会自动替代工程质量

付费 Stage05 原稿 `b7098ca5-f333-4721-8f31-e21e9f03d460` 使用 `deepseek-v4-pro`，单次任务耗时约
16.3 分钟，消耗 932,709 tokens（867,492 input + 65,217 output）。它生成了完整报告形态，并由
模型审查得到刚好 16/20，但仍存在以下不能通过的问题：

1. 5 个 `report_claim` 为 `RC-01…RC-05`，表达审计却登记为 Stage04 的 `C-01…C-05`，五条审计链全部断开；
2. 把 TrendForce 的每晶圆经济性估算写成已确认的因果机制，擅自补入“良率提升、代际切换导致反超”；
3. 多处出现“配置上”“投资者需”“估值溢价”“应警惕某类公司”，超出本次行业研究许可；
4. 两条 Research Edge 的“证伪条件”方向写反：实际列的是主判断继续恶化的信号，并不能证伪差异化观点；
5. 用 7 月 31 日作为数据截止日，晚于 7 月 28 日的实际执行时点；
6. Stage05 把 Stage04 中 `degraded` 的 A03 方法也列为报告依据，形成“路由过方法＝执行过方法”的假追溯。

因此最终 v2 不是简单润色，而是受控后编辑：

- 审计 Claim Register 每次从当前 Stage05 结构化字段重投影；
- 保留 `intensity_lifted=true` 风险标记，重建审计不能洗掉已有越权；
- 预测和估算恢复来源属性，删除未获证据许可的因果细节；
- 投资行动、个股估值和配置语言成为 high-quality 阻断项，免责声明不能抵消正文越权；
- 四个论点章显式补入竞争解释和裁决边界；
- 证伪条件改为真正能够推翻差异化判断的对立结果；
- Stage04→05 只投影 `executed` MethodApplication，`degraded/blocked` 仍留在上游审计但不得冒充报告依据。

这次结果直接验证：更贵模型改善了表达完整度，却没有自动解决证据许可、证伪方向、审计主键和方法
执行状态。高质量标准必须由 Runtime 强制执行，不能交给模型自评。

## 十二、最终 05 相对 RV-T01 的研究价值判断

最终报告的核心增量是：

> HBM 的需求和供给扩张仍强，但 TrendForce 估算的 DDR5 每晶圆经济性已反超 HBM；这构成产品分配
> 可能回摆的领先反向信号，因此不能把 HBM 出货扩张、价格持续性和边际盈利合并成一个“景气”指标。

这条洞见在 RV-T01 中不是主线，具有增量价值。报告同时把四类对象分开：

- HBM：景气延续，但经济性排序变化只是一项领先信号，尚未证明产能已经回流；
- 通用 DRAM：供给约束驱动的价格改善，不等于终端需求共振；
- 企业级 NAND：四类中证据最强，但需用 CSP 资本开支和实际部署排除渠道填充；
- 消费级 NAND：价强量弱，不能把全 NAND 合约价预测外推为消费需求改善。

相对金标，最终 05 的优势是差异化变量更鲜明、六个月触发条件更集中；不足是来源主要集中于
Samsung、Micron、TrendForce 三组，缺少 RV-T01 中更完整的跨公司经营样本、连续价格斜率和多层库存
序列。因此正确结论是“达到研究员可用并形成新洞见”，不是“所有维度均优于金标”。

## 十三、三库如何真正落实到 Runtime

本轮最终验证的不是“三个目录进入 prompt”，而是三类治理资产分别进入执行闭环：

| 治理库 | Runtime 落地点 | 本轮证据 |
|---|---|---|
| `01_semantic/01_ontology/` | 正式目录、指纹、领域业务参数、任务实例、关系选项、规则谓词、权威实例图 | `product:HBM`、四类产品与晶圆/封装环节进入 Stage02/04 实例图；未知裸 ID 仍拒绝 |
| `03_capabilities/05_method_libraries/` | 方法注册、候选召回、JudgmentUnit 路由、MethodApplication 状态、证据/裁决绑定 | 17 个方法应用中，A02/A01/A04 与裁决法实际执行；缺时间序列的 A03 保持 degraded，未进入 05 依据链 |
| `02_tasks/04_workflows/` | 分阶段上下文、质量门、返工/停止条件、审计投影、确认门 | 03 的来源冻结、04 的强度与表达许可、05 的研究价值和越权阻断均在确认时重算 |

组合治理版本和注入资产留痕用于回答“本轮读取了哪些文件”；结构化对象、方法状态、确定性规则和确认门
用于回答“这些文件是否真的改变了运行”。只有后者成立，三库维护才会持续提升产出。

## 十四、交付状态与仍未伪装为完成的边界

已导出本轮正式布局目录：

`04_execution/03_workspace/00_本机运行/formal/DRAM及NAND的价格与库存周期-20260728-1`

其中 05 主报告为：

`05-DRAM及NAND的价格与库存周期行业周期判断-20260728-1.md`

Stage05 与工作台 run 已完成；但正式发布包仍未宣称 `publishable`。`validate_run.py` 首次校验发现，
工作台 Markdown 到 formal-pack 的导出投影尚未补齐正式合同要求的 `task_id/execution_id` front
matter，且独立语义审查仍为 `needs_human`。这属于发布链缺口，不影响已经确认的研究正文质量，但必须
在正式发布前修复；不能因为目录已生成就把它称为“正式发布成功”。

## 十五、最终回归结果

- 研究质量回归：`PASS`；
- 全量 Runtime 测试：77 个测试文件、470 项测试全部通过；
- TypeScript：`tsc --noEmit` 通过；
- Next.js 生产构建：通过；
- 标准资产完整性：Ontology 20/20、Methods 77/77、Workflow 25/25、Governance 67/67；
- Runtime Context：50 个注册文件全部就绪；
- Ontology vocabulary check：通过；
- 语义单一权威审计：通过，0 violations。

这些结果证明工程合同和离线行为已经收敛；它们不替代对研究内容的判断。本轮内容通过还额外经过了
RV-T01 对比、逐条证据边界复核、竞争解释/证伪方向检查，以及 Stage05 19/20 研究价值审查。
