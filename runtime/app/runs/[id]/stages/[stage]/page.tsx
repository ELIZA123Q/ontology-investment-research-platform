import Link from "next/link";import { notFound } from "next/navigation";import { getRun,latestArtifact } from "@/adapters/db";import { StageWorkspace } from "@/app/components/stage-workspace";import { STAGES } from "@/engine/types";
export const dynamic="force-dynamic";const names=["问题定义","判断结构","来源收集与证据草稿","判断裁决","研究表达"];
export default async function StagePage({params}:{params:Promise<{id:string,stage:string}>}){const {id,stage}=await params;const n=Number(stage);const run=getRun(id);if(!run||n<1||n>5)notFound();const kind=STAGES[n-1];const artifactRow=latestArtifact(id,kind);const artifact=artifactRow?{...artifactRow}:undefined;const unlocked=n===1||Boolean(latestArtifact(id,STAGES[n-2],["approved"]));return <><div className="pagehead"><div><div className="eyebrow">Stage {String(n).padStart(2,"0")} · {artifact?.status || "not started"}</div><h1>{names[n-1]}</h1><Link className="backlink" href={`/runs/${id}`}>← 返回运行总览</Link></div>{n===4&&<Link className="button-secondary" href={`/runs/${id}/judgments`}>打开判断卡视图 ↗</Link>}</div><StageWorkspace runId={id} stage={n} artifact={artifact} unlocked={unlocked}/></>}
