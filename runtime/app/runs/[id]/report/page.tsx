import Link from "next/link";import ReactMarkdown from "react-markdown";import remarkGfm from "remark-gfm";import { latestArtifact } from "@/adapters/db";
export const dynamic="force-dynamic";
export default async function Report({params}:{params:Promise<{id:string}>}){const {id}=await params;const a=latestArtifact(id,"stage_05",["approved","needs_review"]);return <><div className="pagehead"><div><div className="eyebrow">Research expression</div><h1>研究报告</h1><Link className="backlink" href={`/runs/${id}`}>← 返回运行总览</Link></div>{a&&<a className="button-secondary" href={`/api/runs/${id}/report.md`}>导出 Markdown ↓</a>}</div>{a?<article className="card markdown"><ReactMarkdown remarkPlugins={[remarkGfm]}>{a.markdown_content}</ReactMarkdown></article>:<div className="card empty-state"><h2>报告尚未生成</h2><p className="muted">完成阶段 05 后，研究表达会出现在这里。</p><Link className="button" href={`/runs/${id}/stages/5`}>进入阶段 05</Link></div>}</>}
