"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function BaselineButton({runId}:{runId:string}){const [busy,setBusy]=useState(false);const [error,setError]=useState("");const router=useRouter();async function go(){setBusy(true);const r=await fetch(`/api/runs/${runId}/baseline`,{method:"POST"});const d=await r.json();if(!r.ok)setError(d.error);else router.refresh();setBusy(false)}return <div><button className="button-secondary" disabled={busy} onClick={go}>{busy?"正在生成基线…":"生成普通 GPT 基线"}</button>{error&&<div className="notice error">{error}</div>}</div>}
