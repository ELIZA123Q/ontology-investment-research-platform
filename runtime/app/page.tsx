import Link from "next/link"; import { listRuns } from "@/adapters/db";
export const dynamic="force-dynamic";
export default function Home(){const runs=listRuns();return <><section className="hero"><div><div className="eyebrow">Research runtime experiment</div><h1>让研究过程<br/>真正运行起来</h1><p>不是让 AI 自动写一篇报告，而是逐步验证：问题如何被定义、来源如何变成证据草稿、证据如何约束判断。</p><Link className="button" href="/runs/new">开始一次研究</Link></div><div className="metric"><strong>{runs.length}</strong><span>次可复盘的研究运行</span></div></section><div className="pagehead"><div><div className="eyebrow">Runs</div><h1>研究运行</h1></div></div>{runs.length?<div className="grid">{runs.map(r=><Link href={`/runs/${r.id}`} className="card run-card" key={r.id} style={{textDecoration:"none",color:"inherit"}}><span className={`badge ${r.status==="complete"?"":"warn"}`}>{r.status}</span><h3>{r.question}</h3><p>{r.domain==="semiconductor"?"半导体":"通用领域"} · 当前阶段 {r.current_stage}/5</p></Link>)}</div>:<div className="card"><h2>还没有研究运行</h2><p className="muted">从一个具体、可验证的问题开始。</p></div>}</>}
