# tasks/definitions

- **回答什么：** 标准 Task 定义（目标、默认角色、推荐 workflow、所需 skill 集合）。
- **不放什么：** 单次 run 实例、Agent 实现代码、评测协议正文。
- **当前权威资产：** 从受理主任务类型与评测案例抽象登记（见 `registry.yaml`）。
- **status:** migrating

本目录先落地任务登记，不直接承载运行时产物。
