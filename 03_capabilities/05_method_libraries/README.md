# 方法库（method libraries）

## 回答什么

- 研究框架、取证方法卡、裁决方法、表达方法，以及方法登记索引。
- Runtime / Agent 选方法、组知识包时默认读这里。

## 不放什么

- 本体模型 YAML（在 `01_semantic/01_ontology`）
- 证据来源速查 B00–B02（在 `01_semantic/04_evidence`）
- MCP 通道注册与 OPS_MCP（在 `03_capabilities/04_protocols/mcp`）
- 阶段产出模板（在 `02_tasks/04_workflows/deep_research/templates`）

## status

`migrating` — 自旧 `methods/` 并入；路径稳定后升为 `authoritative`。
