# Skill 注册

Skill 只保存“这类事情怎样做好”的程序性知识，不等同于 Runtime 步骤。

唯一可执行注册源：`07_runtime/src/capabilities/registry.ts`。

Context Builder 是 Runtime Service；Source Capture 是 Tool；引用完整性是 Verifier；重规划是 Research Lead Policy；模型客户端是 Provider Adapter。它们不再伪装成 Skill。
