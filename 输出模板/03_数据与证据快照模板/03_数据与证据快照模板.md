---
document_type: data_evidence_snapshot_summary
schema_version: 1.2.0
task_id: ""
execution_id: ""
snapshot_version: "1.2.0"
preparation_ref: "<核心主题>数据与证据准备模板-<YYYYMMDD>-<当日序号>.md"
snapshot_directory: "<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/"
admission: failed
evidence_quality_level: low
confidence_ceiling: low
allowed_scope: ""
coverage_unit_total: 0
evidence_backed_unit_count: 0
evidence_coverage_rate: 0.00
required_coverage_rate: 0.95
critical_node_gate_status: not_met
search_status: in_progress
generated_at: ""
---

# <核心主题>数据与证据快照

<!--
正式文件名应与所在文件夹同名：
<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>.md
-->

## 1. 整体结果

| 项目 | 本次结果 |
|------|----------|
| 证据包可信程度 | `high` / `medium` / `low` |
| 准入结果 | `normal_pass` / `restricted_pass` / `incomplete_pass` / `failed` |
| 证据覆盖单元 | `<evidence_backed_unit_count>/<coverage_unit_total>` |
| 证据覆盖率 / 门槛 | `<evidence_coverage_rate>` / `95%` |
| 关键节点门槛 | `met` / `not_met` |
| 搜索状态 | `threshold_met` / `source_tiers_exhausted` / `in_progress` |
| 可使用范围 | |
| 04 结论置信度上限 | `high` / `medium` / `low` |
| 能否进入 04 | |
| 02 研究逻辑与本体视图 | <填写可解析的文件引用和 ID> |

<用两三句话说明这批证据整体是否可信、可以支持哪些范围、最重要的限制是什么。>

> 证据包可信程度评价的是本次数据与证据是否可靠、完整、可追溯，不是对研究结论方向或成立概率的判断。

## 2. 为什么得到这一结果

### 2.1 支持这一等级的主要依据

- <关键来源、交叉验证、时间和范围匹配情况>

### 2.2 为什么不能更高

- <关键缺口、较弱来源、未解决冲突或范围限制>

## 3. 关键状态变量覆盖

| 状态变量 | 锚点对象/类型 | 映射状态 | profile 最低要求 | 覆盖状态 | 04 使用上限 | 主要缺口 |
|----------|---------------|----------|------------------|----------|-------------|----------|
| | | | `met/partial/not_met/not_applicable` | `covered/partial/missing/contested/blocked/not_applicable` | `full/directional/background_only/blocked` | |

<逐项覆盖 02 选中的所有变量；多个锚点可以合并展示，但不得掩盖对象间分化。>

## 4. 关键需求覆盖

| requirement_id | 对应问题或节点 | 状态 | 主要证据 | 反证检查 | 可信程度 | 限制 |
|----------------|----------------|------|----------|----------|----------|------|
| | | `covered` / `partial` / `missing` / `contested` / `blocked` | | | `high` / `medium` / `low` | |

## 5. 最重要的支持证据

| evidence_id | 支持对象或节点 | 核心事实 | 来源 | 质量状态 | 为什么重要 |
|-------------|----------------|----------|------|----------|------------|
| | | | | | |

## 6. 反证、冲突与缺口

| 类型 | 对象或节点 | 主要内容 | 影响 | 当前处理 |
|------|------------|----------|------|----------|
| 反证 / 冲突 / 缺口 | | | | |

## 7. 使用提示

- 可以使用：<允许进入 04 的对象、时间和路径范围>；
- 受限使用：<必须降级或附带说明的范围>；
- 不可使用：<缺少关键证据或存在不可解释冲突的范围>。

## 8. 快照文件

| 文件 | 内容 |
|------|------|
| manifest.csv | 任务、范围、版本和整体准入结果 |
| source_snapshot.csv | 本次实际使用的来源 |
| acquisition_log.csv | 获取尝试、失败和停止原因 |
| semantic_instances.csv | 对象实例 |
| semantic_relations.csv | 关系实例 |
| reasoning_inputs.csv | 事件、观测和信号 |
| evidence_records.csv | 单条证据及质量判断 |
| state_variable_coverage.csv | 状态变量×锚点实例覆盖与推理准入边界 |
| path_readiness.csv | 路径节点准备状态 |
| gaps_and_risks.csv | 数据、证据和来源缺口 |

> `path_readiness.csv` 只说明各节点的输入是否准备就绪，不判断传导路径是否成立；路径结论由 04 应用本体规则后形成。
