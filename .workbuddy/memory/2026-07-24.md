# 2026-07-24 工作日志

## 项目垃圾数据清理
- 删除 20 个 `.pending` 占位文件 (evaluation/03_执行/runs/*/contexts/)
- 删除 14 个 `.vitest-api-*.sqlite` 测试数据库残留 (instances/00_本机运行/)
- 删除临时续跑笔记 (`evaluation/03_执行/续跑_e79e5e8f_冲突链.md`)
- 删除 17 个 `.DS_Store` macOS 系统垃圾
- 删除 3 个失败评估运行目录: single_vendor 原始/v2/v3 (首次运行 calibration_failed)
- 清理所有 `__pycache__/` 和 `*.pyc` Python 缓存
- 保留: 7:13/、.next/、node_modules/
- 剩余运行: mock_unlock、pro_flash

## P0修复：重构项目上下文选入不足导致产出质量下降

### 根因
对比 main vs 重构项目分支：核心 MD 文档迁移完整，但 Runtime 上下文装配让 LLM 实际读到内容远少于原始文档。
1. `runtime_contexts.yaml` 只加载骨架文档（B00-B03、A00），未加载方法血肉（A01-A09、A01-A10）
2. `method_guidance.ts` 章节提取正则过窄（仅8关键词），大量关键内容丢弃
3. 字符预算不足（每个方法12K/总量72K，知识上下文72K）

### 修复
1. **runtime_contexts.yaml**: stage_03 +9个 A01-A09取证方法；stage_04 +8个 A01-A08裁决方法 +A00-附录3/4；stage_05 +00A_runtime_quality_card
2. **method_guidance.ts**: BODY_PER_METHOD_CHARS 12K→24K，BODY_TOTAL_CHARS 72K→128K；SECTION_PRIORITY_PATTERN 扩展7个新关键词
3. **context_assembler.ts**: knowledge 72K→128K，method_guidance 48K→96K

## P1修复：更多上下文装配问题

### 根因（第二轮排查）
1. **stage_02 仅加载5个骨架文件，框架库15+8个实际框架 .md 完全缺位** — LLM无法看到框架的 output_gates/变量/停止条件，自然无法做有效框架选择
2. **knowledge.ts 单文件硬截断 24000 字符** — 对 OPS 手册等大文件（15-19KB）形成第一层截断
3. **loadRoutedKnowledge 的 perFile 计算未计入 marker(~50字)+suffix(~19字) 开销** — 28文件累积多出 ~2000字符，导致末尾文件被 break 静默丢弃

### 修复
1. **runtime_contexts.yaml stage_02**: 从5文件扩充到28文件：+15个基础框架库（01_供需~15_宏观周期）+8个半导体行业主框架（IF-SC-01~IF-MAT-01）
2. **knowledge.ts**: 单文件截断 24000→40000，让 OPS 手册完整通过第一层
3. **context_assembler.ts**: perFile 计算引入 markerOverhead=80 预留，28文件 × ~4560 = 127,680 < 128,000 → 全部纳入

## P1修复（第三轮）：context_assembler 优先级系统 + 附录遗漏 + prompts 可读性

### 根因
1. context_assembler.ts 被外部重写为优先级加权分配系统，但 min=6000 导致 28文件仅加载 25 个（3 个半导体框架被丢弃）
2. stage_01 遗漏 01_附录1（场景参考，3094B）、stage_03 遗漏 03_附录1（字段速查，4733B）+ 03_附录3（素材检查，3321B）、stage_04 遗漏判断简报模板（4378B）
3. prompts.ts stage_03/04 极长单行（5352/3736 字符）极难被 LLM 解析

### 修复
1. **context_assembler.ts**: min 6000→4000，新增 OVERHEAD_PER_FILE=100 计入 marker+suffix，fallback chunk 也扣减 marker+suffix。28 文件全部加载（利用率 93%）
2. **runtime_contexts.yaml**:
   - stage_01: +01_附录1_任务受理字段与场景参考.md（6文件）
   - stage_03: +03_附录1（证据字段速查）+ 03_附录3（交付素材检查）（20文件）
   - stage_04: +04_判断简报模板.md（15文件 + 1 条件）
3. **prompts.ts**: stage_03 从 5352 字符单行拆为 8 个章节（方法继承 / 证据压缩 / MCP通道 / 来源登记 / 证据草稿 / 半导体领域 / 前置缺口 / 双产物），stage_04 拆为 6 个章节（方法收敛 / 判断档位 / 输出对象 / 半导体 / J0合法性 / 双产物）。内容一字不差保留，TypeScript 编译通过
4. **排除项**: knowledge_context 与 method_guidance 双重加载是刻意的架构设计（同数据、不同槽位、不同目的）；ontology 文件通过动态实例图生成，不走静态加载

## P1修复（第四轮）：同类问题批量修复 — Priority 正则遗漏 + 模板缺失 + Budget 冲突

### 诊断方法
逐阶段文件 × priority regex 交叉验证，发现 3 类模式化问题影响多个阶段。

### 根因
1. **Priority 正则遗漏 8 个关键文件**（context_assembler.ts）：
   - `/附录2/` 不匹配 `附录1` (`02_附录1`、`03_附录1`)、`附录3` (`03_附录3`、`A00-附录3`)、`附录4` (`A00-附录4`)
   - `/B00_|B03_/` 不匹配 `B01_通用来源速查`、`B02_半导体来源速查`
   - 这 8 个关键文件全部被降权为默认 priority=1，而非应有的 1.8
2. **模板 priority=0.7 过低**：`01_投研需求说明模板`、`04_判断简报模板` 等输出结构参考被压到最低优先级
3. **2 个模板未加载到 runtime_contexts.yaml**：
   - stage_02 缺 `02_研究逻辑模板.md` (8532B)
   - stage_03 缺 `03_数据与证据准备模板.md` (14188B)
4. **Budget 冲突**：`method_guidance` slot (96K) ≠ `BODY_TOTAL_CHARS` (128K)，方法提取产出 128K 但只放 96K
5. **stage_02 29 文件 knowledge 128K 溢出**：29×min_4000+marker = 文件被丢弃

### 修复
1. **context_assembler.ts priority regex**: `附录2`→`附录[1-4]`，`B00_|B03_`→`B0[0-4]_`，覆盖全部 8 个遗漏 + 模板 return 0.7→1.0
2. **runtime_contexts.yaml**: stage_02 +02_研究逻辑模板，stage_03 +03_数据与证据准备模板
3. **context_assembler.ts 预算**: knowledge 128K→144K（容纳 stage_02 29 文件），method_guidance 96K→128K（对齐 BODY_TOTAL_CHARS）
4. **验证结果**: 5 阶段全部文件 100% 加载，budget 利用率 99.7%~99.9%

### 第六轮（交付层刚性校验 + 框架文档填表合规 + 模板过度约束）14:30—16:30

**诊断方法**：3个agent并行诊断
- agent1: delivery/ + governance/ 交付体系 → 发现 validate_05_outputs.py 硬编码6个固定章节+2-5个中文编号论点章+17字段/句表达审计
- agent2: methods/ 扫描78个文件 → 14个文件含161项验收清单+33个文件含YAML登记块+42+文件含禁止条款
- agent3: (超时未完成)

**核心发现**：标准层（05_投研表达标准.md）本身强调"结构服从问题"，但校验层和模板层系统性背叛了这一原则：
- 校验层硬编码固定章节名称、禁止"核心观点"等正常章节名、要求合规声明3个特定关键词
- 模板层预写具体结论做章节标题（如"## 四、周期结束取决于信号共振，而不是单一日期"）
- 框架文档14个含"验收检查"清单的文件中，Top 5合计138项合规项

**P0 修改 validate_05_outputs.py（最大改动）**：
- `REQUIRED_FIXED_SECTIONS`(6个固定章节名) → `REQUIRED_CONTENT_FUNCTIONS`(4个内容功能正则)
- `ARGUMENT_CHAPTER_PATTERN`(强制中文编号) → 灵活 `## ` 章节计数，min=1, max=10
- 移除 `FORBIDDEN_SECTION_NAMES`（不再禁止"核心观点""一页摘要"等正常章节名）
- `EXPRESSION_REQUIRED_FIELDS` 13→7字段（删除 section/conditions_preserved/expression_scope_ref/preserved_caveat_refs/scope_relation/semantic_strength_review）
- 移除的字段改为有则校验、无则跳过
- `DISCLAIMER_MARKERS` 3个关键词 → 正则语义匹配
- `STRENGTH_UPGRADE_MARKERS` 增加否定语境豁免（"无法确定见底"不误杀）
- 移除 `FORBIDDEN_HEADER_FIELDS` 和 `REQUIRED_TAIL_MARKERS`
- 移除 `require_body_sections` 导入

**P0 修剪6个框架文档**：删除验收清单+YAML登记块+返工条件，保留核心研究逻辑
- `04_价格周期框架.md`: 397→354行（-12验收检查-9返工条件-YAML登记）
- `05_公司业绩弹性框架.md`: 420→371行（-16验收检查-10返工条件-YAML登记）
- `06_估值影响框架.md`: 319→284行（-16验收检查-YAML登记）
- `08_行业结构与竞争格局框架.md`: -13验收检查-8返工条件-YAML登记-null填充指导
- `07_预期差判断框架.md`: -14验收检查-YAML登记-null填充指导
- `00_基础框架编写与验收标准.md`: -整节"验收门槛"(含_task_selection模板)-整节"返工条件"-量化规则("至少2-4条禁止跳步")

**P1 修复5个模板文件**：
- 预写结论标题 → 功能引导 (`## 一、<用一句结论概括本章核心判断>`)
- 顶部添加弹性声明："章节标题、表格形式和章节数量均可根据研究需要调整"
- 移除原始模板中的强制性语言（"必须""不得"用"表达判断"替代）

**P1 降级00A**：
- `00A_runtime_quality_card.md`: "五条（必须同时满足）"→"五项原则（参考自检，非生成约束）"
- 删除"各阶段最低动作"强制表
- 标题从"质量短约束卡"改为"研究质量原则"

**P1 修复 prompts.ts**：
- stage_05: "固定章节（二级标题）…" → "内容功能（引导而非强制固定章节名）"
- 删除重复的"研报结构强制"块
- VERSION: v2.0-reasoning-first → v2.1-research-quality

**验证**：prompts.ts TS编译通过、validate_05_outputs.py Python编译通过、stage_03 validate通过

### 修改文件汇总
- `governance/03_校验/stages/stage_05/validate_05_outputs.py`: 硬编码校验→功能覆盖+灵活匹配+表达审计瘦身
- `governance/03_校验/00A_runtime_quality_card.md`: 生成约束→参考自检
- `runtime/engine/prompts.ts`: VERSION更新, stage_05章节引导从刚性→柔性
- `delivery/02_模板/05A-05E_*.md`: 预写结论→功能引导, 添加弹性声明
- `methods/02_判断结构/基础框架库/00/04/05/06/07/08_*.md`: 删除验收清单+YAML登记+返工条件

## P0修复（第五轮）：提示词过度限制导致产出质量下降

### 根因
三个独立 agent 并行诊断发现一致模式：
1. **prompts.ts 限制词密度 9.76/千字**（149个必须/不得/禁止/强制），LLM 变成填表机器人
2. **弱模型补偿** 强制"先列事实→再判断→两步走"的刻板推理，对强模型起反作用
3. **双产物强制** 每个阶段都要求 Markdown + YAML 双输出，30-50% 无意义 token
4. **knowledge.ts preferQualitySections** 100% 偏向约束性章节（质量/门禁/停止/禁止），方法指导被丢弃
5. **method_guidance.ts excerptMethodBody** 约束术语以 2.4:1 压倒方法术语
6. **01_供需判断框架** YAML模板 + 10项验收清单 + 6项返工条件 = 将框架写成合规手册

### 修复
1. **prompts.ts**：
   - PROMPT_VERSION: "v1.3.27-quality-enforcement" → "v2.0-reasoning-first"
   - shared: 移除"弱模型补偿"全部内容，从 1552 字符/17 限制词 → ~600 字符/8 限制词
   - stage_01: 1454→945 字符，移除 12 限制词
   - stage_02: 1975 字符单行（20 限制词）→ 1133 字符（3 限制词），移除 12 字段枚举和双产物强制
   - stage_03: 3342→1523 字符，移除字段类型约束和半导体领域强制规则块
   - stage_04: 2472→1386 字符，移除"强制登记"输出对象枚举
   - stage_05: 1499→966 字符，移除"禁止审计腔"章节
   - 总计：限制词 149→13（91%减少），prompt 尺寸 ~15,300→~8,700 字符（43%减少）
2. **knowledge.ts preferQualitySections**：
   - 新增 METHOD_GUIDE 正则（分析步骤/计算方法/数据来源/关键指标/推理步骤/研究路线/案例等）
   - 优先级：方法指导（tier 0）> 约束（tier 1）> 其他（tier 2），头预算 35%→25%
3. **method_guidance.ts**：
   - 新增 METHOD_SECTION_PATTERN（方法指导术语），从 3 层优先级变为 4 层：引导(0) > 方法(1) > 约束(2) > 其他(3)
   - BODY_PER_METHOD_CHARS 24K→32K
   - excerptMethodBody 验证：方法指导章节（分析方法→操作步骤）现在优先于约束（停止条件→质量检查）
4. **01_供需判断框架.md**：
   - 删除 YAML 系统登记块（~15 行"研究员可不读"）
   - 简化 6.3 节：删除 YAML 模板和 6 项返工条件 → 3 行核心信号
   - 删除第 8 节"验收检查"（10 项 checkbox）
   - 总计删除 ~1,750 字符合规开销

### 验证
- 全部 TypeScript 编译通过
- excerptMethodBody 优先级测试：分析方法(priority 1) > 停止条件(priority 2) ✓
- 各阶段 prompt 限制词密度从 9.76/千字降至 ~2/千字

## P0修复（第七轮）：上下文预算重构 + Prompt 操作指导 + 本体定义注入

### 根因（系统性对比 main vs 重构项目）
通过 3 个 Explore agent 并行审计 main 分支、runtime 代码、7:13 标杆产出，发现 7 个根因：

| 根因 | 优先级 | 核心问题 |
|------|--------|----------|
| RC1 知识库双重截断 | P0 | Stage02 的 25+ 框架文件在 144K 预算内每文件仅获 ~5.8K chars，output_gates/停止条件丢失 |
| RC2 本体预算过小 | P0 | ontology 预算仅 12K，实例图摘要被硬截断；本体定义完全未注入 |
| RC3 Prompt过于压缩 | P1 | 每阶段 2-14 行 vs main 分支数千字 MD 方法论 |
| RC4 本体定义未进prompt | P1 | 只注入实例图摘要(ID列表)，不注入本体语义结构 |
| RC5 方法正文静默跳过 | P1 | resolveMethodBodyFile() 返回 null 时无日志 |
| RC6 Prompt与方法正文松耦合 | P2 | 方法正文在 JSON 深处，prompt 只给行为指引 |
| RC7 补证prompt不一致 | P2 | 独立函数风格与 stage_03 主prompt不一致（已复用shared，无需修改） |

### 修复（4 个 agent 并行 + 主线程集成）

**A1: 知识库按语义路由精确加载**
- `runtime_contexts.yaml`: Stage02 框架从 assets(29文件) 移到 conditional_assets(18条规则, 23文件)，按 judgment_types 条件加载
- `knowledge.ts`: 单文件截断 40K→60K；新增回退逻辑（judgmentTypes 为空时加载全部条件资源）
- 效果：Stage02 从全量 25+ 框架降到 3-5 个相关框架，每文件获 30-40K 预算

**A2: 本体预算扩展 + 定义摘要注入**
- `context_assembler.ts`: ontology 12K→36K, upstream_json_soft 120K→96K（平衡）
- `ontology_tools.ts`: 新增 `ontologyDefinitionSummary()` 从 5 个 YAML 文件提取对象类型/关系类型/规则精简摘要(~9K chars)，在 `ontologyContextForPrompt()` 中注入

**B1: Prompt 操作指导增强**
- `prompts.ts` shared: +5条关键产出纪律（J0-J4上限、MA绑定、竞争解释、数字可追溯、结构化叙述）
- stage_02: +框架裁剪操作指导(5条) +变量定义操作指导(3条)
- stage_03: +证据压缩操作指导(5条)
- stage_04: +裁决操作指导(5条)
- stage_05: +研报密度要求(Research Edge 5要素、论点章3必须、不允许骨架稿)

**B3: 方法正文缺失诊断**
- `method_guidance.ts`: resolveMethodBodyFile() 增加 [METHOD:WARN] 日志；loadSelectedMethodGuidance() 增加 skippedIds 统计

### 验证
- TypeScript 编译通过（3个已有错误非本次引入）
- context_assembler.test.ts 4/4 通过, quality_enforcement.test.ts 5/5 通过
- YAML 结构验证：Stage02 = 6 通用 + 18 条件规则(23文件)

### 修改文件汇总
- `runtime/engine/context_assembler.ts`: 预算调整(ontology 36K, upstream 96K)
- `runtime/engine/knowledge.ts`: 截断 40K→60K, 条件资源回退逻辑
- `runtime/engine/ontology_tools.ts`: +ontologyDefinitionSummary(), +import(readFileSync/YAML/repositoryPath)
- `runtime/engine/prompts.ts`: 5个阶段 prompt 增强, shared 段增强
- `runtime/engine/method_guidance.ts`: resolveMethodBodyFile/loadSelectedMethodGuidance 诊断日志
- `governance/01_架构/runtime_contexts.yaml`: Stage02 框架条件加载重构

## 用户旅程改进：8项前端导航与信息架构优化

### 改动1：新建 Stage 1 审阅页
- 新建 `runtime/app/runs/[id]/scope/page.tsx`
- 展示核心判断、时间范围、前提三分法（已知事实/用户假设/待验证假设）
- 已确认/待确认状态 badge + "编辑范围"按钮

### 改动2+4：RunNav 统一导航 + 展开为 8 项
- `run-nav.tsx`: "范围"链接从 `/stages/1` → `/scope`，`activeSceneFromPath` 同步更新
- items 数组从 6 项扩展到 8 项（增加历史、关系图）
- 移除 `⋯` 下拉菜单（`run-nav-more` details 元素）

### 改动3+5：Stage 编辑器审阅导航
- `stages/[stage]/page.tsx`: Stage 1 添加 reviewHref 指向 `/scope`
- Stage 1 也获取 reviewable 状态
- 所有阶段编辑器：reviewable 时显示"返回审阅 →"，否则显示灰化"生成后进入审阅"

### 改动6：Radar 永久"新建独立研究"按钮
- `radar-dashboard.tsx`: radar-head-actions 区域添加 `<Link href="/runs/new">新建独立研究</Link>`，始终可见

### 改动7：概览页提升 A/B 对照和历史
- `runs/[id]/page.tsx`: 将 Compare 和 History 从折叠 advanced-tools 面板提升为独立可见卡片行
- Object-set 和 Experience 保留在折叠面板中

### 改动8：知识库 Tab 精简
- `ontology/page.tsx`: "跨研究"和"治理"分组用 `<details>` 默认折叠，当前活跃 Tab 所在分组自动展开
- `globals.css`: 新增 `details.ontology-tab-group` 样式支持折叠

### CSS
- `globals.css`: 新增 `.button-muted` 类用于灰化按钮状态

### 验证
- TypeScript 编译通过（修改文件无类型错误）
- Next.js 编译通过（✓ Compiled successfully）
