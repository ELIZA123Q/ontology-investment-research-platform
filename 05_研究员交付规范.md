# 05_研究员交付规范

## 1. 文档定位

05 是在 01—04 全链完成后、面向研究员的最终可读交付层。它把 04 推理报告中的判断边界，扩展为符合 01 三问确认交付形态的长文研究产物（当前支持「主题深度研究」）。

05 不负责重新取证、重新推理或修改 03 冻结快照；只负责在 04 已确认的观点强度内，组织研究员可直接使用的叙述、测算框架、产业链拆解、竞争比较、图表和跟踪清单。

正式结构以 `输出模板/05_主题深度研究模板.md` 为准。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

1. 继承 01 确认的研究对象、事件口径、前瞻窗口和交付形态；
2. 在 04 观点边界内写成稿级主题研究；
3. 提供一页摘要、核心观点、空间测算、产业链拆解、竞争格局、演进路径、图表数据、关键跟踪指标和风险提示；
4. 引用可复核来源，并说明口径与样本边界；
5. 确保正文不暴露系统 ID、准入码、路径编号和机器字段。

### 2.2 本阶段不负责

1. 不新增未在 03 冻结的证据或实例；
2. 不突破 04/03 给出的 `allowed_04_output` 与结论强度上限；
3. 不输出个股评级、目标价、收益率预测或仓位建议；
4. 不把代表公司样本外推为全行业排序或投资建议名单；
5. 不替代 04 推理审计的可复核留痕。

---

## 3. 入口条件

开始 05 前必须满足：

1. `validate_publish.py` 对同一运行目录的 01—05 校验通过（或 `--through 04` 且运行目录无 05 产物时仅要求 01—04），或等效地各阶段 `quality_status=high_quality_pass`；
2. 01 三问中的 `delivery_landing` 已确认交付形态（如主题深度研究）；
3. 若拟正式发布，03 快照内 `04_05_materials/05_material_readiness.csv` 中相关素材单元达到 `report_grade_ready`（由 `validate_05_materials.py` 检查）。

---

## 4. 输出文件

```text
05-<核心主题>主题深度研究-<YYYYMMDD>-<当日序号>.md
```

可选省略 `05-` 前缀；`validate_05_outputs.py` 两种写法均接受，但同一运行内应保持一致。文件名中的 `<交付形态>` 当前固定为 `主题深度研究`。

正文必须包含以下章节（与模板一致）：

1. 文首元信息（判断时点、前瞻窗口、研究对象、事件口径）；
2. 一页摘要；
3. 核心观点；
4. 一、为什么现在研究这个主题；
5. 二、空间测算；
6. 三、产业链拆解；
7. 四、竞争格局与壁垒；
8. 五、演进路径；
9. 六、图表与关键数据；
10. 七、关键跟踪指标；
11. 八、风险提示；
12. 主要资料来源；
13. 文末合规声明（不构成证券评级、收益承诺或交易操作建议）。

---

## 5. 质量门槛

### 5.1 最低要求

1. 文件名与交付形态匹配；
2. 正文章节齐全；
3. 观点不高于 04/03 允许强度；
4. 不含投资建议禁用语；
5. 不含 04 正文同类系统术语。

### 5.2 正式发布

拟作为正式研究员交付物时，还应满足：

1. 与 04 核心判断一致，无范围漂移；
2. 图表与关键数据可追溯到 `report_grade_ready` 素材或 03 证据记录；
3. 跟踪信号可观察、可复盘；
4. 代表公司/类别边界写清楚。

---

## 6. 与相邻阶段的关系

```text
04 推理报告（观点边界与审计留痕）
→ 04_05_materials 成稿素材（report_grade_ready）
→ 05 主题深度研究（研究员长文交付）
```

04 观点变更是 05 返工的首要触发条件；素材不足应退回 03 补 `04_05_materials`，不得在 05 临时补搜证据。
