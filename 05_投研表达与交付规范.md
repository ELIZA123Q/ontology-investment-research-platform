# 05_投研表达与交付规范

## 1. 文档定位

05 是投研本体工作流的研报表达与交付层，负责把 01-04 已经形成、审计并允许表达的判断，转化为研究员可读、可改、可排版、可复盘的内部研究稿正文。

01-04 负责统一生产“可验证、可反证、可追溯的判断”：

```text
01：问什么，边界是什么，最后准备交付成哪类研究稿
02：怎么拆，主路径、反证路径和竞争解释是什么
03：准备证据资产、数据资产、来源注释和图表素材
04：裁决观点强度、叙事主线、章节骨架、改判闸门和 05 交接
05：在 04 批准的观点和 03 冻结的证据内完成研报化表达
```

05 负责根据 01 声明的交付原型，把 04 已批准表达的判断写成对应类型的内部研究稿。

05 的核心定位是：

> 04 决定“能不能这么判断”；05 负责“按正确研报类型把已批准判断写成研究员愿意读、能继续改、可排版成 PDF 的正文稿”。

05 不继续推理，不重新取数，不新增证据，不改变 04 的结论强度，不替代研究员人工复核、合规审查、排版制图和机构署名流程。

05 文件本身只包含研报正文，不包含系统留痕、后台元数据、判断追溯表、质量自检或生成结论。追溯、审计和质量判断由配对 04 审计 YAML、03 快照和 `validate_05_outputs.py` 承担。

本文件合并原“05 输出原型说明”和“05 投研表达与交付规范”。后续只维护这一份 05 总规范；输出原型作为本规范章节维护。

---

## 2. 核心设计原则：01-04 统一，05 分型

事件点评、行业动态、行业周期、公司业绩和主题深度，底层都需要 01-04 的判断底座，但最终正文结构、图表需求、测算方式和落点不同。

因此，05 不再使用一个万能模板，而采用：

```text
05 总规范（本文件，内含输出原型选择规则）
+ 05 模板库
+ 05 校验器
```

05 总规范统一以下事项：

- 不新增判断；
- 不新增证据；
- 不编造数据；
- 正文不出现本体和审计字段；
- 图表数据必须来自 03 或 04 允许使用的数据；
- 判断强度不得超过 04；
- 输出性质为内部研究稿，不等同于外发研报；
- 一个 05 文件只能有一个主原型，可嵌入局部模块。

产业链映射不再作为独立 05 主原型，而收敛为行业动态、行业周期或主题深度中的局部模块。政策影响点评不再作为独立 05 主原型，政策、制裁、关税、补贴、监管和出口管制等先归入事件点评；长期制度变化可归入主题深度。

---

## 3. 标准输出文件

05 正式文件命名为：

```text
05-<核心主题><输出原型名称>-<YYYYMMDD>-<当日序号>.md
```

示例：

```text
05-存储芯片周期行业周期判断-20260707-1.md
05-英伟达内存降配事件点评-20260707-1.md
05-全球半导体设备行业动态点评-20260707-1.md
05-某公司FY26Q2公司业绩点评-20260707-1.md
05-先进封装瓶颈主题深度研究-20260707-1.md
```

05 文件只包含研报正文，不包含：

```text
YAML front matter
后台元数据
系统留痕
判断追溯表
质量自检
生成结论
```

若确需导出追溯信息，应从 04 审计和 03 快照另存为独立审计附件，不进入 05 研报正文。

---

## 4. 输入要求

05 必须继承 01-04，不能脱离前序文件自由生成。

| 输入 | 来源 | 05 使用方式 |
|---|---|---|
| 投研需求说明 | 01 | 继承研究问题、对象、边界、判断时点、交付用途和输出原型 |
| 研究逻辑 | 02 | 继承分析框架、主路径、反证路径、竞争解释和对象分化结构 |
| 本体视图 | 02 | 只作为后台结构约束，不把本体字段写入正文 |
| 数据与证据准备 | 03 | 继承证据、反证、缺口、可展示数据和图表候选 |
| 数据与证据快照 | 03 | 优先读取 `display_data_candidates.csv`、图表包、表格素材包、来源注释包和 05 素材缺口 |
| 推理报告 | 04 | 提取核心结论、主导机制、演进路线、改判闸门和跟踪项 |
| 推理审计 | 04 | 提供 `handoff_to_05`，约束可表达判断、限制判断、章节计划、图表/表格意图和表达强度 |

05 的“市场常见理解/预期差”必须来自 02/04 已冻结的竞争解释、反证路径或 04 交接块。若上游没有提供，05 不能自行声称真实市场共识，只能写“常见简化理解”或删除预期差表述。

---

## 5. 01 中必须声明输出原型

01 应在 YAML 中声明 05 交付原型：

```yaml
delivery_archetype:
  primary: industry_cycle_report
  secondary:
    - internal_deep_research_note
  modules:
    - object_strength_ranking
    - event_context
intended_use:
  - internal_research
  - morning_meeting
not_allowed_use:
  - external_published_report
  - trading_recommendation
```

`delivery_archetype.primary` 决定 05 默认调用哪个模板。若 01 未指定，05 不得自行拍脑袋选择；应退回 01 补充，或由研究员确认后再生成。

---

## 6. 标准输出原型：五类

| 原型编号 | 输出原型 | 适用问题 | 核心回答 | 内容重心 |
|---|---|---|---|---|
| 05A | `event_commentary` 事件点评 | 某事件、新闻、政策、公司公告、产品变化、供应链扰动有什么影响 | 发生了什么、市场怎么看、我们怎么看、影响多大、什么会改判 | 事件事实、预期差、影响路径、对象分化、影响测算或边界 |
| 05B | `industry_dynamic_commentary` 行业动态点评 | 最近行业数据、业绩会或多家公司信息说明什么变化 | 最近变了什么、哪些环节受益或承压 | 关键观察、数据验证、产业链和公司类型映射 |
| 05C | `industry_cycle_report` 行业周期判断 | 当前处于什么周期，何时见顶、正常化或下行 | 当前阶段、主导机制、对象分化、改判信号 | 周期位置、证据推进、强弱排序、改判闸门 |
| 05D | `company_earnings_commentary` 公司业绩点评 | 公司财报、指引或订单变化怎么看 | 是否超预期、为什么、经营弹性如何变化、后续看什么 | 业绩与指引、分业务拆解、预期对比、预测调整或经营弹性 |
| 05E | `theme_deep_dive` 主题深度研究 | 某技术、赛道、产业趋势或长期主题怎么看 | 为什么重要、空间多大、驱动是什么、谁受益、如何演进 | 空间测算、行业结构、竞争格局、演进路径、关键对象 |

### 6.1 不再保留独立“产业链映射”原型

产业链映射不再作为独立 05 主原型。相关内容收敛到行业类和主题类稿件中：

| 原独立内容 | 新归属 |
|---|---|
| 产业链传导 | 事件点评中的“影响路径”，或行业动态/周期中的“行业传导” |
| 瓶颈位置 | 行业动态、行业周期或主题深度中的“对象分化 / 环节分化” |
| 价值分配 | 主题深度中的“行业结构与竞争格局”，或公司点评中的“业务弹性” |
| 公司类型映射 | 行业动态、周期判断或主题深度的局部表格 |

若研究问题本身是“哪些产业链环节受益”，05 应根据问题重心选择事件点评、行业动态点评、行业周期判断或主题深度研究，不单设主原型。

### 6.2 不再保留独立“政策影响点评”原型

政策影响点评不再作为独立 05 主原型。政策、制裁、关税、补贴、监管、出口管制等都先视为“事件”，归入 `event_commentary`。

政策类事件点评必须保留政策事实、影响变量、影响路径、受影响对象、缓释和反制、改判信号。如果政策问题不是事件冲击，而是长期制度变化，可嵌入 `theme_deep_dive`，但仍不单独设政策原型。

---

## 7. 输出原型选择规则

### 7.1 按研究问题选择主原型

| 01 问题特征 | 推荐主原型 |
|---|---|
| “某事件会有什么影响？” | 05A 事件点评 |
| “最近行业发生了什么变化？” | 05B 行业动态点评 |
| “目前处于什么周期，周期什么时候结束？” | 05C 行业周期判断 |
| “公司业绩怎么看，指引是否超预期？” | 05D 公司业绩点评 |
| “某主题或赛道机会怎么看？” | 05E 主题深度研究 |
| “产业链哪些环节受益？” | 根据问题重心归入 05A / 05B / 05C / 05E |
| “政策冲击如何影响行业或公司？” | 归入 05A 事件点评；长期制度变化可归入 05E |

### 7.2 允许主模板 + 局部模块

一个 05 文件只能有一个主原型，但可以嵌入其他原型的局部模块。

```yaml
delivery_archetype:
  primary: industry_cycle_report
  modules:
    - event_context
    - industry_segment_mapping
```

例如“存储芯片目前处于什么周期”使用行业周期判断模板；若报告中讨论“某厂商 HBM 认证事件”，可嵌入事件点评模块，但不应把整篇报告改成事件点评。

### 7.3 可嵌入局部模块

| 模块 | 适用位置 | 用途 |
|---|---|---|
| `event_context` | 行业动态、周期判断、主题深度 | 说明某个事件如何影响当前判断 |
| `object_strength_ranking` | 行业动态、周期判断、主题深度 | 排序产品、环节、公司类型或区域强弱 |
| `industry_segment_mapping` | 行业动态、周期判断、主题深度 | 呈现行业内部环节和传导，不作为独立报告 |
| `scenario_and_gate` | 全部原型 | 呈现情景推演和改判闸门 |
| `tracking_dashboard` | 全部原型 | 呈现后续观察指标和更新频率 |
| `quant_boundary` | 事件点评、公司点评、主题深度 | 呈现测算边界、弹性区间或关键变量 |

模块必须来自 03/04 的素材和观点交接。05 不得为了凑模块而新增判断或补造数据。

---

## 8. 正文通用硬规则

### 8.1 禁止后台字段

05 正文不得出现任何本体、审计、流程、质量门槛字段。包括但不限于：

```text
C-01 / JU-05 / EV-01 / RI-06 / DDC-01 / SRC-01
claim_id / judgment_unit_id / evidence_id / input_id / source_id / data_candidate_id
task_id / execution_id / delivery_id
handoff_to_05 / allowed_04_output / quality_gate / quality_status
normal_pass / restricted_pass / failed
full_reasoning_ready / directional_only / conditional_only
```

正文只写自然语言、表格、图表数据、来源名称和口径说明。

### 8.2 标题必须是判断句

标题应直接体现核心判断，而不是主题名。

不好的标题：

```text
存储芯片周期研究
先进封装行业分析
某政策事件分析
```

更好的标题：

```text
存储芯片不是同步强周期，而是 AI/HBM 驱动的结构性高位延续
先进封装瓶颈正在从 CoWoS 产能转向 HBM 认证和系统级交付
出口管制短期扰动供应链，中期关键看替代验证和客户迁移
```

### 8.3 开头先给结论

摘要和核心观点必须先回答：

```text
当前怎么看？
为什么？
最关键的证据是什么？
下一步看什么？
什么会改判？
```

不得先堆背景、流程、限制条件或数据罗列。

### 8.4 先主张，后边界

正文应先给基准判断和预期差，再集中处理限制条件。摘要和核心观点不得反复堆砌：

```text
不能确认 / 不能外推 / 只能作为 / 不足以 / 缺口限制 / 尚未
```

限制、缺口和不确定性主要进入：

```text
改判信号
风险提示
后续跟踪
```

### 8.5 不新增判断

05 可以改变表达方式，但不能改变判断强度。

| 04 判断强度 | 05 正文允许表达 | 05 正文禁止表达 |
|---|---|---|
| strong_directional | 明确判断、核心观点、阶段判断 | 写成确定性预测或唯一结论 |
| medium_directional | 当前更接近、倾向判断、基准情形 | 写成强确定结论 |
| observation | 观察项、边际变化、待验证信号 | 写成趋势确认或周期确认 |
| hypothesis | 假设路径、可能解释、情景条件 | 写成主导机制已经成立 |
| insufficient_evidence | 证据不足、暂不形成方向判断 | 写成方向性观点 |

### 8.6 不编造图表数据

05 可以把 03 中已有数据整理成图表数据包，但不得补造缺失数值。当数据不完整时，应标为：

```text
ready / partial / missing
```

`missing` 不得伪装成图表。`partial` 可以进入正文，但必须说明口径限制和不能外推的范围。

---

## 9. 研报级图表与数据规则

05 的图表不是装饰，而是观点证据的一部分。每张图表必须回答：

```text
这张图支持哪个判断？
读者看完应得到什么结论？
数据是否足以制图？
口径和限制是什么？
```

图表区只展示读者需要的字段：

| 字段 | 说明 |
|---|---|
| 图表标题 | 应是判断句或信息句 |
| 图表结论 | 1-3 句话说明图表含义 |
| 推荐图表类型 | line / bar / stacked_bar / scatter / waterfall / timeline / matrix / table |
| 数据状态 | ready / partial / missing |
| 对象 | 产品、公司、环节、区域或指标对象 |
| 指标 | 指标名称 |
| 时间/期间 | 时间点、季度、年份或事件窗口 |
| 数值 | 仅填写已有数值 |
| 单位 | %、亿美元、天、片、GW 等 |
| 来源 | 自然语言来源名称 |
| 口径限制 | 数据边界、预测属性、样本限制 |

高质量 05 至少应满足：

- 事件点评：至少 1 个影响测算图表或测算表；
- 行业动态点评：至少 3 个关键观察对应图表或表格；
- 行业周期判断：至少 2 个量化图表，或 1 个量化图表 + 1 个强弱排序表；
- 公司业绩点评：必须有业绩与指引表、分业务表、预测调整表或经营弹性表；
- 主题深度研究：必须有空间测算表、行业结构图、竞争格局表或演进路径图。

产业链相关图表作为行业动态、行业周期或主题深度中的“行业结构 / 环节分化 / 传导路径”图表。政策相关图表作为事件点评中的“政策事实 / 影响路径 / 受影响对象”图表。

---

## 10. 各原型最低输入门槛

| 输出原型 | 最低输入门槛 |
|---|---|
| 事件点评 | 事件事实、影响对象、市场担忧或预期差、至少一个影响测算或测算框架 |
| 行业动态点评 | 最近变化、关键指标、至少 3 个观察点、行业/对象分化 |
| 行业周期判断 | 价格、库存、订单/需求、供给、对象分化、改判闸门 |
| 公司业绩点评 | 收入、利润、指引、分业务表现、预期对比、预测调整或经营弹性解释 |
| 主题深度研究 | 空间测算、产业链拆解、核心驱动、竞争格局、关键公司/环节 |

若最低输入门槛不满足，05 应降级输出或退回 03/04；不得在 05 中补造材料。

---

## 11. 质量门槛

05 高质量输出必须做到：

| 检查项 | 合格标准 |
|---|---|
| 原型匹配 | 报告结构与 01 的研究问题和 `delivery_archetype.primary` 一致 |
| 前台纯净 | 05 文件只包含研报正文，没有系统留痕、后台字段和追溯表 |
| 观点锋利 | 标题、摘要、核心观点先给主张，不被限制条件淹没 |
| 预期差明确 | 明确市场常见理解或误读，以及本文不同判断；若不适用，应说明不使用 |
| 证据推进 | 按数据如何压实观点来写，不按来源机械罗列 |
| 图表有效 | 图表服务观点，不堆砌、不编造 |
| 对象落点 | 明确产品、环节、公司类型或区域的强弱差异 |
| 判断强度一致 | 未把观察项、假设或证据不足写成确定结论 |
| 改判可执行 | 改判信号有可观察指标、触发条件和影响方向 |
| 内部稿边界 | 不输出未经 01-04 允许的评级、目标价、收益率预测、仓位建议或外发合规结论 |

返工路由：

| 问题 | 处理 |
|---|---|
| 05 正文出现后台字段或追溯表 | 重写 05 |
| 预期差无上游来源 | 退回 04 补交接，或删除市场共识表述 |
| 图表数据不足 | 退回 03 补可展示数据，或降级 |
| 对象强弱排序不足 | 退回 04 补对象排序，必要时退回 03 补数据 |
| 出现新判断 | 删除或退回 04 审计 |
| 原型选错 | 退回 01 确认或重新选择 05 模板 |

---

## 12. 最小可用版本

当前阶段 05 的最小可用版本是：

```text
一份没有系统留痕、但由 04/03 外部可审计的内部研究稿正文
```

05 不承担排版、美化、自动制图、自动 PPT、外发审批等工作。但 05 应为后续 PDF 渲染准备好足够清晰的正文、表格、图表数据和口径说明。
