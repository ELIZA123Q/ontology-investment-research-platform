# 嵌套目录（兼容指针）

**权威入口在上一层：** [`../README.md`](../README.md)。

本嵌套目录存在仅为历史/兼容布局，**不要在这里双写**新的治理模型或概述。改元治理请优先改 `05_control_evaluation/05_元治理本体/` 根下的 `meta_schema.yaml`、`model_registry.yaml`、`models/` 等。

## 给谁看

维护者（误入嵌套路径时）。

## 材料从哪来

同上层权威目录（`knowledge_changes` 亦为指向上层的符号链接）。

## 怎么用 / 怎么维护

打开上层 README，按其流程维护；本文件不增加第二权威。
