# 03_数据与证据准备规范

## 1. 文档定位

本文档规范 03 阶段如何把 02 已经形成的研究逻辑和任务本体视图，转化为一次可执行、可复核、可追溯、可审计的数据与证据准备过程。

03 的核心目标不是泛泛收集资料，也不是用覆盖率制造“证据充分”的错觉，而是判断：

> 关键判断单元是否取得了匹配口径、足够质量、经过反证检查的证据；哪些观点可以交给 04 推理；哪些只能受限使用；哪些必须暂不可判断或返工；以及本次材料是否足以支撑目标 05 交付形态。

03 必须先设定证据要求，再执行取证。不得先搜索资料，再倒推“证据充分”。

03 应回答以下问题：

1. 本次判断需要哪些现实实例、数据、证据和反证；
2. 每个核心判断单元需要达到什么证据等级；
3. 哪些证据必须是一手硬证据，哪些可以使用代理指标，哪些只能作为背景材料；
4. 每项材料应该从哪些来源取得，采用什么口径和获取方式；
5. 实际取得的材料是否直接、独立、可复核、足够新、权限合规；
6. 材料如何清洗、归一，并映射到 02 的任务本体视图；
7. 每个关键判断单元是否满足最低证据门槛；
8. 本次证据包允许 04 判断什么，不允许判断什么；
9. 本次材料是否足以支撑目标 05；
10. 若不足以支撑高质量 05，缺少哪类成稿资产，应如何补齐。

本阶段形成三项正式产物：

```text
03-<核心主题>数据与证据准备-<YYYYMMDD>-<当日序号>.md
03-<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
```

准备 Markdown 说明“本次需要什么证据、采用什么取证策略、去哪取、怎么取、怎么处理、质量是否合格、是否够用、允许 04 怎么用、能否支撑 05”。

快照目录保存本次实际取得的数据、证据、证据要求预设、SourceProfile 运行视图、推荐来源使用情况、获取记录、质量状态、缺口、反证、冲突、准入结果和 05 成稿素材。

两项产物必须使用同一组任务标识、运行标识、冻结范围、数据截止时点、来源版本和证据要求预设，不得拆开流转。

`normal_pass`、`restricted_pass`、`incomplete_pass`、`failed` 是证据准入状态，不等同于研究结论强度。正式交付时，还必须满足 `00A_高质量产出判别标准.md` 中对 03 的质量要求。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

03 负责把 02 的判断结构变成可执行的数据与证据准备方案，主要包括：

1. 读取并校验 02 研究逻辑和任务本体视图；
2. 冻结本次运行的时间范围、对象范围、来源范围、口径范围和数据截止时点；
3. 为每个核心判断单元预设证据要求、证据等级、必需证据篮子和降级规则；
4. 调用 03 取证策略库，将判断类型映射为取证规则、推荐取证来源和获取方式；
5. 将 02 的判断单元、路径节点、实例需求、观测要求、证据要求和反证要求拆解为具体数据需求；
6. 为每项需求明确首选来源、交叉验证来源、替代来源、代理指标边界和取数口径；
7. 区分来源生产者、内容领域、获取通道、权限边界和材料形态；
8. 记录实际获取过程，包括来源、发布时间、业务时间、查询条件、获取方式、失败原因和停止原因；
9. 对取得材料进行清洗、去重、身份归一、口径转换、时间对齐和质量评价；
10. 将材料映射为对象实例、关系实例、事件、状态观测、信号、市场预期、证据记录和反证记录；
11. 判断每项需求、每个状态变量锚点、每条路径和每个核心判断单元是否具备后续推理所需的最低输入；
12. 主动检查反证、冲突证据和竞争解释；
13. 输出证据包可信程度、准入状态、可使用范围、置信度上限和 04 最多能形成的结论；
14. 判断目标 05 是否具备成稿素材，包括图表数据、强弱排序素材、来源注释、证据边界和补齐计划；
15. 判断本阶段产物是草稿、最低通过，还是达到高质量交付标准。

### 2.2 本阶段不负责

03 不做以下事情：

1. 不修改 01 已确认的研究问题、对象范围和时间边界；
2. 不修改 02 已确定的核心判断、问题树、路径节点、本体结构和最低验证条件；
3. 不为了“完整”而抓取与当前判断无关的全部新闻、全部公司或全部历史数据；
4. 不把搜索结果、网页摘要、模型记忆或二手转述直接当作证据；
5. 不把 MCP、API、搜索引擎、数据库终端或 AI 工具本身当作证据来源；
6. 不把预测、观点、假设、指引和已发生事实混为一类；
7. 不执行 02 的推理规则，也不形成方向性投研结论；
8. 不因主源不可用而静默更换数据口径；
9. 不用来源数量、综合分数、材料篇数或覆盖率替代关键质量门槛；
10. 不在运行产物中保存账号、密钥、Cookie 等敏感凭据。

如果 02 的核心问题、最低验证条件或任务本体视图不清，03 应退回 02，不得通过临时搜索资料自行补造判断结构。

03 发现可复用的新证据结构或本体能力时，只提交本体候选与缺口文件。新事实不得直接写入 `evidence.yaml`。

---

## 3. 入口检查、固定本次研究口径与证据要求

### 3.1 入口校验

03 开始前，应先确认 02 产物是否足以进入数据与证据准备。

至少确认以下事项：

1. 已收到配对的研究逻辑 Markdown 和任务本体视图 YAML；
2. 两份文件对应同一研究问题、同一对象范围、同一最终要回答的问题和同一任务 ID；
3. 02 已形成核心判断和最小问题树；
4. 02 已列出关键路径节点及节点关系；
5. 每个关键路径节点已经说明所需实例、观测、证据、反证和最低验证条件；
6. 02 已说明反证、竞争解释、停止条件和降级规则；
7. 如使用候选结构或存在本体缺口，相关限制已经传递到 03；
8. 02 阶段状态和质量状态可解析。

入口处理规则如下：

| 02 状态 | 03 处理 |
|---|---|
| `high_quality_pass` | 可进入正式 03 |
| `minimum_pass` | 可进入 03，但 03 应标记上游质量限制，并不得自动形成高质量准入 |
| `draft` | 不进入正式 03 |
| `return_required` | 退回 02 |

如果缺少的是现实公司、产品、关系、事件、数值、来源或材料，由 03 继续准备。
如果缺少的是核心判断、路径结构、本体承接、对象拆分或最低验证条件，应退回 02。
如果研究问题和对象边界仍有关键歧义，应退回 01。

### 3.2 固定本次研究口径

入口检查通过后，03 必须固定本次研究的范围和截止时点。

每次研究至少明确并固定以下内容：

| 固定项 | 说明 |
|---|---|
| 任务标识 | 对应本次研究问题或判断任务 |
| 运行标识 | 对应本次数据与证据准备过程 |
| 数据截止时点 | 明确本次材料统计到什么时候 |
| 时间范围 | 将“当前”“近半年”“事件后”等表达转为具体时间规则 |
| 对象范围 | 明确纳入和排除的公司、产品、材料、资产、地区或时间段 |
| 来源范围 | 明确本次允许使用的数据源、公开材料、内部材料或授权材料 |
| 口径范围 | 明确产品规格、币种、单位、地域、价格类型、产能类型等关键口径 |
| 取数方式范围 | 明确可使用网页、数据库、API、MCP、人工上传、内部系统或调研材料 |
| 02 文件指纹 | 防止证据准备期间上游结构漂移 |

同一次研究中，准备 Markdown、快照目录和后续 04 判断必须使用同一套范围和截止时点。

如后续需要扩大对象、改变时间范围、替换关键口径、修改来源范围或新增取数方式，应生成新的运行版本，不得覆盖旧快照。

### 3.3 证据要求预设

03 不得在未预设证据要求的情况下开始正式取数。

证据要求预设的目的，是在取数前明确：

```text
为了让 04 输出某种强度的判断，03 至少需要取得什么证据；
哪些证据是必需项；
哪些可以替代；
哪些只能做代理；
哪些缺失会导致降级或停止。
```

每个核心判断单元必须先形成 `evidence_requirements.csv`。03 不得把所有字段堆进 `judgment_units` 或 `evidence_requirements`，而应拆成以下链路：

```text
JudgmentUnit
→ EvidenceRequirement
→ EvidenceRecipe
→ EvidenceBasket
→ SourceProfile / AcquisitionChannel
→ SourceDocument
→ EvidenceClaim
→ EvidenceFact
→ EvidenceAssessment（单项证据可用性）
→ EvidenceReadinessAssessment
→ 04 最多能形成的结论
```

`judgment_units` 只说明“判断类型和需要什么证据角色”；`evidence_requirements` 只说明“本次必须取得什么证据”。通用取证方法和推荐来源由 `03取证策略库` 提供；SourceProfile、SourceDocument、AcquisitionChannel、AccessScope、ArtifactType 和 ContentDomain 由通用证据域定义。单项材料是否可用于本次判断由 EvidenceAssessment 记录，整个判断单元能否进入 04 由 EvidenceReadinessAssessment 记录；单次运行的配方匹配、篮子填充和准入结果落入 03 快照。

`evidence_requirements.csv` 建议字段如下：

| 字段 | 说明 |
|---|---|
| `evidence_requirement_id` | 单项证据需求 ID |
| `target_judgment_unit_id` | 对应 02 的核心判断单元 |
| `target_path_node_id` | 对应路径或节点 |
| `target_state_variable_id` | 对应状态变量 |
| `requirement_purpose` | 支持、削弱、阻断、验证、交叉验证或背景 |
| `evidence_role` | 主证据、交叉验证、反证、阻断、代理或背景 |
| `required_evidence_category` | 价格、需求、供给、库存、订单、政策、估值等 |
| `required_content_domains` | 财务、经营、产业、政策、市场价格等内容域 |
| `required_object_scope` | 公司、产品、地区、产业链环节等对象范围 |
| `required_time_scope` | 时间窗口 |
| `required_grain` | 日度、月度、季度、事件点、文本主张等粒度 |
| `minimum_quality_level` | 本判断最低需要的证据等级 |
| `minimum_source_tier` | 最低来源层级 |
| `minimum_independent_source_count` | 最少独立来源数 |
| `mandatory_baskets` | 必须满足的证据篮子 ID |
| `counter_baskets` | 必须检查的反证篮子 ID |
| `allowed_proxy` | 是否允许代理指标 |
| `preferred_source_profiles` | 首选 SourceProfile ID；来源清单仅提供推荐优先来源 |
| `allowed_acquisition_channels` | 允许的取数方式 |
| `forbidden_sources` | 禁止来源 |
| `required_freshness` | 时效要求 |
| `required_traceability` | 可追溯要求 |
| `required_comparability` | 可比口径要求 |
| `stop_condition` | 什么情况下不得进入方向性推理 |
| `missing_policy` | 缺失时如何处理 |
| `allowed_04_output_if_met` | 满足后 04 最高使用方式 |
| `allowed_04_output_if_missing` | 不满足时 04 降级方式 |

证据要求预设不是结论，不得写成“当前已经证明”。它只规定本次 03 的取证标准和准入边界。

### 3.4 候选结构的处理

如果 02 使用了任务候选结构，03 可以继续准备数据、实例和证据，但必须说明：

1. 哪些需求依赖候选结构；
2. 候选结构的使用边界；
3. 可能影响哪些传导环节；
4. 是否限制 04 的观点强度；
5. 后续是否需要进入本体优化。

只要关键路径依赖候选结构，即使数据和证据充分，本次整体证据评估原则上也不应高于 `restricted_pass`，除非 02 已说明候选结构不影响本次判断强度并通过独立审阅。

---

## 4. 取证策略调用与数据需求拆解

### 4.1 按核心问题拆解，不按主题泛搜

03 不应围绕宽泛主题搜索资料，而应从 02 的核心问题和传导环节出发，把每个关键节点拆成可执行的数据需求。

每项数据需求至少说明：

| 维度 | 说明 |
|---|---|
| 需求目的 | 用来支持、削弱、阻断或验证哪个判断节点 |
| 证据角色 | 事实确认、状态测量、趋势判断、机制验证、对象比较、财务测算、估值预期或反证 |
| 本体目标 | 对应对象、关系、事件、状态变量、观测、信号或证据 |
| 最低字段 | 身份、数值、单位、时间、范围、来源等不可缺字段 |
| 时间要求 | 业务时间、发布时间、数据频率、截面或时间序列要求 |
| 口径要求 | 产品规格、地域、币种、价格类型、产能类型等 |
| 覆盖要求 | 需要覆盖哪些对象、时间点、来源或反面证据条件 |
| 质量门槛 | 可用于后续推理所需满足的最低条件 |
| 来源结构 | 对应 SourceProfile、ContentDomain、AccessScope 和 ArtifactType |
| 获取方式 | 对应 AcquisitionChannel，如网页、数据库、API、MCP、文件、人工、内部系统 |
| 路径绑定 | 对应路径、节点或判断问题 |
| 反证绑定 | 对应削弱、阻断或竞争解释 |
| 05 成稿用途 | 是否可能支撑图表、表格、来源注释、强弱排序或改变判断的信号 |
| 优先级 | 关键、必要或补充 |

### 4.2 判断类型与取证规则

03 应根据 02 已明确的判断类型选择取证方法，不得根据手头已有材料反推研究问题。判断类型与 A01—A09 取证规则、默认最低证据组合的权威映射，只在 [`03取证策略库/00_取证策略库总览与调用规则.md`](03取证策略库/00_取证策略库总览与调用规则.md) 和 `00_strategy_registry.yaml` 维护；本规范不再复制映射表。

单次任务只需记录：本次关键判断采用什么主要取证方法、需要哪些辅助方法、为什么适用，以及证据缺失时怎样降低结论强度。不得为了显得完整而堆叠无关规则；如果一个关键判断需要过多互不相同的取证方法，通常说明问题过大，应退回 02 拆分。

所有准备形成方向判断的关键判断都必须主动检查相反证据和其他可能解释。只有直接指标不可得、明显滞后或权限受限时，才使用代理指标，并说明代理关系、适用范围、失效条件，以及这组证据最多能支持什么结论。

### 4.3 证据篮子

03 应把单条证据和必要证据组合分开。系统字段仍使用 `EvidenceBasket`，研究员正文统一写“必要证据组合”。

单条证据回答：

```text
某个来源具体说了什么，或某个数据字段具体是多少。
```

必要证据组合回答：

```text
某个方向性判断需要哪些类型的证据组合，目前是否满足。
```

不同判断需要哪些必要证据组合，以 [`03取证策略库/C_Evidence_Recipe/01_核心判断证据配方.md`](03取证策略库/C_Evidence_Recipe/01_核心判断证据配方.md) 为准，本规范不再维护平行清单。单次任务必须展开并检查实际取得的证据组合，不能只记录一个配方编号，也不能用材料数量代替关键证据是否齐备。

### 4.4 最小充分原则

03 只准备回答当前问题和目标 05 原型所需的最小充分集合。

准备顺序如下：

1. 先满足关键节点的身份、关系和硬性证据要求；
2. 再满足支持证据、反面证据和其他可能解释的最低交叉验证要求；
3. 再补充能够显著降低冲突或范围不确定性的材料；
4. 再补充目标 05 必需的展示型数据、来源注释和对比基准；
5. 达到最低门槛，且新增信息不会改变节点状态或 05 素材状态时，应停止补充；
6. 补充性材料不得阻塞整体准入，但应影响 05 成稿成熟度判断。

“没有找到”只表示在当前权限、来源和检索范围内缺失，不表示事实本身不存在。

### 4.5 核心结论证据门槛

03 必须把“证据覆盖率”和“核心结论证据门槛”分开。

覆盖率回答：

```text
02 要求的变量—锚点、实例和证据记录完成了多少。
```

核心结论证据门槛回答：

```text
某个 04 可能输出的方向性观点，最低需要哪些证据，目前是否满足。
```

因此，03 必须形成“核心问题证据门槛表”。

| 字段 | 说明 |
|---|---|
| `judgment_unit_id` | 核心判断单元 |
| `possible_view` | 可能支持的 04 观点 |
| `required_evidence_baskets` | 必要证据组合 |
| `actual_evidence_baskets` | 实际取得证据组合 |
| `quality_level` | 实际达到的最高证据等级 |
| `support_status` | 支持证据状态 |
| `counter_status` | 反证检查状态 |
| `conflict_status` | 冲突证据状态 |
| `proxy_dependency` | 是否依赖代理指标 |
| `scope_limit` | 使用范围限制 |
| `allowed_04_output` | 允许 04 输出的最高观点等级 |
| `gap_action` | 缺口处理或返工要求 |

某个变量覆盖率高，不代表对应核心观点可以方向性输出。只有核心问题证据门槛满足，04 才能使用该观点。

---

## 5. 数据质量控制

### 5.1 三层质量控制

03 的质量要求采用三层叠加：

```text
基础质量底线：所有材料都必须满足；
判断类型加严：不同证据角色有不同专项要求；
取数方式修正：不同获取方式有不同留痕和审计要求。
```

三层分别回答：

| 层级 | 回答的问题 | 是否统一 |
|---|---|---|
| 基础质量底线 | 这条材料能否进入证据包 | 统一 |
| 判断类型加严 | 这类判断需要哪些额外证据门槛 | 按问题不同 |
| 取数方式修正 | 这种获取方式需要保留什么审计链 | 按取数方式不同 |

数据质量不是单独打分项，而是直接决定 04 最多能形成的结论。

### 5.2 证据质量等级

03 建议使用以下证据质量等级：

| 等级 | 含义 | 04 最多能形成的结论 |
|---|---|---|
| `Q0_unusable` | 不可追溯、权限不清、口径不明、来源不合格或无法定位 | 不得使用 |
| `Q1_background` | 可作为背景、线索、市场叙事或检索入口 | 不得支撑方向性判断 |
| `Q2_reasoning_usable` | 可作为受限推理输入，但存在口径、时间、来源或代理限制 | 条件判断或弱倾向判断 |
| `Q3_directional_ready` | 可支撑方向性判断，来源、口径、时间和反证检查基本满足 | 可形成方向性观点 |
| `Q4_report_grade` | 可支撑高质量 05 图表、排序表、核心论证或来源注释 | 可进入研报级表达 |

质量等级不得高于其最薄弱关键环节。若核心证据完全依赖代理指标，原则上不得高于 `Q2_reasoning_usable`，除非代理逻辑、交叉验证和适用边界充分说明。

### 证据强度决定可以形成多强的结论（系统名 Evidence Permission Matrix）

Q0—Q4 不只是质量标签，还必须转换为 04 的硬使用权限。每个 JudgmentUnit 的最终证据等级与权限都写入 03 gate；04 只能继承或主动降级，禁止上调。

| 最终证据等级 | `evidence_permission` | 允许 04 输出 | 04 边界 |
|---|---|---|---|
| `Q4_report_grade` | `core_judgment` | 核心判断 | 仍受目标 Claim 类型、J0—J4 上限、范围与反证状态约束 |
| `Q3_directional_ready` | `directional_judgment` | 方向判断 | 不得表达为高概率确认或无条件核心结论 |
| `Q2_reasoning_usable` | `conditional_judgment` | 条件判断 | 必须显式保留条件、缺口和降级原因 |
| `Q1_background` | `background_only` | 背景 | 不得进入 Claim register 充当判断依据 |
| `Q0_unusable` | `prohibited` | 禁止 | 不得进入 04 推理或正文 |

最终等级不是“最好一条证据”的等级，而是综合强制篮子、反证、冲突、代理依赖、范围和时效后的 JudgmentUnit 级最弱约束。`source_quality_status` 只描述来源质量，不能替代最终等级。

### 5.2A 单项证据可靠度评价（系统名 Evidence Confidence Model）

EvidenceAssessment 必须解释一项证据为什么能用于当前判断，而不能只继承来源层级或写一个 high / medium / low。评估采用五个可解释维度，每项 1—5 分：

| 维度 | 1 分 | 5 分 |
|---|---|---|
| 来源可靠性 | 来源身份、方法或责任边界不清 | 原始责任主体或权威数据生产者，方法与权限清楚 |
| 数据直接性 | 仅为主题相关背景或多手转述 | 直接测量、原文披露或直接对应判断对象 |
| 时间新鲜度 | 超出本次有效窗口 | 覆盖本次判断时点和业务窗口 |
| 口径匹配度 | 对象、单位、范围或定义不可比 | 与判断单元的对象、时间、地域和统计口径一致 |
| 独立验证程度 | 无独立来源，或所谓验证来自同源转述 | 至少一组真正独立且口径可比的验证 |

诊断总分为五项之和，范围 5—25：

| 总分 | 建议可靠度 | 使用含义 |
|---|---|---|
| 22—25 | high | 仍需服从证据角色、反证与判断类型门槛 |
| 17—21 | medium | 可使用，但必须披露最弱维度与范围限制 |
| 5—16 | low | 原则上只作背景、线索或受限输入 |

分数用于解释和排序，不是事实为真的概率，也不得与判断置信度相乘。以下硬限制优先于总分：

1. 无法追溯原始来源、权限不清或来源身份不明，直接 blocked；
2. 动态变量缺业务时间，时间维度最高 2 分，且不得支撑趋势判断；
3. 同源转述不得提高独立验证分；
4. 对象或口径错配时，即使来源权威，口径匹配度也不得高于 2 分；
5. 代理指标必须记录置信折扣，不能因其他维度较高而替代必需直接证据；
6. 单项 EvidenceAssessment 的 reliability_band 不得直接复制为 EvidenceReadinessAssessment 或 Judgment 的 confidence。

每项评估必须保存五维分数、总分、可靠度、最弱维度、评分理由和硬限制。EvidenceReadinessAssessment 再基于多项证据、证据篮子、反证、冲突和代理依赖评价整个判断单元。

### 5.3 基础质量底线

所有进入有效证据计数的材料，至少检查以下维度：

| 维度 | 要求 |
|---|---|
| 可追溯性 | 能回到原始来源、字段、页码、段落、查询参数、文件版本或内容指纹 |
| 来源清晰 | 来源生产者、发布者、获取位置和访问权限明确 |
| 主张类型清晰 | 区分事实、数据、估算、预测、指引、观点、假设 |
| 业务时间明确 | 动态内容必须说明业务发生时间、统计期或观测窗口 |
| 口径清晰 | 指标定义、单位、统计范围、产品规格、地域和币种明确 |
| 权限合规 | 获取和使用符合公开、授权、内部或保密边界 |
| 独立性 | 交叉验证来源不能来自同一原始来源或转述链 |
| 一致性 | 与本次冻结口径、历史口径和其他来源不发生未解释冲突 |
| 完整性 | 核心字段不缺失，缺失字段已说明影响 |
| 适用性 | 与当前判断单元直接相关，不只是主题相关 |

以下材料不得进入有效证据计数：

1. 无法定位原始来源的网页摘要；
2. 搜索结果页或 AI 回答；
3. 来源权限不清的内部材料；
4. 无业务时间的动态变量材料；
5. 未区分事实和观点的二手转述；
6. 与当前判断单元无直接绑定的宽泛背景材料。

### 5.4 判断类型加严

不同证据角色有不同专项质量门槛：

| 证据角色 | 专项质量要求 |
|---|---|
| 事实确认 | 必须有原文、主体、时间、适用范围；新闻转述不能替代官方或公司原文 |
| 状态测量 | 必须有业务时间、口径、单位、统计周期；动态变量不得只用发布时间替代业务时间 |
| 趋势与阶段 | 不得用单点数据判断趋势；至少说明方向、持续性、时间窗口和反向信号 |
| 机制验证 | 不得跳过中间节点；每个关键传导节点必须有证据或明确降级 |
| 对象比较 | 强弱排序必须绑定暴露度、弹性、约束或财务承接变量 |
| 财务影响 | 缺收入拆分、量价拆分、成本或毛利率口径时，不得做精细利润测算 |
| 市场预期 | 基本面改善不等于预期差；必须同时看盈利预期、价格反应或市场叙事 |
| 反证解释 | 无反证检查不得高质量通过；竞争解释未排除时只能条件判断 |
| 代理指标 | 必须说明代理对象、代理逻辑、适用条件、失效条件和置信度折扣 |

### 5.5 取数方式修正

不同获取方式需要不同留痕要求：

| 获取方式 | 质量重点 |
|---|---|
| 网页 / 搜索 | 必须回到原始来源，记录 URL、发布时间、获取时间、段落或页码，搜索结果不得作为证据 |
| 数据库终端 | 必须记录库名、字段、查询条件、口径、导出时间和权限范围 |
| API | 必须记录接口版本、请求参数、响应时间、原始响应、字段血缘和错误状态 |
| MCP | 必须记录 Connector 名称、上游来源、查询参数、原始响应、字段血缘、权限范围和回放能力 |
| 文件上传 | 必须记录文件名、来源、版本、页码、表格、内容指纹和上传时间 |
| 人工调研 | 必须记录样本来源、访谈时间、样本偏差、是否一手、是否可交叉验证 |
| 内部系统 | 必须记录系统名称、字段定义、权限、脱敏状态、更新时间和可表达边界 |
| AI 抽取 | 必须回溯原文，标注模型抽取过程，关键字段需人工或规则校验，不得把 AI 生成内容作为来源 |

MCP、API、数据库终端和 AI 工具是获取通道，不是来源生产者。它们影响可审计性，不自动提高来源权威性。

### 5.6 质量问题与 04 最多能形成的结论

质量问题必须映射到 04 最多能形成的结论：

| 质量问题 | 04 最多能形成的结论 |
|---|---|
| 无法追溯原始来源 | `blocked`，不得进入方向性推理 |
| 权限或合规边界不清 | `blocked`，不得进入正式证据包 |
| 动态变量缺业务时间 | `conditional_only` 或以下 |
| 来源不独立却被当作交叉验证 | 不得作为交叉验证计入 |
| 核心证据完全依赖代理指标 | 原则上不高于 `conditional_only` |
| 单点数据用于趋势判断 | 不得形成趋势或阶段判断 |
| 财务测算缺关键拆分 | 只能方向性说明，不得精细测算 |
| 反证检查缺失 | 不得 `high_quality_pass`，不得强方向性输出 |
| 证据冲突未处理 | `contested`，必须表达争议或待验证 |
| 图表数据口径不稳定 | 不得标记为 `report_grade_ready` |

---

## 6. 来源结构、推荐取证来源与取数方式

### 6.1 五维拆分

03 必须区分五个概念，不得混用。五个概念的对象定义和硬规则由通用证据域承接；03 取证策略库的 `B_取证来源` 只负责推荐优先来源、来源分层和降级边界。

| 概念 | 回答的问题 | 示例 |
|---|---|---|
| `SourceProfile` | 谁生产或发布材料，权威性如何 | 交易所、上市公司、Wind、Bloomberg、TrendForce、SEMI、内部数仓 |
| `ContentDomain` | 材料内容属于什么领域 | 金融市场、公司财务、行业供需、产业链经营、政策监管、宏观经济 |
| `AcquisitionChannel` | 通过什么方式获取 | 官网、数据库终端、API、MCP、文件上传、文件检索、内部系统、人工调研 |
| `AccessScope` | 能否使用、如何使用 | public、licensed、internal、confidential、restricted |
| `ArtifactType` | 实际取得的材料形态 | PDF、HTML、CSV、API response、数据库字段、访谈纪要、截图 |

正确示例：

```text
SourceProfile：Wind
ContentDomain：金融市场 / 一致预期
AcquisitionChannel：MCP Connector
AccessScope：licensed
ArtifactType：API response / CSV export
```

错误示例：

```text
来源：MCP
来源：搜索工具
来源：AI 总结
```

MCP、API、网页、人工上传、文件检索、数据库终端和内部系统都是获取通道，不是来源。

### 6.2 来源使用原则

03 应优先使用已知、稳定、可复核的来源，而不是每次重新设计来源体系。

推荐来源的调用顺序：

```text
A 类取证规则确定证据篮子
→ B02 通用来源清单匹配跨行业来源
→ 行业专业变量进入 B03_行业专用来源
→ 跨行业专题冲击进入 B04_专题来源
→ B01 来源分层校验使用上限
→ 实际取得材料登记为 SourceDocument
```

`B_取证来源` 是推荐优先来源清单，不是封闭白名单。清单外来源可以使用，但进入关键判断前必须补充来源生产者、内容领域、发布时间、业务时间、口径、权限、可复核路径和使用限制。

来源选择原则如下：

1. 政策、监管、财务和已披露经营事实，优先官方原文、交易所文件和公司正式披露；
2. 订单、交付、良率、产能和供应关系，优先一手经营记录；无权限时，用公司披露和独立产业来源交叉验证；
3. 行业规模、设备、材料、产能和价格统计，优先方法论明确、口径稳定的行业数据源；
4. 市场价格、估值和一致预期，优先合法授权且口径可复现的市场数据源；
5. 预测、渠道调研和产业判断，应保留其预测或观点性质，不得替代已发生事实；
6. 搜索引擎只用于发现来源，不应直接作为证据来源；
7. 卖方研报和专家观点可以用于市场预期、逻辑线索和竞争解释，但不得作为硬事实唯一证据；
8. 内部数据必须说明权限边界、脱敏状态和可表达范围。

### 6.3 来源质量判断

每条进入证据包的来源至少判断：

| 维度 | 要求 |
|---|---|
| 直接性 | 来源是否接近原始事实或一手披露 |
| 独立性 | 是否与其他来源属于同一来源组或转述链 |
| 可复核性 | 后续是否能重新找到、重新读取或复算 |
| 时效性 | 发布时间和业务时间是否满足判断窗口 |
| 口径清晰度 | 指标定义、范围、单位和统计方法是否清楚 |
| 权限合规 | 来源获取和使用是否符合权限要求 |
| 方法透明度 | 数据生产方法、样本、统计规则是否可理解 |
| 稳定性 | 来源是否长期稳定、是否易失效或频繁修订 |
| 使用边界 | 可支持事实、观点、预测、预期还是背景 |

两条新闻转述同一份报告，不构成交叉验证。供应链传闻、渠道观点和模型摘要不得与一手事实等同处理。

### 6.4 取数方式

对于重复出现的数据需求，可以沉淀为取数方式或取数配方。当前阶段不要求工程化建设完整配方库，但每次运行应说清楚：

1. 首选来源是什么；
2. 交叉验证来源是什么；
3. 替代来源是什么；
4. 来源属于什么 SourceProfile 和 ContentDomain；
5. 采用什么 AcquisitionChannel；
6. 查询条件、时间范围和对象范围是什么；
7. 关键字段如何解释；
8. 口径如何统一；
9. 原始输出如何保存；
10. 获取失败时如何处理；
11. 何时停止继续补充。

主源不可用时，可以使用替代来源，但必须说明替代来源与主源之间的口径差异，以及该差异对 04 的使用限制。

### 6.5 MCP 与 AI 工具化取数

MCP、API 和 AI 工具化取数应提高取数过程的可重复性和可审计性，但不替代来源质量判断。

MCP 取数至少保留：

1. MCP Connector 名称；
2. 上游来源或数据库；
3. 授权范围；
4. 查询参数；
5. 查询时间；
6. 原始响应或快照；
7. 字段血缘；
8. 版本或接口信息；
9. 异常、缺失和失败记录；
10. 回放或复算可能性。

若 MCP 只返回摘要而不能回到上游原始材料，该结果不得高于 `Q1_background`。

AI 抽取可以提高效率，但关键字段、数值、时间、单位和原文定位必须回源校验。AI 生成内容不能作为正式证据来源。

### 6.6 数据元数据与质量底线

当前不单独新增 `C_数据元数据及质量要求` 目录。元数据要求由通用证据域对象、`00_quality_gates.yaml` 和本节共同承接；只有当固定接口、内部数仓和自动化取数规模化后，再升级为独立数据治理目录。

每条进入正式证据包的 SourceDocument 至少需要记录：

| 字段组 | 最低要求 |
|---|---|
| 来源身份 | `source_document_id`、`title`、`source_profile_id`、`source_name`、`source_tier` |
| 来源适配 | `content_domain`、适用证据篮子、禁止用途 |
| 获取方式 | `acquisition_channel`、查询参数或获取路径、获取时间 |
| 权限边界 | `access_scope`、授权或引用限制；不清时不得进入正式证据包 |
| 材料形态 | `artifact_type`、原文定位、字段血缘、快照或内容指纹 |
| 时间信息 | `published_at`、`captured_at`、`business_time` 或统计期 |
| 复核定位 | `location`、`locator`、`raw_artifact_ref`、`content_hash` |
| 口径说明 | `methodology_note`、单位、样本、统计范围和版本 |
| 使用边界 | `usage_limit`、`source_reliability`、质量状态和 04 最多能形成的结论 |

结构化数据额外需要记录字段名、表名或接口端点、查询条件、字段口径、单位、频率、币种、复权/季调/累计口径、数据截止点、版本或修订状态。文本材料额外需要页码、段落、表格、截图或快照位置。人工材料额外需要样本说明、记录时间、访谈对象或来源类别、偏差和权限边界。

无法补齐上述元数据时，可以保留为线索、背景或候选材料，但不得计入关键证据篮子的正式满足状态。

---

## 7. 材料处理、归一与实例入库

### 7.1 区分四类材料

03 必须区分四类材料：

| 类型 | 含义 |
|---|---|
| 原始材料 | 接口响应、表格、公告、财报、网页、访谈纪要等尚未处理的输入 |
| 语义实例 | 公司、产品、材料、环节、设施、资产及稳定关系 |
| 推理输入 | 有业务时间的事件、状态绑定、观测、信号或市场预期 |
| 证据记录 | 来源文档、最小主张、归一事实候选及其支持、削弱、阻断或冲突作用 |

结构化数据不因来自数据库就自动成为事实。文本材料也不因来自一手来源就自动满足目标口径。所有材料都必须经过来源确认、范围对齐、口径检查、时间匹配和质量判断。

### 7.2 留痕要求

每条进入快照的记录，至少应能回溯以下链路：

```text
证据要求预设
→ 取证规则
→ SourceProfile / 推荐取证来源
→ 获取方式
→ 原始材料或原始字段
→ 原子主张或原始数值
→ 清洗与归一处理
→ 对象/关系/事件/观测/证据记录
→ 绑定的状态变量、路径节点或判断单元
→ 质量等级
→ 04 可使用范围
```

不能回溯的材料不得进入有效证据计数。

### 7.3 身份和关系归一

公司、产品、材料、设施、客户、供应关系、地区和资产必须进行身份归一。

至少记录：

1. 原始名称；
2. 标准名称；
3. 别名；
4. 来源；
5. 归一规则；
6. 不确定性；
7. 是否为正式本体实例或本次运行实例。

关系实例必须说明关系类型、方向、时间、来源和证据状态。不得把推测关系写成已确认关系。

### 7.4 数值和时间归一

数值类材料必须保留：

1. 原始值；
2. 原始单位；
3. 转换后值；
4. 转换规则；
5. 业务时间；
6. 发布时间；
7. 获取时间；
8. 统计周期；
9. 口径说明；
10. 字段来源和查询参数。

时间必须区分业务发生时间、材料发布时间、系统获取时间和本次数据截止时点。

### 7.5 证据归一

证据记录应拆成最小可复核主张。每条证据至少说明：

| 字段 | 说明 |
|---|---|
| `source_document` | 来源文档 |
| `claim_text` | 来源中的最小主张 |
| `fact_candidate` | 归一后的事实候选 |
| `statement_type` | 事实、数据、预测、观点、指引、假设或估计 |
| `effect_on_judgment` | 支持、削弱、阻断、冲突或补充 |
| `target_node` | 绑定的路径节点或判断单元 |
| `evidence_basket` | 所属证据篮子 |
| `quality_level` | 证据质量等级 |
| `confidence_limit` | 可支持的最高判断强度 |
| `counter_check` | 是否完成反证检查 |
| `usage_limit` | 可用边界和不可外推范围 |

来源观点不得被归一为已发生事实。预测和指引可以作为预期或情景输入，但不得替代实际观测。

### 7.6 原始材料定位与内容指纹

每个来源文档应保留可复核定位信息。至少包括：

1. 来源名称；
2. 标题；
3. 发布时间；
4. 获取时间；
5. URL、文件名、数据库查询、API 请求、MCP 调用或内部文档 ID；
6. 页码、表格、段落、字段位置或响应字段路径；
7. 内容指纹或版本标识；
8. 访问权限和使用限制。

---

## 8. 质量判断与准入

### 8.1 质量判断分六层

03 的质量判断应至少分六层：

1. 证据要求质量：是否先完成证据要求预设，且与 02 判断单元匹配；
2. 来源质量：来源是否直接、独立、可复核、足够新、权限合规；
3. 记录质量：字段、单位、时间、口径、来源和获取留痕是否完整；
4. 映射质量：是否正确映射到对象、关系、状态变量、路径节点和判断单元；
5. 证据门槛：核心判断单元是否满足最低证据组合、反证和竞争解释检查；
6. 包级准入：本次证据包整体允许 04 使用到什么范围和强度。

不得用其中任何一层替代其他层。尤其不得用覆盖率、材料数量或来源数量替代核心判断单元证据门槛。

### 8.2 单项需求状态

单项需求状态建议使用：

| 状态 | 含义 |
|---|---|
| `met` | 满足最低证据要求 |
| `partial` | 有有效输入，但不足以完整支持 |
| `missing` | 未取得有效材料 |
| `conflicted` | 存在无法消解的证据冲突 |
| `blocked` | 关键来源、权限、口径或实例缺失，无法继续 |
| `not_applicable` | 经确认不适用于本次范围 |

### 8.3 关键变量证据覆盖率

关键变量覆盖率用于检查 02 要求的变量—锚点单元是否完成证据闭环。

覆盖率计算必须说明：

1. 分母如何确定；
2. 哪些单元计入；
3. 哪些单元不计入；
4. 每个计入单元是否完成来源—主张—事实候选—关键变量绑定；
5. `partial` 是否计入，计入理由是什么；
6. 未计入单元的影响。

不得通过删除锚点、缩小范围或复用宽泛主张来提高覆盖率。

### 8.4 核心问题准入

每个核心问题必须同时记录两类正交状态：

| 维度 | 允许值 | 回答的问题 |
|---|---|---|
| `full_reasoning_ready` | 支持、反证、口径、时间和质量要求均满足 | 可形成已确认或倾向判断 |
| `directional_only` | 主方向有支持，但来源、口径、代理或反证限制明显 | 只能形成倾向或弱方向判断 |
| `conditional_only` | 必须依赖特定假设、代理指标或受限来源 | 只能形成条件判断 |
| `insufficient` | 关键证据不足 | 只能写暂不可判断 |
| `blocked` | 关键前提、来源权限、引用链或证据链断裂 | 路径停止，不得方向性推理 |
| `contested` | 支持和反证冲突未消解 | 必须写争议或待验证 |

`claim_mode`、`judgment_status`、`path_status` 分别记录条件模式、争议/削弱状态和路径阻断，不参与强度排序。04 实际等级不得超过 `maximum_judgment_level`。

### 8.5 整体证据评估

整体证据评估用于说明本次证据包整体能否进入 04。

| 状态 | 使用条件 | 04 处理 |
|---|---|---|
| `normal_pass` | 关键判断最低证据要求和关键路径全部满足；反证和竞争解释已完成最低检查 | 可在范围内执行完整路径推理 |
| `restricted_pass` | 只有部分判断单元达到方向性推理门槛，或存在候选结构、口径、来源、代理、反证限制 | 只允许对明确列出的单元方向性推理 |
| `incomplete_pass` | 证据不足以方向性判断，但足以说明缺口和后续补证据方向 | 只能生成高质量“暂不可判断”或缺口报告 |
| `failed` | 关键证据、来源、引用链、权限或入口条件不足 | 不进入 04，输出返工要求 |

`restricted_pass` 必须列出允许使用的核心问题，不得只写“受限通过”。

**核心问题发布底线：** 拟标记为 `high_quality_pass` 或进入全链正式发布时，与 01 核心判断问题直接绑定的核心问题中，至少 1 个 `maximum_judgment_level` 达到 J2。若全部为 J0/J1，整体证据评估不得高于 `incomplete_pass`。

### 8.6 证据包可信程度

证据包可信程度建议使用 `high`、`medium`、`low`。

判断时应考虑：

1. 证据要求预设是否完整；
2. 关键来源质量；
3. 独立交叉验证程度；
4. 时间和口径匹配程度；
5. 反证检查程度；
6. 冲突处理情况；
7. 关键判断单元满足比例；
8. 代理指标依赖程度；
9. 对 04 结论强度的限制；
10. 对 05 成稿素材的支撑程度。

可信程度不能高于关键核心问题证据门槛的实际满足程度。
证据包可信程度不得用单项 Evidence Reliability Score 的平均值替代；关键单元的最低门槛、反证和冲突具有一票降级作用。

---

## 9. 05 成稿素材门槛

03 不写 05 正文，也不裁决投研观点，但必须判断本次证据是否足以支撑目标 05 交付形态。03 应从 01 的 `delivery_archetype.primary` 读取目标原型，并按原型检查成稿素材。

若 01 指定 `industry_cycle_report`，03 至少应检查以下素材单元：

| 素材单元 | 最低要求 | 对 05 的影响 |
|---|---|---|
| 主结论证据 | 主方向至少有两类独立证据支撑，并完成反证检查 | 决定能否写成高质量主线 |
| 价格证据 | 至少有同口径价格、现货价、合约价或公司实现价序列；需说明口径限制 | 决定价格章节和图表质量 |
| 需求证据 | AI/服务器需求与消费端需求必须分开，不得用单一需求指标替代 | 决定分化表达质量 |
| 供给证据 | 名义产能、有效供给、良率、认证、封装或晶圆分配至少拆开关键层级 | 决定机制章节质量 |
| 库存证据 | 原厂、渠道、客户至少说明覆盖层级和缺失层级 | 决定周期结束/正常化表达边界 |
| 订单证据 | 订单兑现、取消、延迟拉货、重复下单或客户锁单至少有可观察口径 | 决定改变判断的信号是否可靠 |
| 分对象排序素材 | 强弱排序需绑定证据，不得只按直觉排序 | 决定对象分化章节质量 |
| 预期差素材 | 市场常见理解、竞争解释或误读风险需有材料来源或研究问题来源 | 决定预期差章节能否成立 |
| 图表素材 | 至少 2 个研报级量化图表候选，或 1 个量化图表候选 + 1 个强弱排序表候选 | 决定能否 high_quality_pass |
| 改判信号素材 | 至少三类关键变量具备可观察触发条件 | 决定跟踪和风险质量 |
| 来源注释素材 | 关键数据可追溯到来源、发布时间、业务时间、口径和使用限制 | 决定 05 来源表达是否可复核 |

素材单元状态使用：

| 状态 | 含义 |
|---|---|
| `report_grade_ready` | 可支撑目标 05 的高质量成稿 |
| `usable_with_caveat` | 可用，但必须保留限制，05 不得放大 |
| `partial` | 有素材，但不足以支撑稳定成稿 |
| `missing` | 缺失 |
| `not_applicable` | 不适用于本次 05 原型 |

### 9.1 成稿素材与观点的边界

03 可以写：

```text
该数据可支撑价格阶段判断，具备季度对比口径，使用限制是不代表 HBM 同代际成交价。
```

03 不应写：

```text
价格已经兑现，下一步看斜率。
```

前者是证据资产说明，属于 03；后者是观点裁决，属于 04。

---

## 10. 单次运行产物

### 10.1 产物组成

03 每次运行形成三项配对产物：

~~~text
03-<核心主题>数据与证据准备-<YYYYMMDD>-<当日序号>.md
03-<核心主题>语义域与证据域实例清单-<YYYYMMDD>-<当日序号>.yaml
03-<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
~~~

实例清单说明本次生成了哪些语义域实例、证据域实例和冻结推理输入；快照目录保存批量实例与质量门结果。03 不生成推理规则执行结果或 Judgment。

快照目录按四层组织：

~~~text
manifest.csv
01_plan/
  evidence_requirements.csv
  evidence_recipe_matches.csv
  evidence_baskets.csv
  source_profiles.csv
  acquisition_channels.csv
  proxy_indicators.csv
02_assets/
  source_snapshot.csv
  acquisition_log.csv
  source_documents.csv
  evidence_claims.csv
  evidence_facts.csv
  evidence_relations.csv
  evidence_assessments.csv
  semantic_instances.csv
  semantic_relations.csv
  reasoning_inputs.csv
  evidence_records.csv
03_gate/
  evidence_readiness_assessments.csv
  judgment_unit_readiness.csv
  state_variable_coverage.csv
  path_readiness.csv
  gaps_and_risks.csv
04_05_materials/
  display_data_candidates.csv
  chart_data_package.csv
  table_material_package.csv
  source_annotation_package.csv
  05_material_readiness.csv
~~~

职责说明：

1. manifest.csv 保存总索引、准入状态和版本；
2. 01_plan 保存 02 已冻结的证据合同在本次运行中的执行计划；
3. source_documents、evidence_claims、evidence_facts、evidence_relations 和 evidence_assessments 构成规范化证据域实例；
4. semantic_instances 和 semantic_relations 保存本次确认的语义域实例；
5. reasoning_inputs 只保存有证据支撑并已冻结的 Observation、Event 等推理输入；
6. evidence_readiness_assessments 保存判断单元整体证据准入和 04 使用方式；
7. judgment_unit_readiness 保存 J0—J4 的最高判断强度及其证据依据；
8. 04_05_materials 保存可进入推理报告和研报正文的图表、表格与来源注释素材；
9. evidence_records.csv 汇总原子证据的任务级使用属性；来源、主张和事实必须回溯到规范化证据链。

### 10.2 准备 Markdown 应包含

字段、章节和 schema 以 输出模板/03_数据与证据准备模板.md 为准。当前准备文档使用 schema_version 1.2.0；判断强度门使用 J0—J4。allowed_04_output 只表示材料使用方式，不能替代判断强度。

正文必须用研究员能够快速复核的语言说明：

1. 本次范围和研究基线；
2. 数据与证据需求；
3. 来源、取数方式和口径；
4. 语义实例、证据实例与推理输入如何形成；
5. 各核心判断需要什么支持证据、反证和独立来源；
6. 覆盖、路径、缺口和补证动作；
7. 04/05 可使用哪些数据，哪些不能外推；
8. 最终准入结论和快照索引。

### 10.3 数据与证据快照摘要

字段与章节以 `输出模板/03_数据与证据快照模板/03_数据与证据快照模板.md` 为准。摘要文件必须与快照目录同名，并放在目录内。

1. 准入状态；
2. 证据包可信程度；
3. 证据质量等级分布；
4. 覆盖率；
5. 核心判断单元状态；
6. 反证和竞争解释检查状态；
7. 代理指标依赖程度；
8. 主要缺口；
9. 04 允许使用范围；
10. 04 禁止外推范围；
11. 置信度上限；
12. 文件指纹和引用链。

### 10.4 快照文件分工

每个快照文件应职责单一，避免同一信息在多个文件中反复维护。

如某项信息同时出现在摘要和 CSV 中，应保证口径一致。摘要用于人读，CSV 用于机器校验。

### 10.5 05 可展示数据支持

03 不设计研报图表，也不替 05 写研报结论；但 03 必须标明哪些数据具备后续制图或制表条件，且要进一步标明这些数据是否达到研报级展示密度。否则 05 只能写研究整理稿，不能稳定生成高质量内部深度研报。

`display_data_candidates.csv` 至少包含：

| 字段 | 说明 |
|---|---|
| `data_candidate_id` | 可展示数据候选 ID |
| `linked_judgment_unit_ids` | 绑定的核心判断单元 |
| `linked_input_ids` | 对应推理输入 |
| `linked_evidence_ids` | 对应证据记录 |
| `object` | 展示对象 |
| `indicator` | 指标名称 |
| `value_field` | 使用 `raw_value`、`normalized_value` 或文本事实字段 |
| `unit` | 单位 |
| `period` | 业务时间或统计期 |
| `grain` | 时间序列、横截面、事件、文本观测等粒度 |
| `source_ids` | 来源 ID |
| `quality_level` | 证据质量等级 |
| `data_status` | `ready / partial / missing` |
| `visual_role` | `quant_chart / ranking_table / event_timeline / supporting_table / qualitative_table` |
| `chart_readiness` | 制图成熟度：`ready / partial / missing` |
| `quantitative_or_qualitative` | 数据性质：`quantitative / qualitative / mixed` |
| `time_points_count` | 可用于时间序列或窗口比较的时间点数量；非时间序列可填对象数量或 0 |
| `comparison_baseline` | 可比较基准，例如前期、同业、强弱排序、基准情景等 |
| `ranking_support` | 是否支持对象强弱排序：`none / weak / medium / strong` |
| `report_grade_status` | 是否达到研报级展示标准：`report_grade_ready / usable_with_caveat / not_report_grade` |
| `gap_to_report_grade` | 未达到研报级展示时缺少什么 |
| `usage_limit` | 可展示但不可外推的限制 |
| `caveat` | 数据缺口、口径差异或解释注意事项 |

可展示数据候选必须来自本次冻结快照，不得为了后续图表补造新数值。若某项数据只能支持文字观察或仅为预测，应在 `data_status`、`chart_readiness`、`report_grade_status` 和 `usage_limit` 中说明。若没有可展示数据，应生成空表头，并在准备文档中说明原因。

若下游目标是 05 高质量内部深度研报，03 至少应能提供：

1. 两个 `report_grade_ready` 的量化图表候选；或
2. 一个 `report_grade_ready` 的量化图表候选，加一个 `report_grade_ready` 且 `ranking_support=strong` 的强弱排序表候选。

达不到上述条件时，03 可以进入 04 推理，但 05 不得标为高质量研报稿；应降级为内部研究整理稿、缺口稿，或退回 03 补充可展示数据。

---

## 11. 执行流程

03 按以下顺序执行：

1. 读取并校验 02 产物；
2. 冻结运行标识、时间范围、对象范围、数据截止时点、来源范围、取数方式范围和口径范围；
3. 识别核心判断单元的证据角色；
4. 为核心判断单元预设证据要求；
5. 调用取证策略库，匹配主取证规则、辅助取证规则、反证规则和必要的代理指标规则；
6. 展开必需证据篮子和可选证据篮子；
7. 查询 B_取证来源，匹配推荐来源和 SourceProfile，并明确 ContentDomain、AccessScope 和 ArtifactType；
8. 选择 AcquisitionChannel，并确定取数留痕要求；
9. 将判断路径拆解为数据、实例、证据和反证需求；
10. 为核心判断单元建立证据门槛表；
11. 按关键、必要、补充的优先级排序；
12. 按约定口径获取材料；
13. 记录每次获取尝试、参数、结果、失败原因和停止原因；
14. 对材料进行清洗、去重、身份归一、口径转换和时间对齐；
15. 将材料映射为对象实例、关系实例、推理输入和证据记录；
16. 对单条材料、证据记录、证据篮子和判断单元进行质量评价；
17. 按“状态变量 × 锚点实例”形成覆盖矩阵；
18. 按核心判断单元检查最低证据门槛；
19. 检查支持证据、反证、竞争解释和来源冲突；
20. 判断每项需求、每个状态变量锚点、每条路径和每个核心判断单元的就绪状态；
21. 判断 05 可展示数据和成稿素材成熟度；
22. 形成准备 Markdown 和数据证据快照；
23. 输出包级准入结论、04 最多能形成的结论和质量状态。

同一来源在同一参数和同一口径下，应产生可解释的一致结果。来源发生修订时，应保留获取时间、业务时间、版本说明和内容指纹，不得覆盖历史记录。

---

## 12. 补充、停止与反馈

### 12.1 补充规则

以下情况应继续补充材料：

1. 关键判断单元尚未满足最低证据门槛；
2. 证据要求预设中的必需证据篮子缺失；
3. 反证或竞争解释尚未检查；
4. 来源不独立；
5. 口径冲突尚未解释；
6. 时间窗口不匹配；
7. 代理指标使用边界不清；
8. 证据冲突会改变 04 观点等级；
9. 03 无法说明 04 最多能形成的结论；
10. 目标 05 所需成稿素材缺口会导致交付形态降级。

### 12.2 单项停止规则

单项需求可以停止补充的条件是：

1. 已满足证据要求预设中的最低要求；
2. 支持证据和反证检查达到最低要求；
3. 口径和时间范围匹配；
4. 来源质量和取数方式可解释；
5. 继续补充不会改变该单元可推理状态；
6. 停止原因已记录。

### 12.3 包级停止规则

包级停止应区分四种情况：

1. 全部关键核心问题满足，可以 `normal_pass`；
2. 部分关键核心问题满足，可以 `restricted_pass`；
3. 证据不足但缺口清楚，可以 `incomplete_pass`；
4. 引用链、来源、权限或关键前提断裂，必须 `failed`。

不得因为时间耗尽、材料数量较多或覆盖率表面达标而冻结正式准入。

### 12.4 反馈规则

03 应把问题反馈给对应阶段：

| 问题类型 | 反馈对象 |
|---|---|
| 研究问题、范围或用途不清 | 01 |
| 核心问题、路径、最低验证条件不清 | 02 |
| 本体无法承接关键机制 | 02 或本体优化 |
| 证据要求无法设定 | 02 |
| 来源、口径、权限或材料缺失 | 03 自身补充 |
| 数据质量不足以支撑方向性观点 | 03 降级或返工 |
| 证据不足以支撑方向性观点 | 04 降级或 03 返工 |
| 05 素材不足 | 05 降级或 03 补齐素材 |

---

## 13. 与 04 推理阶段的接口

03 交给 04 的不是“材料包”，而是“带使用边界的推理输入包”。其中 Evidence Permission Matrix 是强制桥接合同：每个 JudgmentUnit 必须同时交接最终 `evidence_quality_level`、`evidence_permission`、`allowed_04_output` 和 `maximum_judgment_level`；四者取最严格边界。

必须明确：

1. 哪些核心问题允许方向性推理；
2. 哪些核心问题只能条件判断；
3. 哪些核心问题只能暂不可判断；
4. 哪些路径逻辑难以继续成立；
5. 哪些反面证据已检查；
6. 哪些其他可能解释仍未排除；
7. 哪些证据冲突会限制 04 结论；
8. 哪些证据依赖代理指标；
9. 哪些证据存在来源、口径、时间或权限限制；
10. 04 的置信度上限；
11. 04 不得外推的对象、时间、地域和结论；
12. 目标 05 原型下，哪些图表、表格、来源注释和强弱排序素材已经达到研报级使用，哪些只能降级或退回补充。

04 必须继承这些限制，不得用整体证据评估支持所有观点。

---

## 14. 质量门槛与返工规则

03 产物必须满足 `00A_高质量产出判别标准.md` 中关于数据与证据准备阶段的要求。为避免只达到流程合格，本阶段生成前必须完成以下检查。

### 14.1 阶段质量状态

建议使用以下质量状态：

| 状态 | 含义 | 处理 |
|---|---|---|
| `draft` | 证据准备尚未完成或未校验 | 不进入 04 |
| `minimum_pass` | 流程和文件完整，可供 04 草稿使用 | 可进入 04 草稿，但不得正式发布 |
| `high_quality_pass` | 通过确定性校验和独立语义审阅 | 可作为正式 03 产物交付 |
| `return_required` | 关键证据、口径、来源、权限、质量或准入问题未解决 | 返工 |
| `stop_with_gap_report` | 当前条件不足以支撑方向性推理 | 输出缺口说明 |

### 14.2 最低准入门槛

达到以下条件，才可标记为 `minimum_pass`：

1. 02 入口校验通过；
2. 运行范围已冻结；
3. 已完成证据要求预设；
4. 数据需求按判断单元拆解；
5. 取证规则、推荐取证来源、SourceProfile 和取数方式已说明；
6. 快照文件完整；
7. 状态变量覆盖矩阵可复核；
8. 包级准入状态可解释；
9. 04 最多能形成的结论已给出。

### 14.3 高质量语义门槛

达到以下条件，才可标记为 `high_quality_pass`：

1. 证据准备围绕关键判断单元，而不是主题泛搜；
2. 每个核心判断单元已完成证据要求预设；
3. 核心结论证据门槛已单独判断；
4. 已调用合适的取证规则，而不是临时自由搜索；
5. 来源生产者、内容领域、获取通道、权限边界和材料形态已区分；
6. 来源直接性、独立性、可复核性、时效性、口径和权限均已评价；
7. 不同取数方式的留痕要求已满足；
8. 反证和竞争解释有实际检查；
9. 代理指标使用边界、失效条件和置信度折扣已说明；
10. 证据冲突有处理或降级；
11. `restricted_pass` 明确列出可用判断单元；
12. `incomplete_pass` 明确说明为什么只能暂不可判断；
13. 04 的使用上限清楚、可执行；
14. 准备 Markdown 与快照 CSV 一致；
15. 独立语义审阅通过；
16. 若 01 目标包含高质量 05，已明确 05 素材成熟度、图表数据密度、强弱排序支撑和补齐路径。

### 14.4 不得进入 04 方向性推理的情况

出现以下任一情况，不得进入 04 方向性推理：

1. 未完成证据要求预设；
2. 关键判断单元证据门槛未满足；
3. 必需证据篮子缺失；
4. 反证检查缺失；
5. 来源不可复核；
6. 来源不独立却被当作交叉验证；
7. 口径与 02 判断单元不匹配；
8. 时间窗口不匹配；
9. 动态变量缺业务时间；
10. 权限或合规边界不清；
11. 证据冲突未处理；
12. 代理指标越权使用；
13. 准入范围没有明确列出；
14. 03 无法说明 04 的使用上限。

### 14.5 返工路由

| 问题表现 | 处理 |
|---|---|
| 02 路径、判断单元或最低验证条件不清 | 退回 02 |
| 无法设定证据要求 | 退回 02 或重做 03 证据要求预设 |
| 证据需求拆解过宽 | 重做 03 需求拆解 |
| 取证规则选择错误 | 重选取证规则并重做证据篮子 |
| 来源结构混乱 | 重做 SourceProfile / ContentDomain / AcquisitionChannel / AccessScope / ArtifactType 拆分 |
| 来源不直接或不可复核 | 更换或补充来源 |
| 证据口径不匹配 | 重新取数或降级 |
| 获取方式留痕不足 | 补充查询参数、原始响应、字段血缘或内容指纹 |
| 缺少反证检查 | 补充反证检索 |
| 代理指标越权 | 降级或补充直接证据 |
| 覆盖率高但核心观点门槛不足 | 降级或补证据 |
| 准入范围不清 | 重写整体证据评估和 04 结论适用范围 |
| 快照与准备 Markdown 不一致 | 同步修正并重新校验 |

---

## 15. 运行检查清单

每次 03 完成前，至少检查：

1. 是否继承 02 的核心问题和路径；
2. 是否冻结运行范围；
3. 是否完成证据要求预设；
4. 是否识别每个核心判断单元的证据角色；
5. 是否调用合适的取证规则；
6. 是否展开必需证据篮子；
7. 是否区分 SourceProfile、ContentDomain、AcquisitionChannel、AccessScope 和 ArtifactType；
8. 是否按判断单元拆解证据需求；
9. 是否形成核心判断单元证据门槛表；
10. 是否区分事实、数据、观点、预测、指引和假设；
11. 是否评价来源直接性、独立性、可复核性、时效性、口径和权限；
12. 是否满足不同取数方式的留痕要求；
13. 是否进行反证和竞争解释检查；
14. 是否处理证据冲突；
15. 是否说明代理指标使用边界；
16. 是否明确单项需求、状态变量、路径和判断单元状态；
17. 是否明确包级准入、置信度上限和 04 最多能形成的结论；
18. 是否说明 05 成稿素材成熟度；
19. 是否说明返工、补充和停止原因。

---

## 16. 一句话原则

03 的好坏不取决于材料数量，而取决于：

> 是否先为关键判断设定证据要求，再取得匹配口径、可追溯、可复核、经过反证检查的足够证据，并清楚告诉 04 只能判断到哪里。
