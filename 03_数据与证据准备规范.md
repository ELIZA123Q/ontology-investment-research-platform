# 03_数据与证据准备规范

## 1. 文档定位

本文档规范 03 阶段如何把 02 已经形成的研究逻辑和任务本体视图，转化为一次可执行、可复核、可追溯的数据与证据准备过程。

03 的核心目标不是泛泛收集资料，也不是用覆盖率制造“证据充分”的错觉，而是判断：**关键核心问题是否取得了足够质量的证据，哪些观点可以交给 04 推理，哪些只能受限使用，哪些必须暂不可判断或返工。**

03 应回答六个问题：

1. 本次判断需要哪些现实实例、数据、证据和反面证据；
2. 每项材料应该从哪些来源取得，采用什么口径；
3. 实际取得的材料是否直接、独立、可复核、足够新；
4. 材料如何清洗、归一，并映射到 02 的任务本体视图；
5. 每个关键核心问题是否满足最低证据门槛；
6. 本次证据包允许 04 判断什么，不允许判断什么。

本阶段只形成两项正式产物：

```text
03-<核心主题>数据与证据准备-<YYYYMMDD>-<当日序号>.md
03-<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
```

正式产出必须以阶段前缀 `03-` 开头。

准备 Markdown 说明“本次需要什么、去哪取、怎么取、怎么处理、是否够用、允许 04 怎么用”；快照目录保存本次实际取得的数据、证据、质量状态、缺口、反面证据、冲突和准入结果。

两项产物必须使用同一组任务标识、运行标识、冻结范围、数据截止时点和来源版本，不得拆开流转。

`normal_pass`、`restricted_pass`、`incomplete_pass`、`failed` 是证据准入状态，不等同于研究结论强度。正式交付时，还必须满足 `00A_高质量产出判别标准.md` 中对 03 的质量要求。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

03 负责把 02 的判断结构变成可执行的数据与证据准备方案，主要包括：

1. 读取并校验 02 研究逻辑和任务本体视图；
2. 冻结本次运行的时间范围、对象范围、来源范围、口径范围和数据截止时点；
3. 将 02 的核心问题、传导环节、实例需求、观测要求、证据要求和反面证据要求拆解为具体数据需求；
4. 为每项需求明确首选来源、交叉验证来源、替代来源和取数口径；
5. 记录实际获取过程，包括来源、发布时间、业务时间、查询条件、失败原因和停止原因；
6. 对取得材料进行清洗、去重、身份归一、口径转换和时间对齐；
7. 将材料映射为对象实例、关系实例、事件、状态观测、信号、市场预期、证据记录和反面证据记录；
8. 判断每项需求、每个关键变量锚点、每条路径和每个核心问题是否具备后续推理所需的最低输入；
9. 主动检查反面证据、冲突证据和其他可能解释；
10. 输出证据包可信程度、准入状态、可使用范围、置信度上限和 04 结论适用范围；
11. 判断本阶段产物是草稿、最低通过，还是达到高质量交付标准。

### 2.2 本阶段不负责

03 不做以下事情：

1. 不修改 01 已确认的研究问题、对象范围和时间边界；
2. 不修改 02 已确定的判断脊柱、问题树、传导环节、本体结构和最低验证条件；
3. 不为了“完整”而抓取与当前判断无关的全部新闻、全部公司或全部历史数据；
4. 不把搜索结果、网页摘要、模型记忆或二手转述直接当作证据；
5. 不把预测、观点、假设、指引和已发生事实混为一类；
6. 不执行 02 的推理规则，也不形成方向性投研结论；
7. 不因主源不可用而静默更换数据口径；
8. 不用来源数量、综合分数、材料篇数或覆盖率替代关键质量门槛；
9. 不在运行产物中保存账号、密钥、Cookie 等敏感凭据。

如果 02 的核心问题、最低验证条件或本体映射不清，03 应退回 02，不得通过临时搜索资料自行补造判断结构。

---

## 3. 入口校验与运行冻结

### 3.1 入口校验

03 开始前，应先确认 02 产物是否足以进入数据与证据准备。

至少确认以下事项：

1. 已收到配对的研究逻辑 Markdown 和任务本体视图 YAML；
2. 两份文件对应同一研究问题、同一对象范围、同一判断落点和同一任务 ID；
3. 02 已形成判断脊柱和最小问题树；
4. 02 已列出关键传导环节及节点关系；
5. 每个关键传导环节已经说明所需实例、观测、证据、反面证据和最低验证条件；
6. 02 已说明反面证据、其他可能解释、停止条件和降级规则；
7. 如使用候选结构或存在本体缺口，相关限制已经传递到 03；
8. 02 阶段状态和质量状态可解析。

入口处理规则如下：

| 02 状态 | 03 处理 |
|---|---|
| `high_quality_pass` | 可进入正式 03 |
| `minimum_pass` | 可进入 03，但 03 应标记上游质量限制，并不得自动形成高质量准入 |
| `draft` | 不进入正式 03 |
| `return_required` | 退回 02 |

如果缺少的是现实公司、产品、关系、事件、数值、来源或材料，由 03 继续准备。  
如果缺少的是判断脊柱、路径结构、本体承接、对象拆分或最低验证条件，应退回 02。  
如果研究问题和对象边界仍有关键歧义，应退回 01。

### 3.2 运行冻结

入口校验通过后，03 必须冻结本次运行范围。

每次运行至少冻结以下内容：

| 冻结项 | 说明 |
|---|---|
| 任务标识 | 对应本次研究问题或判断任务 |
| 运行标识 | 对应本次数据与证据准备过程 |
| 数据截止时点 | 明确本次材料统计到什么时候 |
| 时间范围 | 将“当前”“近半年”“事件后”等表达转为具体时间规则 |
| 对象范围 | 明确纳入和排除的公司、产品、材料、资产、地区或时间段 |
| 来源范围 | 明确本次允许使用的数据源、公开材料或内部材料 |
| 口径范围 | 明确产品规格、币种、单位、地域、价格类型、产能类型等关键口径 |
| 02 文件指纹 | 防止证据准备期间上游结构漂移 |

同一次运行中，准备 Markdown、快照目录和后续 04 推理必须使用同一套冻结范围。

如后续需要扩大对象、改变时间范围、替换关键口径或修改来源范围，应生成新的运行版本，不得覆盖旧快照。

### 3.3 候选结构的处理

如果 02 使用了任务候选结构，03 可以继续准备数据、实例和证据，但必须说明：

1. 哪些需求依赖候选结构；
2. 候选结构的使用边界；
3. 可能影响哪些传导环节；
4. 是否限制 04 的观点强度；
5. 后续是否需要进入本体优化。

只要关键路径依赖候选结构，即使数据和证据充分，本次整体证据评估原则上也不应高于 `restricted_pass`，除非 02 已说明候选结构不影响本次判断强度并通过独立审阅。

---

## 4. 数据需求拆解原则

### 4.1 按核心问题拆解，不按主题泛搜

03 不应围绕宽泛主题搜索资料，而应从 02 的核心问题和传导环节出发，把每个关键节点拆成可执行的数据需求。

每项数据需求至少说明：

| 维度 | 说明 |
|---|---|
| 需求目的 | 用来支持、削弱、逻辑失效或验证哪个判断节点 |
| 本体目标 | 对应对象、关系、事件、关键变量、观测、信号或证据 |
| 最低字段 | 身份、数值、单位、时间、范围、来源等不可缺字段 |
| 时间要求 | 业务时间、发布时间、数据频率、截面或时间序列要求 |
| 口径要求 | 产品规格、地域、币种、价格类型、产能类型等 |
| 覆盖要求 | 需要覆盖哪些对象、时间点、来源或反面证据条件 |
| 质量门槛 | 可用于后续推理所需满足的最低条件 |
| 路径绑定 | 对应路径、节点或判断问题 |
| 反面证据绑定 | 对应削弱、逻辑失效或其他可能解释 |
| 优先级 | 关键、必要或补充 |

### 4.2 区分四类材料

03 必须区分四类材料：

| 类型 | 含义 |
|---|---|
| 原始材料 | 接口响应、表格、公告、财报、网页、访谈纪要等尚未处理的输入 |
| 语义实例 | 公司、产品、材料、环节、设施、资产及稳定关系 |
| 推理输入 | 有业务时间的事件、状态绑定、观测、信号或市场预期 |
| 证据记录 | 来源文档、最小主张、归一事实候选及其支持、削弱、逻辑失效或冲突作用 |

结构化数据不因来自数据库就自动成为事实。文本材料也不因来自一手来源就自动满足目标口径。所有材料都必须经过来源确认、范围对齐、口径检查、时间匹配和质量判断。

### 4.3 最小充分原则

03 只准备回答当前问题所需的最小充分集合。

准备顺序如下：

1. 先满足关键节点的身份、关系和硬性证据要求；
2. 再满足支持证据、反面证据和其他可能解释的最低交叉验证要求；
3. 再补充能够显著降低冲突或范围不确定性的材料；
4. 达到最低门槛，且新增信息不会改变节点状态时，应停止补充；
5. 补充性材料不得阻塞整体准入。

“没有找到”只表示在当前权限、来源和检索范围内缺失，不表示事实本身不存在。

### 4.4 核心结论证据门槛

03 必须把“证据覆盖率”和“核心结论证据门槛”分开。

覆盖率回答：

```text
02 要求的变量—锚点、实例和证据记录完成了多少。
```

核心结论证据门槛回答：

```text
某个 04 可能输出的方向性观点，最低需要哪些证据，目前是否满足。
```

因此，03 必须形成“核心问题证据门槛表”。

| 字段 | 说明 |
|---|---|
| judgment_unit_id | 核心问题 |
| possible_view | 可能支持的 04 观点 |
| required_evidence | 必要证据组合 |
| support_status | 支持证据状态 |
| counter_status | 反面证据检查状态 |
| conflict_status | 冲突证据状态 |
| scope_limit | 使用范围限制 |
| allowed_04_output | 允许 04 输出的最高观点等级 |
| gap_action | 缺口处理或返工要求 |

某个变量覆盖率高，不代表对应核心观点可以方向性输出。只有核心问题证据门槛满足，04 才能使用该观点。

---

## 5. 数据来源与取数方式

### 5.1 来源使用原则

03 应优先使用已知、稳定、可复核的来源，而不是每次重新设计来源体系。

来源选择原则如下：

1. 政策、监管、财务和已披露经营事实，优先官方原文、交易所文件和公司正式披露；
2. 订单、交付、良率、产能和供应关系，优先一手经营记录；无权限时，用公司披露和独立产业来源交叉验证；
3. 行业规模、设备、材料、产能和价格统计，优先方法论明确、口径稳定的行业数据源；
4. 市场价格、估值和一致预期，优先合法授权且口径可复现的市场数据源；
5. 预测、渠道调研和产业判断，应保留其预测或观点性质，不得替代已发生事实；
6. 搜索引擎只用于发现来源，不应直接作为证据来源。

### 5.2 来源质量判断

每条进入证据包的来源至少判断：

| 维度 | 要求 |
|---|---|
| 直接性 | 来源是否接近原始事实或一手披露 |
| 独立性 | 是否与其他来源属于同一来源组或转述链 |
| 可复核性 | 后续是否能重新找到、重新读取或复算 |
| 时效性 | 发布时间和业务时间是否满足判断窗口 |
| 口径清晰度 | 指标定义、范围、单位和统计方法是否清楚 |
| 权限合规 | 来源获取和使用是否符合权限要求 |

两条新闻转述同一份报告，不构成交叉验证。供应链传闻、渠道观点和模型摘要不得与一手事实等同处理。

### 5.3 取数方式

对于重复出现的数据需求，可以沉淀为取数方式或取数配方。当前阶段不要求工程化建设完整配方库，但每次运行应说清楚：

1. 首选来源是什么；
2. 交叉验证来源是什么；
3. 替代来源是什么；
4. 采用什么查询条件、时间范围和对象范围；
5. 关键字段如何解释；
6. 口径如何统一；
7. 获取失败时如何处理；
8. 何时停止继续补充。

主源不可用时，可以使用替代来源，但必须说明替代来源与主源之间的口径差异，以及该差异对 04 的使用限制。

---

## 6. 执行流程

03 按以下顺序执行：

1. 读取并校验 02 产物；
2. 冻结运行标识、时间范围、对象范围、数据截止时点、来源范围和口径范围；
3. 将判断路径拆解为数据、实例、证据和反面证据需求；
4. 为核心问题建立证据门槛表；
5. 按关键、必要、补充的优先级排序；
6. 为每项需求匹配首选来源、交叉验证来源和替代来源；
7. 按约定口径获取材料；
8. 记录每次获取尝试、参数、结果、失败原因和停止原因；
9. 对材料进行清洗、去重、身份归一、口径转换和时间对齐；
10. 将材料映射为对象实例、关系实例、推理输入和证据记录；
11. 按“关键变量 × 锚点实例”形成覆盖矩阵；
12. 按核心问题检查最低证据门槛；
13. 检查支持证据、反面证据、其他可能解释和来源冲突；
14. 判断每项需求、每个关键变量锚点、每条路径和每个核心问题的就绪状态；
15. 形成准备 Markdown 和数据证据快照；
16. 输出整体证据评估结论、04 结论适用范围和质量状态。

同一来源在同一参数和同一口径下，应产生可解释的一致结果。来源发生修订时，应保留获取时间、业务时间、版本说明和内容指纹，不得覆盖历史记录。

---

## 7. 处理、归一与本体映射

### 7.1 留痕要求

每条进入快照的记录，至少应能回溯以下链路：

```text
来源
→ 原始材料或原始字段
→ 原子主张或原始数值
→ 清洗与归一处理
→ 对象/关系/事件/观测/证据记录
→ 绑定的关键变量、传导环节或核心问题
→ 04 可使用范围
```

不能回溯的材料不得进入有效证据计数。

### 7.2 身份和关系归一

公司、产品、材料、设施、客户、供应关系、地区和资产必须进行身份归一。

至少记录：

1. 原始名称；
2. 标准名称；
3. 别名；
4. 来源；
5. 归一规则；
6. 不确定性；
7. 是否为正式本体实例或本次运行实例。

关系实例必须说明关系类型、方向、时间、来源和证据状态。不得把推测关系写成已确认关系。

### 7.3 数值和时间归一

数值类材料必须保留：

1. 原始值；
2. 原始单位；
3. 转换后值；
4. 转换规则；
5. 业务时间；
6. 发布时间；
7. 统计周期；
8. 口径说明。

时间必须区分业务发生时间、材料发布时间、系统获取时间和本次数据截止时点。

### 7.4 证据归一

证据记录应拆成最小可复核主张。每条证据至少说明：

| 字段 | 说明 |
|---|---|
| source_document | 来源文档 |
| claim_text | 来源中的最小主张 |
| fact_candidate | 归一后的事实候选 |
| statement_type | 事实、预测、观点、指引、假设或估计 |
| effect_on_judgment | 支持、削弱、逻辑失效、冲突或补充 |
| target_node | 绑定的传导环节或核心问题 |
| confidence_limit | 可支持的最高判断强度 |
| counter_check | 是否完成反面证据检查 |

来源观点不得被归一为已发生事实。预测和指引可以作为预期或情景输入，但不得替代实际观测。

### 7.5 原始材料定位与内容指纹

每个来源文档应保留可复核定位信息。至少包括：

1. 来源名称；
2. 标题；
3. 发布时间；
4. 获取时间；
5. URL、文件名、数据库查询或内部文档 ID；
6. 页码、表格、段落或字段位置；
7. 内容指纹或版本标识；
8. 访问权限和使用限制。

---

## 8. 质量判断与准入

### 8.1 质量判断分五层

03 的质量判断应至少分五层：

1. 来源质量：来源是否直接、独立、可复核、足够新；
2. 记录质量：字段、单位、时间、口径和来源是否完整；
3. 映射质量：是否正确映射到对象、关系、关键变量、传导环节和核心问题；
4. 证据门槛：核心问题是否满足最低证据组合、反面证据和其他可能解释检查；
5. 整体证据评估：本次证据包整体允许 04 使用到什么范围和强度。

不得用其中任何一层替代其他层。尤其不得用覆盖率替代核心问题证据门槛。

### 8.2 单项需求状态

单项需求状态建议使用：

| 状态 | 含义 |
|---|---|
| `met` | 满足最低证据要求 |
| `partial` | 有有效输入，但不足以完整支持 |
| `missing` | 未取得有效材料 |
| `conflicted` | 存在无法消解的证据冲突 |
| `blocked` | 关键来源、权限、口径或实例缺失，无法继续 |
| `not_applicable` | 经确认不适用于本次范围 |

### 8.3 关键变量证据覆盖率

关键变量覆盖率用于检查 02 要求的变量—锚点单元是否完成证据闭环。

覆盖率计算必须说明：

1. 分母如何确定；
2. 哪些单元计入；
3. 哪些单元不计入；
4. 每个计入单元是否完成来源—主张—事实候选—关键变量绑定；
5. `partial` 是否计入，计入理由是什么；
6. 未计入单元的影响。

不得通过删除锚点、缩小范围或复用宽泛主张来提高覆盖率。

### 8.4 核心问题准入

每个核心问题应单独给出准入状态：

| 状态 | 含义 | 04 最高使用方式 |
|---|---|---|
| `full_reasoning_ready` | 支持、反面证据、口径和时间要求均满足 | 可形成已确认或倾向判断 |
| `directional_only` | 主方向有支持，但限制明显 | 只能形成倾向或条件判断 |
| `conditional_only` | 必须依赖特定假设或条件 | 只能形成条件判断 |
| `insufficient` | 关键证据不足 | 只能写暂不可判断 |
| `blocked` | 关键前提或证据链断裂 | 路径停止，不得方向性推理 |
| `contested` | 支持和反面证据冲突未消解 | 必须写争议或待验证 |

04 不得超过该状态允许的最高使用方式。

### 8.5 整体证据评估

整体证据评估用于说明本次证据包整体能否进入 04。

| 状态 | 使用条件 | 04 处理 |
|---|---|---|
| `normal_pass` | 关键判断最低证据要求和关键路径全部满足；反面证据和其他可能解释已完成最低检查 | 可在范围内执行完整路径推理 |
| `restricted_pass` | 只有部分核心问题达到方向性判断门槛，或存在候选结构、口径、来源、反面证据限制 | 只允许对明确列出的单元方向性推理 |
| `incomplete_pass` | 证据不足以方向性判断，但足以说明缺口和后续补证据方向 | 只能生成高质量“暂不可判断”或缺口报告 |
| `failed` | 关键证据、来源、引用链或入口条件不足 | 不进入 04，输出返工要求 |

`restricted_pass` 必须列出允许使用的核心问题，不得只写“受限通过”。

**核心问题发布底线：** 拟标记为 `high_quality_pass` 或进入全链正式发布时，与 01 核心判断问题直接绑定的核心问题中，至少 1 个 `allowed_04_output` 达到 `conditional_only` 及以上（含 `directional_only`、`full_reasoning_ready`）。若上述核心问题全部为 `insufficient` 或 `blocked`，整体证据评估不得高于 `incomplete_pass`；缺口清楚时标记 `incomplete_pass`，否则按 `failed` 或 `stop_with_gap_report` 处理。

### 8.6 证据包可信程度

证据包可信程度建议使用 `high`、`medium`、`low`。

判断时应考虑：

1. 关键来源质量；
2. 独立交叉验证程度；
3. 时间和口径匹配程度；
4. 反面证据检查程度；
5. 冲突处理情况；
6. 关键核心问题满足比例；
7. 对 04 结论强度的限制。

可信程度不能高于关键核心问题证据门槛的实际满足程度。

---

## 9. 单次运行产物

### 9.1 产物组成

03 产物包括：

```text
<核心主题>数据与证据准备-<YYYYMMDD>-<当日序号>.md
<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
```

可选使用 `03-` 阶段前缀；校验器接受带前缀与不带前缀两种写法，但同一运行内应保持一致。

快照目录根目录应包含：

1. `manifest.csv`：总索引、准入状态和文件指纹；
2. `source_snapshot.csv`：来源文档与版本；
3. `acquisition_log.csv`：获取记录；
4. `semantic_instances.csv`：对象实例；
5. `semantic_relations.csv`：关系实例；
6. `reasoning_inputs.csv`：事件、状态绑定、观测、信号和市场预期；
7. `evidence_records.csv`：证据记录；
8. `state_variable_coverage.csv`：关键变量覆盖矩阵；
9. `judgment_unit_readiness.csv`：核心问题证据门槛和 04 结论适用范围；
10. `path_readiness.csv`：路径准备状态；
11. `gaps_and_risks.csv`：缺口、限制、冲突和返工要求。

### 9.2 准备 Markdown

字段与章节以 `输出模板/03_数据与证据准备模板.md` 为准。front matter 至少应包含 `document_type`、`schema_version`、`task_id`、`execution_id`、`plan_id`、`source_02_view_id`、`source_02_logic_id`、`source_02_view_ref`、`source_02_logic_ref`、运行冻结字段（`execution_date`、`timezone`、`data_cutoff`、`resolved_*`）、`snapshot_ref`、`snapshot_summary_ref`、`preparation_status`、`quality_status`、`quality_gate_ref`、`admission`、`evidence_quality_level`、`confidence_ceiling`、覆盖率与门槛字段、`search_status`、`allowed_04_output`、`return_required`、`return_stage`。

正文必须包含以下章节，结构与模板一致：

1. 本次范围；
2. 02 交接基线（含关键变量交接基线与核心问题交接基线）；
3. 数据与证据需求；
4. 来源与取数方式；
5. 处理与本体映射；
6. 核心问题证据门槛；
7. 覆盖与路径就绪状态；
8. 准入结论；
9. 快照文件索引；
10. 03 质量门槛检查。

正文第 10 章可保留人工审阅表和质量结论，但不得重复抄写完整 YAML 或 `manifest.csv`。

### 9.3 数据与证据快照摘要

字段与章节以 `输出模板/03_数据与证据快照模板/03_数据与证据快照模板.md` 为准。摘要文件必须与快照目录同名，并放在目录内。

front matter 至少应包含：`document_type`、`schema_version`、`task_id`、`execution_id`、`snapshot_version`、`preparation_ref`、`snapshot_directory`、`admission`、`evidence_quality_level`、`confidence_ceiling`、`allowed_scope`、覆盖率与核心问题计数、`search_status`、`quality_status`、`quality_gate_ref`、`generated_at`。

正文建议包含：整体结果、结果依据、核心问题证据门槛、覆盖重算、关键传导环节、主要来源与证据质量、缺口与风险、对 04 的使用说明、快照文件索引。

### 9.4 快照文件分工

每个快照文件应职责单一，避免同一信息在多个文件中反复维护。

如某项信息同时出现在摘要和 CSV 中，应保证口径一致。摘要用于人读，CSV 用于机器校验。

---

## 10. 补充、停止与反馈

### 10.1 补充规则

以下情况应继续补充材料：

1. 关键核心问题尚未满足最低证据门槛；
2. 反面证据或其他可能解释尚未检查；
3. 来源不独立；
4. 口径冲突尚未解释；
5. 时间窗口不匹配；
6. 证据冲突会改变 04 观点等级；
7. 03 无法说明 04 结论适用范围。

### 10.2 单项停止规则

单项需求可以停止补充的条件是：

1. 已满足 02 规定的最低验证条件；
2. 支持证据和反面证据检查达到最低要求；
3. 口径和时间范围匹配；
4. 来源质量可解释；
5. 继续补充不会改变该单元可推理状态；
6. 停止原因已记录。

### 10.3 包级停止规则

包级停止应区分四种情况：

1. 全部关键核心问题满足，可以 `normal_pass`；
2. 部分关键核心问题满足，可以 `restricted_pass`；
3. 证据不足但缺口清楚，可以 `incomplete_pass`；
4. 引用链、来源或关键前提断裂，必须 `failed`。

不得因为时间耗尽、材料数量较多或覆盖率表面达标而冻结正式准入。

### 10.4 反馈规则

03 应把问题反馈给对应阶段：

| 问题类型 | 反馈对象 |
|---|---|
| 研究问题、范围或用途不清 | 01 |
| 核心问题、路径、最低验证条件不清 | 02 |
| 本体无法承接关键机制 | 02 或本体优化 |
| 来源、口径或材料缺失 | 03 自身补充 |
| 证据不足以支撑方向性观点 | 04 降级或 03 返工 |

---

## 11. 与 04 推理阶段的接口

03 交给 04 的不是“材料包”，而是“带使用边界的推理输入包”。

必须明确：

1. 哪些核心问题允许方向性推理；
2. 哪些核心问题只能条件判断；
3. 哪些核心问题只能暂不可判断；
4. 哪些路径逻辑难以继续成立；
5. 哪些反面证据已检查；
6. 哪些其他可能解释仍未排除；
7. 哪些证据冲突会限制 04 结论；
8. 04 的置信度上限；
9. 04 不得外推的对象、时间、地域和结论。

04 必须继承这些限制，不得用整体证据评估支持所有观点。

---

## 12. 质量门槛与返工规则

03 产物必须满足 `00A_高质量产出判别标准.md` 中关于数据与证据准备阶段的要求。为避免只达到流程合格，本阶段生成前必须完成以下检查。

### 12.1 阶段质量状态

建议使用以下质量状态：

| 状态 | 含义 | 处理 |
|---|---|---|
| `draft` | 证据准备尚未完成或未校验 | 不进入 04 |
| `minimum_pass` | 流程和文件完整，可供 04 草稿使用 | 可进入 04 草稿，但不得正式发布 |
| `high_quality_pass` | 通过确定性校验和独立语义审阅 | 可作为正式 03 产物交付 |
| `return_required` | 关键证据、口径、来源或准入问题未解决 | 返工 |
| `stop_with_gap_report` | 当前条件不足以支撑方向性推理 | 输出缺口说明 |

### 12.2 最低准入门槛

达到以下条件，才可标记为 `minimum_pass`：

1. 02 入口校验通过；
2. 运行范围已冻结；
3. 数据需求按核心问题拆解；
4. 来源和取数口径已说明；
5. 快照文件完整；
6. 关键变量覆盖矩阵可复核；
7. 整体证据评估可解释；
8. 04 结论适用范围已给出。

### 12.3 高质量语义门槛

达到以下条件，才可标记为 `high_quality_pass`：

1. 证据准备围绕关键核心问题，而不是主题泛搜；
2. 核心结论证据门槛已单独判断；
3. 来源直接性、独立性、可复核性、时效性和口径均已评价；
4. 反面证据和其他可能解释有实际检查；
5. 证据冲突有处理或降级；
6. `restricted_pass` 明确列出可用核心问题；
7. `incomplete_pass` 明确说明为什么只能暂不可判断；
8. 04 的结论适用范围清楚、可执行；
9. 准备 Markdown 与快照 CSV 一致；
10. 独立语义审阅通过。

### 12.4 不得进入 04 方向性推理的情况

出现以下任一情况，不得进入 04 方向性推理：

1. 关键核心问题证据门槛未满足；
2. 反面证据检查缺失；
3. 来源不可复核；
4. 来源不独立却被当作交叉验证；
5. 口径与 02 核心问题不匹配；
6. 时间窗口不匹配；
7. 证据冲突未处理；
8. 准入范围没有明确列出；
9. 03 无法说明 04 的结论适用范围。

### 12.5 返工路由

| 问题表现 | 处理 |
|---|---|
| 02 路径、核心问题或最低验证条件不清 | 退回 02 |
| 证据需求拆解过宽 | 重做 03 需求拆解 |
| 来源不直接或不可复核 | 更换或补充来源 |
| 证据口径不匹配 | 重新取数或降级 |
| 缺少反面证据检查 | 补充反面证据检索 |
| 覆盖率高但核心观点门槛不足 | 降级或补证据 |
| 准入范围不清 | 重写整体证据评估和 04 结论适用范围 |
| 快照与准备 Markdown 不一致 | 同步修正并重新校验 |

---

## 13. 运行检查清单

每次 03 完成前，至少检查：

1. 是否继承 02 的核心问题和路径；
2. 是否冻结运行范围；
3. 是否按核心问题拆解证据需求；
4. 是否形成核心问题证据门槛表；
5. 是否区分事实、观点、预测和假设；
6. 是否评价来源直接性、独立性、可复核性和时效性；
7. 是否进行反面证据和其他可能解释检查；
8. 是否处理证据冲突；
9. 是否明确单项需求、关键变量、路径和核心问题状态；
10. 是否明确整体证据评估、置信度上限和 04 结论适用范围；
11. 是否说明返工、补充和停止原因。

---

## 14. 一句话原则

03 的好坏不取决于材料数量，而取决于：

> 关键判断有没有取得匹配口径、可复核、经过反面证据检查的足够证据，以及是否清楚告诉 04 只能判断到哪里。
