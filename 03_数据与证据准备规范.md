# 03_数据与证据准备规范

## 1. 文档定位

本文档规范 03 阶段如何把 02 已经形成的研究逻辑和任务本体视图，转化为一次可执行、可复核、可追溯的数据与证据准备过程。

03 的核心任务不是直接判断结论，也不是泛泛收集资料，而是回答四个问题：

1. 本次判断需要哪些现实实例、数据、证据和反证；
2. 这些材料应该从哪些来源取得，使用什么口径；
3. 取得后如何清洗、归一，并映射到 02 的任务本体视图；
4. 本次准备结果是否足以交给 04 进入后续推理。

本阶段只形成两项正式产物：

```text
<核心主题>数据与证据准备-<YYYYMMDD>-<当日序号>.md
<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
```

其中，准备 Markdown 说明“本次需要什么、去哪取、怎么取、怎么处理、是否够用”；快照目录保存本次实际取得的数据、证据、质量状态、缺口和准入结果。

两项产物必须使用同一组任务标识、运行标识、冻结范围、数据截止时点和来源版本，不得拆开流转。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

03 负责把 02 提出的判断需求变成可执行的数据与证据准备方案，主要包括：

1. 冻结本次运行的时间范围、对象范围、来源范围、口径范围和数据截止时点；
2. 将 02 的实例需求、证据需求、反证要求和路径节点拆解为具体数据需求；
3. 为每项需求明确首选来源、交叉验证来源、替代来源和取数口径；
4. 记录实际获取过程，包括来源、发布时间、业务时间、查询条件、失败原因和停止原因；
5. 对取得材料进行清洗、去重、身份归一、口径转换和时间对齐；
6. 将材料映射为对象实例、关系实例、事件、状态观测、信号、市场预期、证据记录和反证记录；
7. 判断每项需求、每个状态变量锚点、每条路径是否具备后续推理所需的最低输入；
8. 输出本次证据包的可信程度、准入状态、可使用范围和后续推理置信度上限。

### 2.2 本阶段不负责

03 不做以下事情：

1. 不修改 02 已经确定的研究问题、判断路径、对象范围和本体结构；
2. 不为了“完整”而抓取与当前判断无关的全部新闻、全部公司或全部历史数据；
3. 不把搜索结果、网页摘要、模型记忆或二手转述直接当作证据；
4. 不把预测、观点、假设、指引和已发生事实混为一类；
5. 不执行 02 中的推理规则，也不形成方向性投研结论；
6. 不因主源不可用而静默更换数据口径；
7. 不用来源数量、综合分数或材料篇数替代关键质量门槛；
8. 不在运行产物中保存账号、密钥、Cookie 等敏感凭据。

---

## 3. 入口校验与运行冻结

### 3.1 入口校验

03 开始前，应先确认 02 产物是否足以进入数据与证据准备。

入口校验只判断“是否可以执行 03”，不重新设计研究框架，也不重新判断本体结构是否合理。

至少确认以下事项：

1. 已收到配对的研究逻辑 Markdown 和任务本体视图 YAML；
2. 两份文件对应同一研究问题、同一对象范围和同一判断任务；
3. 任务本体视图已经列出关键路径节点；
4. 每个关键路径节点已经说明所需的实例、观测、证据或反证；
5. 如使用任务候选结构或存在本体缺口，相关限制已经传递到 03；
6. 本阶段能够区分哪些问题属于实例缺口、数据缺口、证据缺口或反证缺口。

处理规则如下：

* 如果缺少的是现实公司、产品、关系、事件、数值、来源或材料，由 03 继续准备；
* 如果研究路径、本体承接、对象拆分或判断边界本身不清楚，应退回 02；
* 如果研究问题和对象边界仍有关键歧义，应退回 01。

03 不得通过临时搜索资料自行补造判断结构。

### 3.2 运行冻结

入口校验通过后，应冻结本次运行范围。

每次运行至少冻结以下内容：

| 冻结项    | 说明                             |
| ------ | ------------------------------ |
| 任务标识   | 对应本次研究问题或判断任务                  |
| 运行标识   | 对应本次数据与证据准备过程                  |
| 数据截止时点 | 明确本次材料统计到什么时候                  |
| 时间范围   | 将“当前”“近半年”“事件后”等表达转为具体时间       |
| 对象范围   | 明确纳入和排除的公司、产品、材料、资产、地区或时间段     |
| 来源范围   | 明确本次允许使用的数据源、公开材料或内部材料         |
| 口径范围   | 明确产品规格、币种、单位、地域、价格类型、产能类型等关键口径 |

同一次运行中，准备 Markdown、快照目录和后续 04 推理必须使用同一套冻结范围。

如后续需要扩大对象、改变时间范围或替换关键口径，应生成新的运行版本，不得覆盖旧快照。

### 3.3 候选结构的处理

如果 02 使用了任务候选结构，说明部分结构尚未正式进入本体。03 可以继续准备数据、实例和证据，但必须在准备 Markdown 和快照摘要中说明：

1. 哪些需求依赖候选结构；
2. 候选结构的使用边界；
3. 可能影响哪些路径节点；
4. 后续推理是否需要降级。

只要关键路径依赖任务候选结构，即使数据和证据充分，本次包级准入原则上也不应高于 `restricted_pass`。

---

## 4. 数据需求拆解原则

### 4.1 按判断路径拆解，不按主题泛搜

03 不应围绕宽泛主题搜索资料，而应从 02 的判断路径出发，把每个关键节点拆成可执行的数据需求。

每项数据需求至少说明：

| 维度   | 说明                       |
| ---- | ------------------------ |
| 需求目的 | 用来支持、削弱、阻断或验证哪个判断节点      |
| 本体目标 | 对应对象、关系、事件、状态变量、观测、信号或证据 |
| 最低字段 | 身份、数值、单位、时间、范围、来源等不可缺字段  |
| 时间要求 | 业务时间、发布时间、数据频率、截面或时间序列要求 |
| 口径要求 | 产品规格、地域、币种、价格类型、产能类型等    |
| 覆盖要求 | 需要覆盖哪些对象、时间点、来源或反证条件     |
| 质量门槛 | 可用于后续推理所需满足的最低条件         |
| 路径绑定 | 对应路径、节点或判断问题             |
| 优先级  | 关键、必要或补充                 |

### 4.2 区分四类材料

03 必须区分四类材料：

| 类型   | 含义                              |
| ---- | ------------------------------- |
| 原始材料 | 接口响应、表格、公告、财报、网页、访谈纪要等尚未处理的输入   |
| 语义实例 | 公司、产品、材料、环节、设施、资产及稳定关系          |
| 推理输入 | 有业务时间的事件、状态绑定、观测、信号或市场预期        |
| 证据记录 | 来源文档、最小主张、归一事实候选及其支持、削弱、阻断或冲突作用 |

结构化数据不因来自数据库就自动成为事实。文本材料也不因来自一手来源就自动满足目标口径。所有材料都必须经过来源确认、范围对齐、口径检查和质量判断。

### 4.3 最小充分原则

03 只准备回答当前问题所需的最小充分集合。

准备顺序如下：

1. 先满足关键节点的身份、关系和硬性证据要求；
2. 再满足支持证据和反证的最低交叉验证要求；
3. 再补充能够显著降低冲突或范围不确定性的材料；
4. 达到最低门槛，且新增信息不会改变节点状态时，应停止补充；
5. 补充性材料不得阻塞整体准入。

“没有找到”只表示在当前权限、来源和检索范围内缺失，不表示事实本身不存在。

---

## 5. 数据来源与取数方式

### 5.1 来源使用原则

03 应优先使用已知、稳定、可复核的来源，而不是每次重新设计来源体系。

来源选择原则如下：

1. 政策、监管、财务和已披露经营事实，优先官方原文、交易所文件和公司正式披露；
2. 订单、交付、良率、产能和供应关系，优先一手经营记录；无权限时，用公司披露和独立产业来源交叉验证；
3. 行业规模、设备、材料、产能和价格统计，优先方法论明确、口径稳定的行业数据源；
4. 市场价格、估值和一致预期，优先合法授权且口径可复现的市场数据源；
5. 预测、渠道调研和产业判断，应保留其预测或观点性质，不得替代已发生事实；
6. 搜索引擎只用于发现来源，不应直接作为证据来源。

### 5.2 取数方式

对于重复出现的数据需求，可以沉淀为取数方式或取数配方。当前阶段不要求工程化建设完整配方库，但每次运行应说清楚：

1. 首选来源是什么；
2. 交叉验证来源是什么；
3. 替代来源是什么；
4. 采用什么查询条件、时间范围和对象范围；
5. 关键字段如何解释；
6. 口径如何统一；
7. 获取失败时如何处理；
8. 何时停止继续补充。

主源不可用时，可以使用替代来源，但必须说明替代来源与主源之间的口径差异。

不得用新闻转述、二手摘要或模型生成内容伪装成同等质量的数据来源。

---

## 6. 执行流程

03 按以下顺序执行：

1. 读取并校验 02 产物；
2. 冻结运行标识、时间范围、对象范围、数据截止时点、来源范围和口径范围；
3. 将判断路径拆解为数据、实例、证据和反证需求；
4. 按关键、必要、补充的优先级排序；
5. 为每项需求匹配首选来源、交叉验证来源和替代来源；
6. 按约定口径获取材料；
7. 记录每次获取尝试、参数、结果、失败原因和停止原因；
8. 对材料进行清洗、去重、身份归一、口径转换和时间对齐；
9. 将材料映射为对象实例、关系实例、推理输入和证据记录；
10. 按“状态变量 × 锚点实例”形成覆盖矩阵；
11. 检查支持证据、反证、竞争解释和来源冲突；
12. 判断每项需求、每个状态变量锚点和每条路径的就绪状态；
13. 形成准备 Markdown 和数据证据快照；
14. 输出包级准入结论。

同一来源在同一参数和同一口径下，应产生可解释的一致结果。来源发生修订时，应保留获取时间、业务时间、版本说明和内容指纹，不得覆盖历史记录。

---

## 7. 处理、归一与本体映射

### 7.1 留痕要求

每条进入快照的记录，至少应能回溯以下链路：

```text
来源
→ 原始材料或原始字段
→ 转换规则和口径说明
→ 标准化记录
→ 本体目标
→ 判断路径或节点
```

关键转换不得只写“已清洗”。涉及单位换算、币种转换、分类映射、时间聚合、缺失值处理或异常值处理时，必须说明处理规则。

### 7.2 身份和关系归一

对象和关系归一应遵守以下原则：

1. 公司优先使用监管标识、交易标识、法定名称和可核验别名；
2. 公司与证券应区分，不得把上市证券直接等同于经营主体；
3. 产品应保留代际、规格、容量、工艺或应用边界；
4. 关系必须有方向、作用范围、业务时间和证据；
5. 同名异物、并购更名、代码变更和跨市场证券必须显式处理；
6. 不得因新闻中共同出现、概念相近或文本相邻，就自动建立业务关系。

`semantic_instances.csv` 的 `instance_type` 使用 02 引用的正式本体类型，`requirement_id` 使用对应的对象实例需求引用，不另增同义列。

状态变量允许锚定哪些实例，由 `state_variable_coverage.csv` 反向展示。

### 7.3 数值和时间归一

数值观测至少保留：

1. 原值；
2. 原单位；
3. 标准值；
4. 标准单位；
5. 币种；
6. 业务时间；
7. 发布时间；
8. 获取时间；
9. 口径说明；
10. 来源说明。

以下口径不得未经说明混用：

1. 报价、合同价、成交价和实现价格；
2. 名义产能、有效产能、良率折算产能和认证产能；
3. 出货、销售、消耗、库存和订单；
4. 自然日、交易日、财报期和滚动窗口；
5. 预测、指引、估算和实际值。

### 7.4 证据归一

文本或材料类证据按以下顺序处理：

```text
来源文档
→ 最小主张
→ 归一事实候选
```

每条证据至少说明：

1. 来源文档或材料位置；
2. 陈述主体；
3. 主张内容；
4. 主张性质：事实、预测、指引、观点或假设；
5. 证据作用：支持、削弱、阻断、冲突或背景；
6. 对应的对象、关系、事件、状态观测或判断节点；
7. 是否存在同源转述、独立来源或冲突来源。

证据不得直接跳过事件、观测或路径节点去证明最终判断。03 中的证据只能说明某个中间节点是否具备可验证输入，不能形成投研结论。

### 7.5 原始材料定位与内容指纹

`source_snapshot.csv` 为每条来源保留两个关键字段：

| 字段                 | 用来回答的问题    | 一般怎么填                                  |
| ------------------ | ---------- | -------------------------------------- |
| `raw_artifact_ref` | 当时依据的原文在哪里 | 填公告、财报、网页、PDF 等原文链接，或本次保存副本在快照目录内的相对位置 |
| `content_hash`     | 这份依据后来有没有变 | 对已经固定下来的原文填写摘要标识，便于日后核对是否被修改或替换        |

填写规则如下：

1. 能保存原文副本时，应在 `raw_artifact_ref` 中写清位置，并尽量填写 `content_hash`；
2. 只能引用在线原文、且本次未保存副本时，`raw_artifact_ref` 可写 URL，`content_hash` 可留空，但必须在获取记录和快照摘要中说明原因；
3. 来源暂时无法打开、需用替代材料时，应记录获取结果和替代依据，不得把未实际取得的材料写成已冻结；
4. 不得伪造内容指纹；
5. 来源发生修订时，应保留旧记录，不得覆盖历史条目。

---

## 8. 质量判断与准入

### 8.1 质量判断分四层

03 不使用总分替代硬性门槛。质量判断分为四层：

| 层级  | 核心问题                     |
| --- | ------------------------ |
| 来源层 | 来源是否有权掌握该信息，能否合法访问并定位原文  |
| 记录层 | 字段、时间、口径、范围、转换和目标映射是否完整  |
| 需求层 | 单项需求是否达到最低覆盖，支持证据和反证是否满足 |
| 路径层 | 判断路径是否具备进入 04 的可用输入      |

任何关键硬条件不满足，都不能由其他维度的高分抵消。

### 8.2 单项需求状态

每项需求应给出以下状态之一：

| 状态          | 含义                      |
| ----------- | ----------------------- |
| `covered`   | 最低数据、证据和反证要求均满足         |
| `partial`   | 仅部分范围、部分对象或较弱来源满足，可受限使用 |
| `missing`   | 关键数据、证据或引用缺失            |
| `contested` | 高质量支持与反证冲突，暂无法判断        |
| `blocked`   | 关键前提被有效反证阻断，相关路径不得正向推进  |

`blocked` 可能来自高质量反证，不等于数据质量失败。有充分反证的单元应作为直接证据记录，不得当作材料缺失。

### 8.3 状态变量证据覆盖率

包级准入首先按“状态变量 × 本次必需锚点实例”计算证据覆盖率，不得只按唯一状态变量名称汇总。

例如，同一个状态变量在 HBM、DRAM、NAND、不同公司或不同设施上的覆盖，应分别计数。任何一个本次必需锚点缺失，都必须保留在分母中。

计算公式为：

```text
证据覆盖率 = evidence_backed_unit_count / coverage_unit_total
```

其中：

1. `coverage_unit_total` 为 `state_variable_coverage.csv` 中本次必需的变量—锚点记录数；
2. `evidence_backed_unit_count` 为证据链可用、可以计入覆盖率的变量—锚点记录数；
3. `not_applicable` 只有在 02 已明确该锚点不属于冻结范围，或有可追溯的前序路径停止依据时，才可排除分母；
4. 搜索不到、权限不足、未来事实尚未发生，不能作为排除分母的理由。

一个单元只有同时满足以下条件，才能计为 `counted`：

1. 映射关系有效：`mapping_status` 为 `aligned`；如使用 `proxy`，必须有明确代理理由和缺口记录；
2. 输入和证据都存在：`linked_input_ids` 与 `linked_evidence_ids` 均非空；
3. 证据确实支撑该输入：证据的 `grounds_input_ids` 必须指向对应输入；
4. 引用链可解析：输入、证据、来源、锚点、状态绑定和观测需求引用均能追溯；
5. 至少一条证据可用，且来源可定位，时间和范围至少部分匹配；
6. 覆盖状态为 `covered`、`partial`、`contested`，或由实质反证支撑的 `blocked`；
7. `partial` 或 `proxy` 只能在允许方向性使用时计入；纯背景材料不计入；
8. 由实质反证导致的 `contested` 或 `blocked` 可以计入，但必须保留冲突或阻断证据。

以下情况一律不计入覆盖率：

1. `missing`；
2. 无输入或无证据；
3. 仅有缺口说明；
4. 仅有搜索摘要；
5. 引用悬空；
6. 映射无效；
7. 只有背景材料；
8. 证据不能指向具体推理输入。

合格的 `not_applicable` 写为 `excluded`，并在原因中说明来自 02 范围还是前序路径停止依据。

覆盖率门槛比较使用未四舍五入的原始分数；对外展示时可保留两位小数。

### 8.4 包级准入状态

03 最终输出四类准入状态：

| 准入状态              | 硬门槛                                                               | 后续处理                                                |
| ----------------- | ----------------------------------------------------------------- | --------------------------------------------------- |
| `normal_pass`     | 证据覆盖率为 100%；关键单元 profile 最低要求满足；所有关键节点输入就绪；不存在关键引用、映射、口径或追溯错误     | 可在快照覆盖范围内执行完整路径验证，但不预设业务结论                          |
| `restricted_pass` | 证据覆盖率不低于 95%，但未达到 `normal_pass`；每个关键节点至少有一个 `counted` 单元          | 只能对 counted 单元和允许范围执行受限验证，置信度最高为中                   |
| `incomplete_pass` | 证据覆盖率不低于 80%且低于 95%；每个关键节点至少有一个 `counted` 单元；首选源、交叉源和替代源均已真实尝试并留痕 | 只对 counted 单元执行低置信度验证；未计入单元在 04 中保持 blocked；置信度上限为低 |
| `failed`          | 证据覆盖率低于 80%；任一关键节点没有 counted 单元；或存在关键引用、映射、口径、追溯错误                | 不进入方向性推理，只输出缺口、失败原因和补数建议                            |

补充规则：

1. 证据覆盖率低于 95%，且来源层级尚未耗尽时，不得提前冻结 `restricted_pass`、`incomplete_pass` 或 `failed`；
2. 只有覆盖率达到 95%，或全部适用来源层级已经真实尝试后，才能冻结最终准入；
3. `normal_pass` 只表示材料足以进入推理，不表示判断成立；
4. 如果 02 使用的是候选结构，本次包级准入最高只能为 `restricted_pass`。

### 8.5 证据包可信程度

除准入状态外，03 还应给出整体 `evidence_quality_level`：

| 等级       | 含义                                                     |
| -------- | ------------------------------------------------------ |
| `high`   | 关键需求达到最低覆盖，来源可追溯，时间和范围匹配，关键证据完成独立交叉验证和反证检查，且没有未解决的关键冲突 |
| `medium` | 关键路径可以使用，但部分对象、时间、来源或交叉验证存在明确限制                        |
| `low`    | 关键证据缺失、不可追溯、范围不匹配或存在未解决的重大冲突，不能支撑稳定推理                  |

`evidence_quality_level` 评价的是本次证据包本身是否可靠、完整和可追溯，不表示研究结论成立，也不表示某个方向发生的概率。

`confidence_ceiling` 表示 04 后续推理结论允许达到的最高置信度。它应结合证据包可信程度、准入状态和关键限制确定，并在快照摘要中说明为什么不能更高。

---

## 9. 单次运行产物

### 9.1 产物组成

03 每次运行只形成一组配对产物：

```text
<核心主题>数据与证据准备-<YYYYMMDD>-<当日序号>.md
<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
```

其中：

1. 准备 Markdown 说明本次需要什么、如何取得和处理；
2. 快照目录保存本次实际结果，并通过同名 Markdown 给出整体可信程度和使用范围；
3. 二者必须共享同一套任务标识、运行标识、时间范围、对象范围、来源范围和数据截止时点。

### 9.2 准备 Markdown 应包含

准备 Markdown 至少包括：

1. 本次任务信息；
2. 02 研究逻辑和任务本体视图的可解析 ID 与文件引用；
3. 数据、实例、证据和反证需求清单；
4. 来源和取数方式说明；
5. 实际取得的材料摘要；
6. 清洗、归一和映射规则；
7. 支持证据、反证和冲突情况；
8. 每个状态变量锚点、每项需求和路径节点的质量状态；
9. 缺口、限制和风险；
10. 是否进入 04 的准入结论；
11. 关联快照文件索引及 02 本体视图内容指纹。

准备 Markdown 应写清楚本次“需要什么、去哪取、怎么取、怎么处理、结果是否够用”，不应写成泛泛的资料搜索总结。

### 9.3 数据与证据快照

当前快照契约版本为 `1.2.0`。

快照目录固定包含一个同名 Markdown 和 10 张 CSV：

```text
<核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>/
├── <核心主题>数据与证据快照-<YYYYMMDD>-<当日序号>.md
├── manifest.csv
├── source_snapshot.csv
├── acquisition_log.csv
├── semantic_instances.csv
├── semantic_relations.csv
├── reasoning_inputs.csv
├── evidence_records.csv
├── state_variable_coverage.csv
├── path_readiness.csv
└── gaps_and_risks.csv
```

同名 Markdown 是给研究员阅读的快照摘要，至少说明：

1. 整体证据包可信程度；
2. 准入结果、可使用范围和 04 结论置信度上限；
3. 支撑该等级的主要依据，以及为什么不能更高；
4. 关键状态变量和关键需求覆盖情况；
5. 最重要的支持证据、反证和冲突；
6. 未解决缺口、风险和不可使用范围。

CSV 保存可复核的明细记录。摘要中的等级、范围和原因必须能够回到 `manifest.csv`、`state_variable_coverage.csv`、`evidence_records.csv`、`path_readiness.csv` 和 `gaps_and_risks.csv`，不得另造一套结论。

### 9.4 快照文件的分工

各 CSV 的分工如下：

| 文件                            | 主要作用                                    |
| ----------------------------- | --------------------------------------- |
| `manifest.csv`                | 保存任务、运行、冻结范围、02 引用、准入状态、覆盖率和证据包可信程度     |
| `source_snapshot.csv`         | 保存来源级留痕，包括来源位置、发布时间、获取时间、原文定位和内容指纹      |
| `acquisition_log.csv`         | 保存每次获取尝试、查询条件、结果、失败原因和停止原因              |
| `semantic_instances.csv`      | 保存公司、产品、材料、设施、资产等对象实例                   |
| `semantic_relations.csv`      | 保存对象之间的稳定关系及其方向、范围、时间和证据                |
| `reasoning_inputs.csv`        | 保存事件、观测、信号、市场预期等可供 04 使用的推理输入           |
| `evidence_records.csv`        | 保存来源主张、归一事实候选及其支持、削弱、阻断或冲突作用            |
| `state_variable_coverage.csv` | 按“状态变量 × 锚点实例”检查覆盖、引用、profile 要求和推理准入边界 |
| `path_readiness.csv`          | 记录每个路径节点的输入是否就绪，不判断路径是否成立               |
| `gaps_and_risks.csv`          | 保存缺口、限制、冲突、风险及其对后续推理的影响                 |

为保证 03 与 04 可以直接衔接：

1. `manifest.csv` 必须同时保存 `source_02_logic_id/ref`、`source_02_view_id/ref` 和 02 YAML 原始字节的 `source_02_view_hash`；
2. 02 本体视图引用的正式本体版本继续由 02 管理，03 不复制或改写规则定义；
3. `path_readiness.csv` 的 `readiness_status` 只表示节点的实例、观测、证据和反证是否准备就绪，不表示路径已经成立；
4. `gaps_and_risks.csv` 必须说明缺口影响哪个节点，是阻断后续推理，还是只限制强度、范围或持续时间；
5. 03 不预先替 04 应用传导规则，不生成方向性路径结论。

---

## 10. 状态变量覆盖矩阵

### 10.1 矩阵粒度

`state_variable_coverage.csv` 采用“状态变量 × 锚点实例”粒度。

同一变量在 HBM、DRAM、NAND、不同公司或不同设施上的覆盖不得合并为一行。尚未取得锚点实例时，也必须保留一条锚点为空、`coverage_status=missing` 的记录。

覆盖矩阵只回答五个问题：

1. 02 引用是否正确；
2. 材料实际覆盖到哪里；
3. profile 最低要求是否满足；
4. 该单元是否计入包级证据覆盖率；
5. 04 最多可以如何使用。

覆盖矩阵不记录变量方向，也不判断传导路径成立。

### 10.2 关键字段规则

覆盖矩阵至少使用以下状态字段：

| 字段                     | 允许值                                                                          | 含义                         |
| ---------------------- | ---------------------------------------------------------------------------- | -------------------------- |
| `mapping_status`       | `aligned`、`proxy`、`misclassified`、`invalid_reference`、`anchor_type_mismatch` | 判断状态变量与锚点实例的映射是否有效         |
| `profile_minimum_met`  | `met`、`partial`、`not_met`、`not_applicable`                                   | 判断 02 规定的最低证据 profile 是否满足 |
| `coverage_status`      | `covered`、`partial`、`missing`、`contested`、`blocked`、`not_applicable`         | 判断该变量—锚点单元的材料覆盖状态          |
| `inference_allowed`    | `full`、`directional`、`background_only`、`blocked`                             | 判断 04 对该单元最多可如何使用          |
| `evidence_gate_status` | `counted`、`not_counted`、`excluded`                                           | 判断该单元是否计入包级证据覆盖率           |

填写规则如下：

1. `misclassified` 只描述映射错误，不得混入覆盖状态；
2. `observation_count` 只统计直接 Observation，Event 和 Signal 不得冒充观测；
3. `quantitative_count` 只统计标准单位非空且不为 `qualitative` 的记录；
4. `counter_evidence_count` 统计 `evidence_role=weaken|block` 的证据；
5. `partial`、`missing`、`misclassified`、`invalid_reference` 和 `anchor_type_mismatch` 必须关联 `gaps_and_risks.csv`；
6. profile 最低要求按同一变量在本次任务范围内的全部相关锚点共同检查；
7. 单个锚点仍须按自身覆盖状态限制使用；
8. `evidence_gate_reason` 必须说明计入链路、未计入原因，或 `not_applicable` 的排除依据，不得只写“已检查”。

### 10.3 与推理输入、证据记录的关系

写入 `reasoning_inputs.csv` 时，以下引用必须可回到 02：

1. `state_binding_ref`；
2. `observation_requirement_ref`；
3. `evidence_profile_refs`；
4. 锚点实例类型；
5. 支持的状态变量列表。

写入 `evidence_records.csv` 时，证据只能通过 `grounds_input_ids` 指向 Observation、Event、MarketExpectation 或 Signal，不得直接指向 Judgment。

`evidence_records.csv` 不新增综合评分字段。`statement_nature` 以及 `source_authority`、`traceability`、`independence`、`time_relevance`、`scope_match` 必须使用正式 `evidence.yaml` 的枚举。

`evidence_role` 取值限定为：

```text
source
support
weaken
block
```

不得用 `high/medium/low` 替代这些维度各自的正式取值。

---

## 11. 补充、停止与反馈

### 11.1 补充规则

每轮补充只针对明确缺口执行，不做泛化搜索。

补充顺序固定为：

```text
官方或经营主源
→ 独立交叉源
→ 口径可解释的替代源
```

覆盖率未达到 95% 时，应按未计入单元和关键节点影响排序继续补充。不得通过删除锚点、合并产品、缩小已冻结范围、重复复用宽泛主张来提高覆盖率。

常见缺口与处理方式如下：

| 缺口类型  | 处理方式                    |
| ----- | ----------------------- |
| 身份不清  | 补充权威名录、法定披露或稳定标识        |
| 关系未知  | 补充合同、认证、供应关系、业务披露或第三方验证 |
| 数值缺口  | 检查时间范围、频率、修订、口径和替代来源    |
| 口径不匹配 | 缩小范围或寻找同口径来源，不强行合并      |
| 反证不足  | 按节点终止条件进行定向查询           |
| 来源冲突  | 回到更接近原始事实的来源，检查业务时间和定义  |
| 权限不足  | 使用已说明限制的公开代理指标，或显式降级    |

### 11.2 单项停止规则

满足以下任一条件时，可以停止对应需求的补充：

1. 已达到最低覆盖，且新增信息不会改变节点状态；
2. 已按约定尝试首选来源、交叉验证来源和替代来源；
3. 当前权限、授权、时间窗口或客观可得性无法补齐；
4. 已有高质量反证阻断该节点，继续补充支持材料不会改变可用范围；
5. 继续获取的成本明显超过该非关键数据对判断的增量价值。

每次停止都必须记录停止原因，不能只写“搜索完成”。

### 11.3 包级停止规则

包级停止还必须满足以下规则：

1. 覆盖率达到 95% 时，`search_status=threshold_met`，可以冻结 `restricted_pass`，或在更高门槛满足时冻结 `normal_pass`；
2. 覆盖率低于 95% 时，只有每个未计入关键单元的首选源、交叉源和替代源均已实际尝试，才能写 `search_status=source_tiers_exhausted`；
3. `source_tiers_exhausted` 后，覆盖率为 80%—95% 时只能冻结 `incomplete_pass`；
4. 覆盖率低于 80%，或关键节点没有 counted 单元时，冻结 `failed`；
5. 未达到 95% 且来源层级未耗尽时，`search_status=in_progress`，不得形成最终准入快照。

`future_fact_not_yet_observable` 只能说明当前为什么无法满足 profile。若来源明确证明设施尚处建设、样品或认证阶段，可以作为当前阶段的直接证据；但不得伪造未来良率、量产或交付事实。

### 11.4 反馈规则

运行结束后，03 可以形成两类反馈：

1. 来源反馈：新发现的有效来源、失效来源、访问变化、字段变化和质量变化；
2. 需求反馈：02 提出的数据需求是否过宽、过窄、不清晰或不可获得。

这些反馈只作为后续维护建议，不自动修改正式本体、长期来源库或取数方式。

---

## 12. 与 04 推理阶段的接口

04 必须通过 `manifest.csv` 的 02 引用，以及 02 本体视图中的 `task_context.source_document`，读取同一任务链上的材料：

1. 01 投研需求说明：确认原始需求、研究对象、问题边界和输出重点；
2. 02 研究逻辑 Markdown：理解核心矛盾、主路径和竞争解释；
3. 02 任务本体视图 YAML：取得本次选择的状态变量、传导模板、规则和正式本体引用；
4. 03 数据与证据准备 Markdown：理解本次需求、来源、口径、失败情况和证据不足；
5. 快照目录中的同名摘要 Markdown；
6. `manifest.csv`、`state_variable_coverage.csv`、`path_readiness.csv` 和 `gaps_and_risks.csv`；
7. 按变量和节点需要读取的其他快照 CSV。

01 提供“需要回答什么”；02 提供“按什么逻辑、结构和规则推理”；03 提供“本次有哪些现实输入可以使用，以及证据存在哪些不足”。

04 不得只读 03 后凭通用常识补出路径，也不得把 03 的节点就绪状态直接当作推理结果。

04 应按准入状态处理：

```text
normal_pass
→ 在快照覆盖范围内执行完整路径验证

restricted_pass
→ 只在允许范围内执行受限验证，并继承限制与置信度上限

incomplete_pass
→ 只读取 evidence_gate_status=counted 的变量—锚点单元；
   其余单元保持 blocked；
   整体置信度上限为低

failed
→ 不形成方向性投研判断；
   只输出不可推理原因和补数建议
```

04 不得绕过 03 使用临时搜索材料，不得在推理阶段补造实例，也不得改变 03 已经冻结的取数口径。

如果 04 发现关键记录错误、来源引用断裂，或快照与准备 Markdown 不一致，必须退回 03 重新生成快照版本，不得原地修改已经冻结的快照。

---

## 13. 运行检查清单

03 完成前，应检查以下事项：

1. 02 产物可解析，且允许进入数据与证据准备；
2. 本次运行标识、时间范围、对象范围、来源范围、口径范围和数据截止时点已经冻结；
3. 数据需求按判断路径和本体目标拆解，而不是按主题泛搜；
4. 每项关键需求均已明确来源、口径、质量门槛和停止条件；
5. 获取尝试、失败原因、来源定位和停止原因已经记录；
6. 原始材料、语义实例、推理输入和证据记录已经区分；
7. 价格、产能、订单、预测和事件状态没有混用口径；
8. 支持证据、反证、竞争解释和来源冲突均已检查；
9. 准备 Markdown、快照摘要和各 CSV 共享同一套运行信息；
10. `source_02_logic_id/ref` 与 `source_02_view_id/ref` 均可解析；
11. `source_02_view_hash` 与实际读取的 02 YAML 原始字节一致；
12. 02 选中的每个状态变量均有覆盖记录；
13. 状态绑定、观测需求、锚点类型和 profile 引用一致；
14. 所有部分、缺失或错误映射均关联缺口；
15. 覆盖统计与推理输入、证据记录一致；
16. 关键记录能够回溯到来源、转换规则、本体目标和判断节点；
17. `path_readiness.csv` 只记录输入就绪状态，没有预先形成路径结论；
18. 未用需求占位符冒充实际取得的实例或证据；
19. 证据包可信程度、准入状态和置信度上限与变量覆盖、需求覆盖、路径状态和缺口风险一致；
20. 快照摘要已经说明为什么可信、为什么不能更高以及可以使用的范围；
21. 已按变量—锚点重算 `coverage_unit_total`、`evidence_backed_unit_count` 和证据覆盖率；
22. 低于 95% 时已核对首选源、交叉源、替代源的尝试记录；
23. 来源层级未耗尽时，没有提前冻结最终准入；
24. `normal_pass`、`restricted_pass`、`incomplete_pass`、`failed` 与 100%/95%/80% 门槛、关键节点门槛和搜索状态一致；
25. 本阶段未形成投研结论或买卖建议。

---

## 14. 一句话原则

03 的核心原则是：

把 02 提出的判断结构，转化为一组可复核的数据、实例、证据和反证准备结果，并判断这些材料是否足够交给 04 使用。

03 不是搜索资料，也不是直接推理，而是为可解释、可验证、可反证、可追溯的投研判断准备输入。
