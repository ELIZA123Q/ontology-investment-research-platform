schema_name: events_reasoning
schema_version: 0.2.0
namespace: investment_ontology
status: deprecated
depends_on:
  actions_schema: actions_reasoning.yaml
canonical_rule: >
  动作后续信号契约已迁入 actions_reasoning.yaml 的 side_effects(type=trigger_followup)。
  外部研究事件仍使用 objects_reasoning.yaml 的 Event 对象；ActionFollowup 由 RecordActionFollowup 落库。
migrated_to: schemas/判断推理域/actions_reasoning.yaml
migration_note: ActionFollowup 是 Action side effect，不是独立 schema 桶。
