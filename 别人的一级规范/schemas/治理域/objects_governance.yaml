schema_name: objects_governance
schema_version: 0.1.0
namespace: investment_ontology
status: draft
depends_on: ../common.yaml
canonical_rule: YAML 是正式机器约束；Markdown 只负责解释和示例。
description: 投研治理域对象类型定义——补丁管理（PatchRequest/PatchLedgerItem）、推理运行与轨迹（InferenceRun/RuleApplication/ReasoningStep）、审计与审核（ActionAudit/ReviewRecord）。公共枚举和域映射见 common.yaml。
relation_direction_note: optional_relations 中的 sourceOf 表示该对象允许被 SourceDocument -> object 的来源关系追溯；不表示该对象作为 sourceOf 的 source。

# ============================================================
# 域四：治理域 — 变更 + 审核 + 推理运行 + 审计
# ============================================================

schema_parts:
  - schemas/治理域/objects_governance_patch.yaml
  - schemas/治理域/objects_governance_logic.yaml
  - schemas/治理域/objects_governance_run_audit.yaml
object_types: {}
