schema_name: events_evidence
schema_version: 0.2.0
namespace: investment_ontology
status: deprecated
depends_on:
  actions_schema: actions_evidence.yaml
canonical_rule: >
  动作后续信号契约已迁入 actions_evidence.yaml 的 side_effects(type=trigger_followup)。
  ActionFollowup 仍作为治理域对象，由 RecordActionFollowup 落库；本文件不再维护事件清单。
migrated_to: schemas/证据域/actions_evidence.yaml
migration_note: ActionFollowup 是 Action side effect，不是独立 schema 桶。
