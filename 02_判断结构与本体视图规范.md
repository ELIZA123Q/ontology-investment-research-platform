# 02_判断结构与本体视图规范

## 1. 文档定位

本文档规范 02 阶段如何在 01 已经明确研究任务之后，独立搭建本次研究的判断结构，并把这套结构转化为 03 可以据此准备实例、观测、证据和反证的任务本体视图。

02 的核心目标不是把 01 的问题扩写成一篇行业分析，也不是把框架库内容拼贴成模板化结构，而是回答五个问题：

1. 本次真正要辨别的核心矛盾是什么；
2. 总问题应拆成哪些最小可判断单元，为什么按这个顺序判断；
3. 主路径、反证和竞争解释分别是什么，哪些信号能够区分它们；
4. 正式本体是否能够承接这套判断结构，并把验证需求交给 03；
5. 哪些必要本体项只是上位覆盖、弱覆盖或缺失，会导致 03 无法收证据、04 无法推理或 05 无法表达。

02 与相邻阶段的关系是：

```text
01 提供已经明确的问题、对象、范围、用途和初步线索
→ 02 形成研究逻辑、判断脊柱、最小问题树、主路径、反证和竞争解释
→ 02 将判断结构映射为任务本体视图
→ 02 强制完成本体缺口扫描，区分直接覆盖、上位覆盖、弱覆盖和缺失
→ 03 按路径、变量、实例需求和证据 profile 准备现实输入
```

`aligned` 或“可以进入 03”只表示阶段状态满足交接要求，不代表本体已经完美、证据已经充分或判断已经成立。正式交付时，还必须满足 `00A_高质量产出判别标准.md` 中对 02 的质量要求。

02 不查询现实数据，不创建现实实例，不判断证据是否充分，不形成方向性投研结论。但 02 必须判断当前正式本体是否足以指导 03 收证据、支撑 04 推理，并在进入 03 前完成本体缺口扫描。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

02 负责把 01 的研究任务转化为可执行的判断结构，主要包括：

1. 继承 01 的研究问题、判断落点、对象范围、时间口径、用途和不研究事项；
2. 识别本次判断的核心矛盾，并形成一句清晰的**判断脊柱**；
3. 把总问题拆成最小问题树，说明前置、并列、竞争和停止关系；
4. 确定需要拆分的对象、产品、环节、地区或公司差异；
5. 组织主路径、削弱条件、阻断条件、反证和竞争解释；
6. 为每个关键判断单元定义最低验证条件、降级规则和停止条件；
7. 按本次核心矛盾选择、裁剪和排除框架库内容，记录主轴框架、辅助框架、场景卡及其使用理由；
8. 选择正式本体中的对象、关系、事件、状态变量、传导模板、推理规则和证据 profile；
9. 生成研究逻辑 Markdown 和任务本体视图 YAML，并保证二者一致；
10. 对本次必要对象、变量、关系、路径、规则、反证条件和证据 profile 做强制缺口扫描；
11. 区分直接覆盖、上位覆盖、弱覆盖、缺失、任务候选和本体缺口，并说明使用限制；
12. 判断本阶段产物是草稿、最低通过，还是达到正式交付质量。

### 2.2 本阶段不负责

02 不做以下事情：

1. 不改变 01 已经确认的研究意图、主要对象、时间窗口或使用目的；
2. 不查询公告、新闻、财报、价格、数据源或其他现实材料；
3. 不创建现实公司、产品、设施、订单、价格、库存等实例；
4. 不判断现实证据是否已经满足；
5. 不用材料摘要替代判断结构；
6. 不输出周期阶段、影响强弱、投资结论或方向性判断；
7. 不为了让本体匹配更容易而压平对象差异；
8. 不把一次性事实、临时表述或材料观点直接写成正式本体定义。

正式数据与证据准备由 03 完成，路径推理和研究员报告由 04 完成。02 只定义“应该如何判断”和“本体如何承接”。

---

## 3. 02 要形成的核心成果

02 的重点不是整理字段，而是形成一套能被 03 和 04 使用的判断结构。完成后应能回答：

1. **判断脊柱**：一句话说清本次最需要辨别的主矛盾和主导机制；
2. **最小问题树**：总问题拆成哪些子问题，哪些是前置、并列、竞争或停止节点；
3. **对象分化**：哪些产品、公司、环节或地区不能合并判断，为什么；
4. **主路径**：最需要验证的传导链是什么；
5. **反证与竞争解释**：哪些信号会削弱、阻断或替代主路径；
6. **最低验证条件**：每个关键判断单元至少需要什么实例、观测、证据和反证；
7. **降级规则**：最低条件不满足时，后续只能形成倾向判断、条件判断、暂不可判断还是路径阻断；
8. **框架使用**：本次参考了哪些主轴框架、辅助框架或场景卡，哪些模块被裁剪，哪些相关框架被排除；
9. **本体承接**：正式本体、任务候选和本体缺口分别承接了哪些内容；
10. **缺口扫描**：每个必要本体项是直接覆盖、上位覆盖、弱覆盖还是缺失，是否能指导 03 收证据、支撑 04 推理；
11. **03 交接**：哪些需求可以直接交给 03 准备数据与证据，哪些只能作为候选补丁或限制条件交接。

只纳入会影响本次判断的内容。仅仅“可能相关”、但不能改变路径或结论强弱的对象和变量，不进入本次研究框架。

---
## 4. 如何承接 01 的研究需求

01 给出的是研究意图和边界，不是现成的分析框架。02 应保留 01 的核心问题，同时重新判断这个问题应如何拆解。

| 01 提供的内容 | 02 需要作出的判断 |
|---------------|-------------------|
| 核心问题 | 真正需要辨别的机制和矛盾是什么 |
| 研究对象 | 是否需要按产品、角色、环节或公司分别讨论 |
| 判断起点 | 应从事件、状态、假设还是历史事实开始 |
| 观察维度 | 哪些变化是原因、过程、结果或验证信号 |
| 关系提示 | 哪些传导需要验证，哪些只是初步设想 |
| 时间与对象范围 | 哪些边界必须保持，哪些内部差异需要进一步拆分 |
| 初步验证和反证方向 | 如何转化为路径节点、支持条件、阻断条件和替代解释 |

02 可以补充严谨分析不可缺少的结构，但不能借“完善框架”改变 01 的问题、主要对象或研究边界。发现这些内容仍有关键歧义时，应退回 01。

01 中的相对时间和动态对象选择应保持原意。02 只说明这些规则会怎样影响研究框架，不把它们固化为文档生成日对应的绝对日期或临时对象名单。

---

## 5. 如何独立搭建研究框架

研究框架应从当前问题本身出发独立形成。框架库可以帮助更快地想到常见机制、证据和反证，但不能代替本次判断，也不能把库中某个模板拆出几项就当作研究框架。

### 5.1 先形成判断脊柱

先判断表面问题背后真正要区分什么。例如，“价格上涨能否持续”可能需要区分：

- 需求真实增长，还是补库、抢单或重复下单；
- 名义产能不足，还是良率、认证、材料或封装限制导致有效供给不足；
- 基本面改善，还是预期、估值和资金推动；
- 总量变化，还是产品结构或客户结构迁移。

只有核心矛盾明确，后面的对象拆分、路径和证据要求才有依据。

### 5.2 再搭最小问题树和分析顺序

把总问题拆成若干能够分别验证的子问题，并说明它们之间的关系：

- 哪些问题是前置条件；
- 哪些问题需要并列比较；
- 哪些问题代表竞争解释；
- 哪个节点失败后，后续传导应停止。

问题树应服务于判断，而不是追求覆盖所有相关主题。每个子问题都应能够被证据增强、削弱或否定。

### 5.3 确定对象、范围和必要差异

对象是否拆分，取决于不同子对象是否具有不同机制、证据口径或反证条件。

例如，研究存储芯片周期时，DRAM、NAND 和 HBM 是否分别分析，应由它们的需求、供给、库存和价格机制是否不同决定；不能因为都属于存储芯片就合并，也不能因为彼此相关就全部展开。

时间、地域、产业链和公司范围同样遵循“足以回答问题”的原则。范围过小会漏掉关键传导，范围过大会稀释核心判断。

### 5.4 组织主路径、反证和替代解释

主路径说明本次最需要验证的传导，例如：

```text
终端需求变化
→ 订单与库存变化
→ 有效供给松紧
→ 价格与交期变化
→ 公司收入和利润影响
```

同时说明：

- 哪些条件会削弱或阻断主路径；
- 是否存在能够解释同一现象的其他路径；
- 不同解释需要用什么信息区分；
- 哪些前置节点不成立时，不能继续向后推导。

反证不是附加清单，而是研究框架的一部分。

### 5.5 明确最低验证条件

对每个关键判断说明：

- 需要确认哪些现实对象和关系；
- 需要观察哪些变化、数值或时点；
- 什么证据会增强判断；
- 什么证据会削弱、阻断或支持替代解释；
- 最低需要看到什么，才值得继续验证下一层路径。

02 只定义这些验证需要，不提前判断现实中是否已经具备。

### 5.6 用框架库检查和完善

独立形成基本研究框架后，可以参考 [`02框架库/README.md`](02框架库/README.md) 及相关框架，检查是否遗漏常见机制、对象差异、传导环节、最低证据或反证条件。

框架库的作用是启发、加速和校验，不是替代本次判断。最终保留什么、删去什么、增加什么，仍由本次问题决定。即使库中没有合适框架，02 也应根据问题本身形成完整的研究逻辑；即使库中存在高度相似的框架，也要重新判断其是否适用于本次对象、范围和目标。

使用框架库时，应特别避免两类错误：

1. **关键词套框架**：看到 HBM、AI、车规、国产替代等词，就直接引用对应框架，而没有先判断本次核心矛盾；
2. **平铺堆框架**：为了显得完整，把所有相关框架都搬入 02，导致问题树变宽、主路径失焦。

### 5.7 框架选择与裁剪规则

框架库经过分层管理后，02 不按“框架数量”组织研究，而按“主判断轴”组织研究。每次任务必须先从 01 已明确的研究问题、对象、时间、地域和产业链边界中，提炼本次判断脊柱，再选择能够支撑该判断脊柱的框架和场景卡。

推荐使用顺序如下：

```text
01 已明确的研究问题与边界
→ 本次主判断轴 / 判断脊柱
→ 1 个主轴框架
→ 0—2 个辅助框架
→ 0—1 张场景卡
→ 必要时引用基础框架检查点
→ 裁剪无关模块
→ 记录排除理由
→ 映射正式本体
```

一般规则如下：

| 层级    | 02 中的作用                                     | 使用要求                            |
| ----- | ------------------------------------------- | ------------------------------- |
| 基础框架  | 提供通用判断机制，如供需、政策、传导、价格、业绩、估值、风险、预期差          | 不必完整展开；只在本次判断需要时引用关键检查点         |
| 行业主框架 | 提供行业化判断机制，如半导体周期、晶圆制造、封装测试、设备、材料、外部约束与供应链韧性 | 可作为主轴框架，也可作为辅助框架；主轴框架原则上只能 1 个  |
| 场景卡   | 承接具体产品、应用、事件或高频问题入口，如 HBM、AI 算力、地缘冲击、国产设备突破 | 用于补充特殊变量、证据、误判和停止条件；不得当作正式框架；正式任务原则上最多 1 张 |
| 案例复盘  | 提供历史误判、反例、滞后窗口和验证经验                         | 只作为校验和启发；若要进入正式本体，必须经过证据归一与任务裁剪 |

这里的“主轴框架”和“辅助框架”是 02 中的使用角色，不是框架库中的固定资产层级。同一个框架在不同任务中可以作为主轴框架，也可以作为辅助框架。选择依据不是框架层级高低，而是它是否最能回答本次研究的核心判断。半导体行业库已取消“适配框架/适配卡”层，产品与事件入口统一由场景卡承接。

例如：

* 若问题核心是“供需是否紧张”，主轴可选基础供需框架；
* 若问题核心是“存储周期是否见顶”，主轴宜选半导体周期框架，并引用存储周期或 HBM 场景卡；
* 若问题核心是“国产设备是否真正突破”，主轴宜选半导体设备框架，外部约束、国产替代与供应链韧性框架作为辅助；
* 若问题核心是“地缘冲击如何传导到半导体”，主轴可选产业链传导框架或外部约束与供应链韧性框架，再按冲击落点补充材料、设备、制造或周期框架。

若某个框架只是“相关”，但不会改变本次主路径、反证、最低验证条件、停止条件或本体映射，不纳入本次 02。

若必须同时调用 3 个以上正式框架，或需要同时调用多张场景卡，通常说明研究问题尚未收敛，应优先回到 01 检查研究范围，或在 02 中重新压缩判断脊柱。02 不应把多个框架简单拼接成百科式问题树。

框架使用后，02 必须说明：

1. 本次主判断轴是什么；
2. 为什么选择该主轴框架；
3. 辅助框架分别补充哪一段判断；
4. 场景卡补充了哪些特殊变量、证据、误判或停止条件；若未使用场景卡，说明原因；
5. 哪些模块实际进入本次问题树、主路径、反证或最低验证条件；
6. 哪些相邻框架被排除，为什么排除；
7. 框架内容与 01 边界不一致时，如何裁剪；
8. 框架中出现但正式本体尚未承接的内容，是任务候选、本体缺口，还是不纳入本次任务。

最终进入 02 的不是“框架全集”，而是围绕本次判断脊柱裁剪后的最小可用结构。框架库只用于帮助识别机制、变量、证据和反证，不得替代研究员对本次问题边界和推理主轴的确认。

---

## 6. 如何形成任务本体视图

研究逻辑讲清“这个问题应该怎样分析”，任务本体视图说明“这套分析如何由本体承接”。二者先后有别：先把研究逻辑想清楚，再匹配本体；二者表达的是同一套判断，但面向不同读者。

### 6.1 两个文件的分工

02 输出两份配对文件：研究逻辑 Markdown 和本体视图 YAML。二者表达同一套判断任务，但用途不同。

研究逻辑 Markdown 面向研究员，负责说明“这个问题应该怎么分析”，包括核心矛盾、问题拆解、对象差异、主路径、反证、替代解释和最低验证需求。

本体视图 YAML 面向 03 和系统处理，负责说明“这套分析如何被本体承接”，包括正式本体引用、框架使用记录、路径节点、实例需求、观测需求、证据要求、反证要求、任务候选和本体缺口。

| 文件            | 负责什么            | 不负责什么                             |
| ------------- | --------------- | --------------------------------- |
| 研究逻辑 Markdown | 讲清研究框架和分析理由     | 不承载完整本体 ID、实例需求 ID、证据要求 ID、机器校验字段 |
| 本体视图 YAML     | 承接本体、路径、实例和证据需求 | 不重复面向研究员的大段分析正文                   |

Markdown 应能独立阅读；YAML 应能直接交给 03 使用。Markdown 中可以保留少量编号用于对齐，但完整技术表达以 YAML 为准。

### 6.2 正式本体引用原则

任务本体视图不是重新定义本体，而是在正式本体中切出本次任务需要的最小结构。

写入 `semantic_view`、`reasoning_view`、`evidence_view` 的对象类型、关系类型、事件类型、状态变量、传导模板、推理规则和证据要求，都必须来自正式本体规范，不得临时编造。

引用顺序如下：

1. 先查[一级通用本体规范/](一级通用本体规范/)；
2. 再查本任务所属领域本体规范（例如半导体任务参考 [二级半导体领域本体规范/](二级半导体领域本体规范/)）；
3. 领域规范是一级规范之上的扩展，二者边界不一致时，以领域规范为准，并在 YAML 中记录来源；
4. 正式规范未覆盖的内容，不得冒充正式本体，应记录为任务候选或本体缺口。

YAML 中应通过 `ontology_sources`、`source_ref`、`source_version` 记录本次引用了哪些规范和定义。

如果缺的是现实公司、产品、事件、数值或材料，交给 03 作为实例、观测或证据需求；如果缺的是稳定的对象类型、关系类型、状态变量、事件类型、规则或证据结构，才记录为本体缺口。

证据要求也必须沿用正式本体。状态变量与证据 profile 的绑定从 `reasoning.yaml` 读取，profile 的支持证据、反证、最低覆盖和降级条件从 `evidence.yaml` 读取。02 负责选择本次需要的 profile，并把 profile 引用和任务特定的最低要求写入 `evidence_view`；03 据此寻找材料和判断质量，不重新设计证据标准。

`evidence.yaml` 不是数据源。一次任务缺少材料或来源时，应交给 03 记录为数据或证据缺口；只有缺少可长期复用的证据 profile、质量规则或正式绑定时，才记录为本体缺口。

任务候选只用于本次研究临时承接尚未正式入库、但又不可缺少的结构。它不得混入正式本体，并应说明使用理由、边界和风险。

### 6.3 本体映射不得用上位概念冒充充分覆盖

02 在匹配正式本体时，不允许仅凭“存在上位对象、上位变量或通用路径”判定已经覆盖。本体承接必须同时通过三项测试：

1. **结构可映射**：能映射到明确对象、状态变量、关系、路径、规则或证据 profile；
2. **证据可收集**：03 能据此设计实例、观测、证据和反证需求；
3. **推理可使用**：04 能据此判断路径成立程度、削弱条件、阻断条件、竞争解释或结论强度。

覆盖状态分为四类：

| 覆盖状态 | 判定标准 | 后续使用 |
|---|---|---|
| `direct_cover` | 已有正式本体项能够直接承接，且能指导 03 收证据、支撑 04 推理 | 可进入正式 03 和 04 |
| `upper_cover` | 只有明确上位类能够承接，但缺少本次问题需要的细分对象、变量、路径或规则 | 可进入 03，但必须记录候选补丁或限制 |
| `weak_cover` | 能做概念描述，但不能形成可执行证据需求或推理规则 | 只能作为观察或缺口说明，不支撑强判断 |
| `missing` | 正式本体没有可用承接项 | 必须记录本体缺口，必要时阻断进入 03 |

例如，研究地缘冲击对半导体的影响时，`SupplyRisk`、`CostImpact`、`ExternalShock` 等上位概念只能提供初步承接。若本次判断依赖航运中断、能源成本、关键材料安全库存、区域产能暴露或军事订单拉动，就必须继续判断这些内容是 `direct_cover`、`upper_cover`、`weak_cover` 还是 `missing`，不能用“风险”“影响”“变化”等泛词替代。

### 6.4 本体视图怎样承接研究逻辑

任务本体视图不是整份本体，也不是研究结论，而是为本次问题切出的一块最小充分结构：

| 研究逻辑 | 本体视图中的表达 |
|----------|------------------|
| 研究对象及其差异 | 对象类型和分类 |
| 框架选择、组合、裁剪与排除 | `framework_usage` |
| 对象之间的作用 | 关系类型及适用边界 |
| 需要观察的变化 | 事件类型和状态变量 |
| 主路径、反证和停止条件 | 传导模板、规则、路径和节点 |
| 验证需要 | 实例需求、证据 profile、证据要求和反证要求 |

进入视图的每项结构都应能说明它表示什么、为什么本次需要、服务于哪一段判断。与本次路径无关的本体内容不进入视图。

YAML 中应单独保留 `framework_usage`，用于记录框架选择与裁剪，不得把框架 ID 直接混入正式本体定义。建议结构如下：

```yaml
framework_usage:
  primary_framework:
    id: ""
    name: ""
    reason: ""
    used_modules: []
    excluded_modules: []
  adapter_frameworks:
    - id: ""
      name: ""
      reason: ""
      used_modules: []
      excluded_modules: []
  scenario_card:
    id: ""
    name: ""
    reason: ""
  excluded_frameworks:
    - id: ""
      reason: ""
  framework_adjustments:
    - source_framework: ""
      adjustment: ""
      reason: ""
```

`framework_usage` 只记录本次如何借用框架库。真正进入 03 的对象、变量、路径、实例和证据需求，仍必须落入后续 `semantic_view`、`reasoning_view`、`instance_requirements` 和 `evidence_view`。

状态变量还必须满足一条完整出口链：

```text
reasoning_view.state_variables
→ candidate_paths.nodes[].ontology_refs.state_variables
→ instance_requirements.state_bindings
→ instance_requirements.observations
→ instance_requirements.evidence_bindings
→ evidence_view.evidence_profiles
```

具体要求如下：

1. 每个选中变量至少被一个路径节点引用，且变量的 `used_by_path_nodes` 与节点反向引用完全一致；
2. 每个选中变量至少有一个 `state_binding`，同一变量可以针对不同对象需求建立多个绑定；
3. 每个状态绑定至少有一个观测需求；
4. 绑定引用的对象需求类型必须属于变量的 `allowed_anchor_types`；
5. 每个状态绑定和观测需求至少被一个 evidence binding 覆盖，所用 profile 必须与变量的 `evidence_profile_ref` 一致；
6. 上述条件统一由 `validation.checks.state_variable_chain_complete` 表示；为 `false` 时不得进入 03。

状态变量可以由 03 准备直接观测、事件或代理信号，但 03 不得由此计算变量方向或执行变量间传导。只有 04 可以在证据准入后应用 02 选定的规则。

本体匹配也会反过来帮助检查研究逻辑：如果一个概念无法稳定定义，可能是对象粒度混乱、关系含义不清，或把一次性事实误当成稳定结构。此时应先澄清逻辑，再决定是否记录本体缺口。

### 6.5 判断单元的取证交接边界

02 的 `judgment_units` 只说明“这个判断是什么类型、需要什么证据角色”，不得把 03 的取证配方、证据篮子完成状态、来源快照或准入评估塞回判断单元。

每个核心判断单元至少交接：

| 字段 | 说明 |
|---|---|
| `judgment_type` | 用于 03 匹配取证策略或配方标签 |
| `required_evidence_roles` | 主证据、交叉验证、反证、阻断条件、代理和背景材料的要求状态 |
| `required_evidence_categories` | 价格、需求、供给、库存、订单、政策、估值、财务、暴露等证据类别 |
| `required_counter_categories` | 必须主动检查的反证和竞争解释类别 |
| `candidate_evidence_recipe_tags` | 推荐 03 优先匹配的取证配方标签，不等同于已执行配方 |
| `minimum_validation_conditions` | 进入 04 对应观点前的最低验证条件 |
| `forbidden_shortcuts` | 禁止使用的偷换证据方式 |
| `proxy_policy` | 是否允许代理指标以及使用边界 |
| `evidence_gap_policy` | 缺证时 04 应如何降级、停止或返工 |

02 的 `evidence_requirements` 只表达“本次必须取得什么证据”。它可以写目标判断单元、路径节点、状态变量、证据角色、证据类别、内容域、对象范围、时间范围、最低质量、来源层级、独立来源数、必需篮子引用、反证篮子引用、代理许可、首选来源画像、允许取数通道、禁用来源、时效、追溯、可比口径、停止条件、缺失策略和 04 降级方式。

完整取证方法属于 03 取证策略库；证据篮子实际满足情况、来源画像快照、取数通道快照、代理指标登记和准入评估属于 03 运行快照。02 不得预填这些运行结果。

### 6.6 两个文件的一致性要求

研究逻辑 Markdown 和本体视图 YAML 必须表达同一个判断任务。
Markdown 中的关键对象、变量、路径、证据和反证，应在 YAML 中找到对应结构；YAML 中的路径、节点、实例需求和证据需求，也应能回到 Markdown 中找到分析依据。
如果任一文件的核心逻辑发生变化，另一份文件必须同步更新。YAML 中不得出现不服务正文判断的孤立结构。Markdown 附录只提供快速定位，不能代替 YAML 的完整技术表达。

---

## 7. 本体缺口强制扫描与处理规则

02 必须把本体缺口扫描作为固定质量门，而不是生成失败时的异常兜底。每次 02 都必须完成缺口扫描；只有扫描通过，才允许标记为 `minimum_pass` 并进入正式 03。

### 7.1 缺口扫描先于本体视图定稿

02 不应先生成一套看似完整的本体视图，再简单判断“是否有缺口”。正确顺序是：

```text
01 研究问题与边界
→ 必要本体项清单
→ 正式本体映射
→ 覆盖状态判定
→ 下游压力测试
→ 本体视图定稿 / 缺口说明 / 退回返工
```

必要本体项至少包括：

1. 本次必须分析的对象、对象类型、对象分层和对象差异；
2. 本次必须观察的状态变量；
3. 影响路径所需的关系、事件类型和传导模板；
4. 关键判断单元所需的推理规则；
5. 削弱、阻断、反证和竞争解释条件；
6. 03 可执行的实例需求、观测需求、证据 profile 和反证 profile；
7. 04 和 05 预期输出所需的判断口径。

### 7.2 缺口扫描矩阵

每次 02 必须在 YAML 或配套扫描文件中形成缺口扫描矩阵。建议字段如下：

| 字段 | 含义 |
|---|---|
| `required_item` | 本次研究最少需要的对象、变量、关系、路径、规则、反证条件或证据 profile |
| `required_by` | 来自 01 的哪个问题、判断目标、路径节点或输出要求 |
| `current_mapping` | 当前正式本体如何承接 |
| `coverage_status` | `direct_cover` / `upper_cover` / `weak_cover` / `missing` |
| `evidence_collectable` | 03 是否能据此收集或标注证据 |
| `reasoning_usable` | 04 是否能据此执行路径判断、反证判断或结论强度判断 |
| `gap_severity` | `none` / `minor` / `major` / `blocking` |
| `needed_action` | 无需补充 / 03 候选补丁 / 02 返工 / 退回 01 / 本体优化 |
| `usage_limit` | 若进入 03 或 04，必须遵守的限制 |

`coverage_status` 不能只写“已覆盖/未覆盖”。尤其要识别 `upper_cover` 和 `weak_cover`，因为大多数本体问题不是完全没有字段，而是有上位壳、无推理承载力。

### 7.3 下游压力测试

每个必要本体项都必须通过下游压力测试。02 不问“文本上有没有写到”，而问：

1. **03 证据压力测试**：03 能否据此设计实例、观测、证据和反证需求？
2. **04 推理压力测试**：04 能否据此判断路径成立、路径削弱、路径阻断或竞争解释？
3. **05 表达压力测试**：05 需要表达的研究结论，能否由 04 在该结构上推出？

只要某项本体承接不能指导 03 收证据，或不能支撑 04 推理，就不能判定为充分覆盖。即使它能被某个上位概念勉强挂接，也只能判定为 `upper_cover` 或 `weak_cover`。

### 7.4 硬触发条件

出现以下任一情况，02 必须触发本体缺口说明，不得只在正文中轻描淡写：

1. 01 的核心研究对象只能映射到泛化对象，无法映射到具体对象或明确上位类；
2. 任一主路径缺少必要状态变量；
3. 任一主路径缺少可执行的实例、观测、证据或反证需求；
4. 任一主路径缺少削弱、阻断或竞争解释条件；
5. 任一关键结论类型找不到对应推理规则或输出口径；
6. 03 必须临时新增对象、变量、关系、路径或证据 profile 才能继续；
7. 04 只能依赖“可能、影响、风险、变化、相关、扰动、因素”等泛化表达完成判断；
8. 关键对象、状态变量、路径、规则或证据 profile 中，`upper_cover`、`weak_cover`、`missing` 合计超过 30%；
9. 出现任一 `blocking` 缺口；
10. 出现会改变判断脊柱、对象范围或输出口径的 `major` 缺口。

硬触发条件优先于模型自评。只要触发，不能把阶段状态写成无缺口通过。

### 7.5 缺口分级与阶段状态

缺口严重度按后续影响分级：

| 严重度 | 判定标准 | 阶段处理 |
|---|---|---|
| `none` | 正式本体直接覆盖，03 可收证据，04 可推理 | 可进入 03 |
| `minor` | 有少量上位覆盖或候选项，但不影响主路径和关键判断 | 可进入 03，但必须传递限制 |
| `major` | 影响部分主路径、关键变量、反证或输出口径 | 原则上返工 02；若继续进入 03，只能作为候选验证任务 |
| `blocking` | 导致 03 无法收证据、04 无法推理，或 01 边界本身无法承接 | 不得进入正式 03，需退回 01、返工 02 或进入本体优化 |

阶段状态建议这样使用：

```yaml
ontology_gap_scan:
  status: no_gap | minor_gap | major_gap | blocking_gap
  can_enter_03: true | false
  limitation_for_03: []
  limitation_for_04: []
```

### 7.6 正式本体不能完全承接时怎么办

没有找到现成定义时，先判断缺的是结构还是现实信息：

| 情形 | 处理方式 |
|------|----------|
| 缺少稳定的对象、关系、变量、事件、规则或证据 profile 定义 | 记录本体缺口；必要时形成任务候选 |
| 名称、角色或范围无法唯一理解 | 先澄清；影响研究意图时退回 01 |
| 已有类型，但缺少现实公司、产品、关系或事件 | 作为实例需求交给 03 |
| 已有状态变量，但缺少现实方向、数值或时间 | 作为观测需求交给 03 |
| 已有证据要求，但尚未取得来源和材料 | 作为证据需求交给 03 |
| 只有上位概念能承接，无法形成证据需求或推理规则 | 判定为 `upper_cover` 或 `weak_cover`，写入缺口扫描 |
| 必须依赖 03 临时新增结构才能继续 | 触发本体缺口说明，并限制 04 不得基于临时结构输出强判断 |

任务候选只用于本次研究临时承接不可缺少、但尚未正式入库的稳定结构。它必须与正式本体分开保存，并说明使用边界和风险。

03 可以基于材料提出候选对象、候选变量、候选路径或候选规则输入，但不得直接把这些内容变成正式本体。04 可以读取候选项作为观察或待验证解释，但不得把未确认候选项作为强结论依据。

### 7.7 缺口说明的生成规则

02 每次都必须完成本体缺口扫描。是否单独生成《02_本体缺口说明》，按以下规则处理：

| 扫描结果 | 是否单独生成缺口说明 | 处理 |
|---|---|---|
| `no_gap` | 不强制 | 在 YAML 中记录扫描通过 |
| `minor_gap` | 可选，但必须在 YAML 中记录 | 可以进入 03，并传递限制 |
| `major_gap` | 必须生成 | 原则上返工 02；如继续，只能进入候选验证 |
| `blocking_gap` | 必须生成 | 不得进入正式 03 |

缺口说明只记录缺失的判断能力、本次影响、下游限制和待确认问题，不直接决定正式本体如何修改。正式入库仍需要本体维护流程确认。

## 8. 质量门槛与返工规则

02 产物必须满足 `00A_高质量产出判别标准.md` 中关于判断结构与本体视图阶段的高质量标准。本阶段不大段复制 00A，而是将其转化为以下执行门槛。

### 8.1 最低准入门槛

满足以下条件时，02 可以标记为 `minimum_pass`，并进入 03：

1. 研究问题、对象、范围、时间和用途与 01 一致；
2. 已形成判断脊柱，且没有把 01 的问题偷换为更容易回答的问题；
3. 已形成最小问题树，并说明前置、并列、竞争和停止关系；
4. 已说明对象是否需要拆分，以及拆分依据；
5. 已列出主路径、削弱条件、阻断条件、反证和竞争解释；
6. 每个关键判断单元都有最低验证条件和不满足时的降级规则；
7. 已说明框架库使用情况，包括主轴框架、辅助框架、场景卡、裁剪模块和排除理由；
8. 研究逻辑 Markdown 与本体视图 YAML 表达同一套判断；
9. 每个选中状态变量都有路径节点、状态绑定、观测需求和 evidence profile 出口链；
10. 已完成本体缺口扫描，且每个必要本体项都有覆盖状态、证据可收集性和推理可使用性判断；
11. 正式本体、任务候选和本体缺口的边界清楚；
12. 不存在未处理的 `major` 或 `blocking` 缺口；
13. 已明确传递给 03 的实例、观测、证据、反证需求和使用限制；
14. 每个核心判断单元具备 `judgment_type`、必需证据角色、证据类别、反证类别、最低验证条件、禁止捷径和代理边界。

`minimum_pass` 只表示可以进入 03，不代表产物已经达到正式交付质量。

### 8.2 高质量语义审阅门槛

满足以下条件时，02 才能标记为 `high_quality_pass`：

1. 判断脊柱足够清晰，研究员读完能知道本次真正要辨别什么；
2. 问题树是“最小可判断结构”，没有无关扩展或框架拼贴；
3. 框架选择能够解释为什么这样组合，而不是按标题、热点词或行业标签机械匹配；
4. 主逻辑、反证逻辑和竞争解释都能落到可执行验证；
5. 停止条件不是“材料足够多”，而是关键判断单元达到最低验证条件；
6. 降级规则能明确约束 04 不能越权输出；
7. 本体映射服务研究逻辑，而不是用技术字段掩盖逻辑不清；
8. Markdown 对研究员可读，YAML 对 03 可执行；
9. 缺口扫描没有停留在自评层面，而是完成必要本体项映射、下游压力测试和硬触发检查；
10. 使用任务候选或存在本体缺口时，已经说明对后续证据准备和推理输出的限制。
11. 证据交接没有把 03 的配方执行、证据篮子状态、来源快照或准入评估提前写入 02 判断单元。

### 8.3 本阶段不得出现

02 不得出现以下问题：

1. 只讲行业常识，没有判断脊柱；
2. 只列变量，没有最小问题树；
3. 只有主线，没有反证和竞争解释；
4. 把框架库内容拼贴成分析结构；
5. 为了完整，把所有相关对象、变量和路径都纳入；
6. 每个节点都写“需要证据验证”，但没有最低验证条件；
7. 停止条件是“继续收集更多资料”；
8. 本体视图靠自填字段通过，但状态变量出口链无法被 03 使用；
9. Markdown 和 YAML 的对象、路径、变量或证据要求不一致；
10. 在 02 中提前形成现实结论或业务方向判断；
11. 未说明主轴框架、辅助框架和场景卡的选择理由；
12. 同时调用过多框架，或同时调用多张场景卡，导致研究问题失焦；
13. 将场景卡、热点问题或案例复盘误当作正式框架；
14. 只因关键词相似而引用框架，未检查对象、范围、机制和证据条件是否适配；
15. 未说明哪些相关框架被排除，以及为什么排除；
16. 用上位概念、泛化变量或抽象风险词冒充本体充分覆盖；
17. 只写“无明显缺口”，但没有必要本体项清单、覆盖状态和下游压力测试；
18. 明明需要 03 临时补对象、变量、路径或证据 profile，却仍将 02 标记为无缺口通过。
19. 把单值 `evidence_role` 当作判断单元全部证据要求，未区分主证据、交叉验证、反证、阻断条件和代理指标。

### 8.4 返工规则

出现以下任一情况，02 不得进入 03：

| 问题表现 | 处理 |
|---|---|
| 发现 01 的问题、对象、用途或范围仍有关键歧义 | 退回 01 |
| 判断脊柱不清，无法说明本次真正要辨别什么 | 返工 02 |
| 最小问题树缺失或过宽 | 返工 02 |
| 框架选择过多、过宽或仅按关键词匹配，导致判断脊柱失焦 | 返工 02 |
| 主路径、反证和竞争解释无法区分 | 返工 02 |
| 关键判断单元没有最低验证条件 | 返工 02 |
| 本体无法承接关键机制，且未记录任务候选或本体缺口 | 返工 02 或进入本体优化 |
| 缺口扫描矩阵缺失，或只做主观自评 | 返工 02 |
| 必要本体项大量停留在 `upper_cover` 或 `weak_cover`，但仍判定无缺口 | 返工 02 |
| 出现 `major` 或 `blocking` 缺口，但未生成缺口说明 | 返工 02 或进入本体优化 |
| 研究逻辑 Markdown 与本体视图 YAML 不一致 | 返工 02 |
| 证据需求无法被 03 执行 | 返工 02 |

返工说明必须写清：问题发生在哪个判断单元、影响哪个路径或变量、需要修改什么，以及修改后如何重新校验。

---

## 9. 输出及与 03 的交接

### 9.1 输出文件

02 每次生成一组同主题、同日期、同序号的配对文件，并必须完成本体缺口扫描：

```text
02-<核心主题>研究逻辑-<YYYYMMDD>-<当日序号>.md
02-<核心主题>本体视图-<YYYYMMDD>-<当日序号>.yaml
02-<核心主题>本体缺口扫描-<YYYYMMDD>-<当日序号>.md 或写入 YAML 的 ontology_gap_scan 字段
```

例如：

```text
02-存储芯片周期研究逻辑-20260611-1.md
02-存储芯片周期本体视图-20260611-1.yaml
```

如存在 `major_gap` 或 `blocking_gap`，必须另行生成：

```text
02-存储芯片周期本体缺口说明-20260611-1.md
```

如只有 `minor_gap`，可以不单独生成缺口说明，但必须在本体视图 YAML 或缺口扫描文件中写清限制。

命名要求：

1. 核心主题使用能够识别研究对象和核心判断的简短业务表述；
2. 日期使用文件实际生成日期；
3. 序号从 `1` 开始，同日同主题依次递增；
4. 配对文件必须使用相同的核心主题、日期和序号；
5. 文件日期只用于审计，不作为滚动时间范围的锚点。

研究逻辑 Markdown 和本体视图 YAML 的 front matter 应同时保留阶段状态和产出质量：

```yaml
stage_status: aligned | needs_revision | returned_to_01
quality_status: draft | minimum_pass | high_quality_pass | return_required
ontology_gap_scan_status: no_gap | minor_gap | major_gap | blocking_gap
can_enter_03: true | false
framework_usage_ref: "<如在 YAML 中记录 framework_usage，写明对应字段或摘要>"
quality_review_ref: "<如有独立语义审阅，记录审阅文件或摘要>"
```

具体正文和结构化字段见 `输出模板/02_研究逻辑模板.md`、`输出模板/02_判断结构与本体视图模板.yaml`、`输出模板/02_本体缺口扫描模板.md` 和可选的 `输出模板/02_本体缺口说明模板.md`。


### 9.2 进入 03 前应确认什么

交给 03 前，应确认：

1. 研究问题、对象和边界与 01 一致；
2. 研究逻辑已经讲清核心矛盾、问题拆解、主路径、反证和替代解释；
3. 已记录框架库使用情况，包括主轴框架、辅助框架、场景卡、裁剪模块和排除理由；
4. 关键对象、关系、变化和路径已在 YAML 中被正式本体、任务候选或缺口记录承接；
5. 每个关键判断都有对应的实例、观测、证据或反证需求；
6. 每项证据和反证要求都绑定了可解析的 evidence profile；
7. 研究逻辑文件与本体视图文件编号、路径和含义一致；
8. 已完成本体缺口扫描，并明确每个必要本体项的 `coverage_status`、`evidence_collectable` 和 `reasoning_usable`；
9. 不存在未处理的 `major_gap` 或 `blocking_gap`；
10. 使用任务候选或存在本体缺口时，相关限制已明确传给 03 和 04；
11. `validation.checks.state_variable_chain_complete = true`，不存在选中后未绑定、未观测或未绑定证据 profile 的状态变量；
12. `validation.checks.ontology_gap_scan_complete = true`，且 `ontology_gap_scan.can_enter_03 = true`；
13. 02 至少达到 `minimum_pass`；如需作为正式交付产物，则必须达到 `high_quality_pass`。

如果 03 发现缺的是数据、实例或证据，由 03 继续处理；如果发现研究路径、本体承接或对象拆分有误，应退回 02；如果问题边界仍不清楚，应退回 01。
