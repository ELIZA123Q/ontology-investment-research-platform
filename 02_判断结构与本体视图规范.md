# 02_判断结构与本体视图规范

## 1. 文档定位

本文档规范 02 阶段如何在 01 已经明确研究任务之后，独立搭建本次研究的判断结构，并把这套结构转化为 03 可以据此准备实例、观测、证据和反面证据的任务本体视图。

02 的核心目标不是把 01 的问题扩写成一篇行业分析，也不是把框架库内容拼贴成模板化结构，而是回答四个问题：

1. 本次真正要辨别的核心矛盾是什么；
2. 总问题应拆成哪些最小可核心问题，为什么按这个顺序判断；
3. 主路径、反面证据和其他可能解释分别是什么，哪些信号能够区分它们；
4. 正式本体是否能够承接这套判断结构，并把验证需求交给 03。

02 与相邻阶段的关系是：

```text
01 提供已经明确的问题、对象、范围、用途和初步线索
→ 02 形成研究逻辑、判断脊柱、最小问题树、主路径、反面证据和其他可能解释
→ 02 将判断结构映射为任务本体视图
→ 03 按路径、变量、实例需求和证据 profile 准备现实输入
```

`aligned` 或“可以进入 03”只表示阶段状态满足交接要求，不代表本体已经完美、证据已经充分或判断已经成立。正式交付时，还必须满足 `00A_高质量产出判别标准.md` 中对 02 的质量要求。

02 不查询现实数据，不创建现实实例，不判断证据是否充分，不形成方向性投研结论。

---

## 2. 本阶段负责什么、不负责什么

### 2.1 本阶段负责

02 负责把 01 的研究任务转化为可执行的判断结构，主要包括：

1. 继承 01 的研究问题、判断落点、对象范围、时间口径、用途和不研究事项；
2. 识别本次判断的核心矛盾，并形成一句清晰的**判断脊柱**；
3. 把总问题拆成最小问题树，说明前置、并列、竞争和停止关系；
4. 确定需要拆分的对象、产品、环节、地区或公司差异；
5. 组织主路径、削弱条件、逻辑失效条件、反面证据和其他可能解释；
6. 为每个关键核心问题定义最低验证条件、降级规则和停止条件；
7. 选择正式本体中的对象、关系、事件、关键变量、传导模板、推理规则和证据 profile；
8. 生成研究逻辑 Markdown 和任务本体视图 YAML，并保证二者一致；
9. 记录正式本体无法承接的结构缺口、任务候选和使用限制；
10. 判断本阶段产物是草稿、最低通过，还是达到正式交付质量。

### 2.2 本阶段不负责

02 不做以下事情：

1. 不改变 01 已经确认的研究意图、主要对象、时间窗口或使用目的；
2. 不查询公告、新闻、财报、价格、数据源或其他现实材料；
3. 不创建现实公司、产品、设施、订单、价格、库存等实例；
4. 不判断现实证据是否已经满足；
5. 不用材料摘要替代判断结构；
6. 不输出周期阶段、影响强弱、投资结论或方向性判断；
7. 不为了让本体匹配更容易而压平对象差异；
8. 不把一次性事实、临时表述或材料观点直接写成正式本体定义。

正式数据与证据准备由 03 完成，路径推理和研究员报告由 04 完成。02 只定义“应该如何判断”和“本体如何承接”。

---

## 3. 02 要形成的核心成果

02 的重点不是整理字段，而是形成一套能被 03 和 04 使用的判断结构。完成后应能回答：

1. **判断脊柱**：一句话说清本次最需要辨别的主矛盾和核心逻辑；
2. **最小问题树**：总问题拆成哪些子问题，哪些是前置、并列、竞争或停止节点；
3. **结构分化**：哪些产品、公司、环节或地区不能合并判断，为什么；
4. **主路径**：最需要验证的传导链是什么；
5. **反面证据与其他可能解释**：哪些信号会削弱、逻辑失效或替代主路径；
6. **最低验证条件**：每个关键核心问题至少需要什么实例、观测、证据和反面证据；
7. **降级规则**：最低条件不满足时，后续只能形成倾向判断、条件判断、暂不可判断还是路径逻辑失效；
8. **本体承接**：正式本体、任务候选和本体缺口分别承接了哪些内容；
9. **03 交接**：哪些需求可以直接交给 03 准备数据与证据。

只纳入会影响本次判断的内容。仅仅“可能相关”、但不能改变路径或结论强弱的对象和变量，不进入本次研究框架。

---
## 4. 如何承接 01 的研究需求

01 给出的是研究意图和边界，不是现成的分析框架。02 应保留 01 的核心问题，同时重新判断这个问题应如何拆解。

| 01 提供的内容 | 02 需要作出的判断 |
|---------------|-------------------|
| 核心问题 | 真正需要辨别的机制和矛盾是什么 |
| 研究对象 | 是否需要按产品、角色、环节或公司分别讨论 |
| 判断起点 | 应从事件、状态、假设还是历史事实开始 |
| 观察维度 | 哪些变化是原因、过程、结果或验证信号 |
| 关系提示 | 哪些传导需要验证，哪些只是初步设想 |
| 时间与对象范围 | 哪些边界必须保持，哪些内部差异需要进一步拆分 |
| 初步验证和反面证据方向 | 如何转化为传导环节、支持条件、逻辑失效条件和替代解释 |

02 可以补充严谨分析不可缺少的结构，但不能借“完善框架”改变 01 的问题、主要对象或研究边界。发现这些内容仍有关键歧义时，应退回 01。

01 中的相对时间和动态对象选择应保持原意。02 只说明这些规则会怎样影响研究框架，不把它们固化为文档生成日对应的绝对日期或临时对象名单。

---

## 5. 如何独立搭建研究框架

研究框架应从当前问题本身出发独立形成。框架库可以帮助更快地想到常见机制、证据和反面证据，但不能代替本次判断，也不能把库中某个模板拆出几项就当作研究框架。

### 5.1 先形成判断脊柱

先判断表面问题背后真正要区分什么。例如，“价格上涨能否持续”可能需要区分：

- 需求真实增长，还是补库、抢单或重复下单；
- 名义产能不足，还是良率、认证、材料或封装限制导致有效供给不足；
- 基本面改善，还是预期、估值和资金推动；
- 总量变化，还是产品结构或客户结构迁移。

只有核心矛盾明确，后面的对象拆分、路径和证据要求才有依据。

### 5.2 再搭最小问题树和分析顺序

把总问题拆成若干能够分别验证的子问题，并说明它们之间的关系：

- 哪些问题是前置条件；
- 哪些问题需要并列比较；
- 哪些问题代表其他可能解释；
- 哪个节点失败后，后续传导应停止。

问题树应服务于判断，而不是追求覆盖所有相关主题。每个子问题都应能够被证据增强、削弱或否定。

### 5.3 确定对象、范围和必要差异

对象是否拆分，取决于不同子对象是否具有不同机制、证据口径或反面证据条件。

例如，研究存储芯片周期时，DRAM、NAND 和 HBM 是否分别分析，应由它们的需求、供给、库存和价格机制是否不同决定；不能因为都属于存储芯片就合并，也不能因为彼此相关就全部展开。

时间、地域、产业链和公司范围同样遵循“足以回答问题”的原则。范围过小会漏掉关键传导，范围过大会稀释核心判断。

### 5.4 组织主路径、反面证据和替代解释

主路径说明本次最需要验证的传导，例如：

```text
终端需求变化
→ 订单与库存变化
→ 有效供给松紧
→ 价格与交期变化
→ 公司收入和利润影响
```

同时说明：

- 哪些条件会削弱或逻辑失效主路径；
- 是否存在能够解释同一现象的其他路径；
- 不同解释需要用什么信息区分；
- 哪些前置节点不成立时，不能继续向后推导。

反面证据不是附加清单，而是研究框架的一部分。

### 5.5 明确最低验证条件

对每个关键判断说明：

- 需要确认哪些现实对象和关系；
- 需要观察哪些变化、数值或时点；
- 什么证据会增强判断；
- 什么证据会削弱、逻辑失效或支持替代解释；
- 最低需要看到什么，才值得继续验证下一层路径。

02 只定义这些验证需要，不提前判断现实中是否已经具备。

### 5.6 用框架库检查和完善

独立形成基本研究框架后，可以参考 [`02框架库/README.md`](02框架库/README.md) 及相关框架，检查是否遗漏常见机制、对象差异、传导环节或反面证据。

框架库的作用是启发、加速和校验。最终保留什么、删去什么、增加什么，仍由本次问题决定。即使库中没有合适框架，02 也应根据问题本身形成完整的研究逻辑；即使库中存在高度相似的框架，也要重新判断其是否适用于本次对象、范围和目标。

---

## 6. 如何形成任务本体视图

研究逻辑讲清“这个问题应该怎样分析”，任务本体视图说明“这套分析如何由本体承接”。二者先后有别：先把研究逻辑想清楚，再匹配本体；二者表达的是同一套判断，但面向不同读者。

### 6.1 两个文件的分工

02 输出两份配对文件：研究逻辑 Markdown 和本体视图 YAML。二者表达同一套判断任务，但用途不同。

研究逻辑 Markdown 面向研究员，负责说明「这个问题应该怎么分析」，包括核心矛盾、问题拆解、对象差异、主逻辑、另一面的数据、其他可能解释和证据要求。

本体视图 YAML 面向 03 和系统处理，负责说明「这套分析如何被本体承接」，包括正式本体引用、传导环节、实例需求、观测需求、证据要求、反面证据要求、任务候选和本体缺口。**YAML 字段名、judgment_units、path_design 等结构不改；仅 Markdown 正文采用研究员语气。**

| 文件            | 负责什么            | 不负责什么                             |
| ------------- | --------------- | --------------------------------- |
| 研究逻辑 Markdown | 讲清研究框架和分析理由     | 不承载 judgment_unit_id、路径 ID、证据要求 ID 等机器表 |
| 本体视图 YAML     | 承接本体、路径、实例和证据需求 | 不重复面向研究员的大段分析正文                   |

**Markdown 正文章节用语（内部逻辑能力不变，仅输出层改名）：**

| 内部能力 / YAML 字段 | 研究逻辑正文章节或表述 |
|---|---|
| `judgment_landing` | 要回答的问题类型 |
| `judgment_spine` | 核心假设 |
| 最小问题树 | 问题拆解与分析顺序 → 子问题清单 |
| 主路径 / 反面证据 / 竞争解释 | 主逻辑与其他可能 |
| 结构分化 | 细分差异与比较口径 |
| `judgment_units` | 关键结论与证据要求（正文不写 ID 表） |
| `handoff_to_03` | 证据准备说明（正文不写 YAML 路径表） |

Markdown 应能独立阅读；YAML 应能直接交给 03 使用。正文不得把 YAML 中的 statement 原句贴入；完整技术表达以 YAML 与 CSV 为准。

### 6.2 正式本体引用原则

任务本体视图不是重新定义本体，而是在正式本体中切出本次任务需要的最小结构。

写入 `semantic_view`、`reasoning_view`、`evidence_view` 的对象类型、关系类型、事件类型、关键变量、传导模板、推理规则和证据要求，都必须来自正式本体规范，不得临时编造。

引用顺序如下：

1. 先查[一级通用本体规范/](一级通用本体规范/)；
2. 再查本任务所属领域本体规范（例如半导体任务参考 [二级半导体领域本体规范/](二级半导体领域本体规范/)）；
3. 领域规范是一级规范之上的扩展，二者边界不一致时，以领域规范为准，并在 YAML 中记录来源；
4. 正式规范未覆盖的内容，不得冒充正式本体，应记录为任务候选或本体缺口。

YAML 中应通过 `ontology_sources`、`source_ref`、`source_version` 记录本次引用了哪些规范和定义。

如果缺的是现实公司、产品、事件、数值或材料，交给 03 作为实例、观测或证据需求；如果缺的是稳定的对象类型、关系类型、关键变量、事件类型、规则或证据结构，才记录为本体缺口。

证据要求也必须沿用正式本体。关键变量与证据 profile 的绑定从 `reasoning.yaml` 读取，profile 的支持证据、反面证据、最低覆盖和降级条件从 `evidence.yaml` 读取。02 负责选择本次需要的 profile，并把 profile 引用和任务特定的最低要求写入 `evidence_view`；03 据此寻找材料和判断质量，不重新设计证据标准。

`evidence.yaml` 不是数据源。一次任务缺少材料或来源时，应交给 03 记录为数据或证据缺口；只有缺少可长期复用的证据 profile、质量规则或正式绑定时，才记录为本体缺口。

任务候选只用于本次研究临时承接尚未正式入库、但又不可缺少的结构。它不得混入正式本体，并应说明使用理由、边界和风险。

### 6.3 本体视图怎样承接研究逻辑

任务本体视图不是整份本体，也不是研究结论，而是为本次问题切出的一块最小充分结构：

| 研究逻辑 | 本体视图中的表达 |
|----------|------------------|
| 研究对象及其差异 | 对象类型和分类 |
| 对象之间的作用 | 关系类型及适用边界 |
| 需要观察的变化 | 事件类型和关键变量 |
| 主路径、反面证据和停止条件 | 传导模板、规则、路径和节点 |
| 验证需要 | 实例需求、证据 profile、证据要求和反面证据要求 |

进入视图的每项结构都应能说明它表示什么、为什么本次需要、服务于哪一段判断。与本次路径无关的本体内容不进入视图。

关键变量还必须满足一条完整出口链：

```text
reasoning_view.state_variables
→ candidate_paths.nodes[].ontology_refs.state_variables
→ instance_requirements.state_bindings
→ instance_requirements.observations
→ instance_requirements.evidence_bindings
→ evidence_view.evidence_profiles
```

具体要求如下：

1. 每个选中变量至少被一个传导环节引用，且变量的 `used_by_path_nodes` 与节点反向引用完全一致；
2. 每个选中变量至少有一个 `state_binding`，同一变量可以针对不同对象需求建立多个绑定；
3. 每个状态绑定至少有一个观测需求；
4. 绑定引用的对象需求类型必须属于变量的 `allowed_anchor_types`；
5. 每个状态绑定和观测需求至少被一个 evidence binding 覆盖，所用 profile 必须与变量的 `evidence_profile_ref` 一致；
6. 上述条件统一由 `validation.checks.state_variable_chain_complete` 表示；为 `false` 时不得进入 03。

关键变量可以由 03 准备直接观测、事件或代理信号，但 03 不得由此计算变量方向或执行变量间传导。只有 04 可以在证据准入后应用 02 选定的规则。

本体匹配也会反过来帮助检查研究逻辑：如果一个概念无法稳定定义，可能是对象粒度混乱、关系含义不清，或把一次性事实误当成稳定结构。此时应先澄清逻辑，再决定是否记录本体缺口。

### 6.4 两个文件的一致性要求

研究逻辑 Markdown 和本体视图 YAML 必须表达同一个判断任务。
Markdown 中的关键对象、变量、路径、证据和反面证据，应在 YAML 中找到对应结构；YAML 中的路径、节点、实例需求和证据需求，也应能回到 Markdown 中找到分析依据。
如果任一文件的核心逻辑发生变化，另一份文件必须同步更新。YAML 中不得出现不服务正文判断的孤立结构。Markdown 附录只提供快速定位，不能代替 YAML 的完整技术表达。

---

## 7. 正式本体不能完全承接时怎么办

没有找到现成定义时，先判断缺的是结构还是现实信息：

| 情形 | 处理方式 |
|------|----------|
| 缺少稳定的对象、关系、变量、事件或规则定义 | 记录本体缺口；必要时形成任务候选 |
| 名称、角色或范围无法唯一理解 | 先澄清；影响研究意图时退回 01 |
| 已有类型，但缺少现实公司、产品、关系或事件 | 作为实例需求交给 03 |
| 已有关键变量，但缺少现实方向、数值或时间 | 作为观测需求交给 03 |
| 已有证据要求，但尚未取得来源和材料 | 作为证据需求交给 03 |


任务候选只用于本次研究临时承接不可缺少、但尚未正式入库的稳定结构。它必须与正式本体分开保存，并说明使用边界和风险。
中高等级本体缺口可另行生成本体缺口说明，供本体维护者复核。缺口说明只记录缺失的判断能力、本次影响和待确认问题，不直接决定正式本体如何修改。
---

## 8. 质量门槛与返工规则

02 产物必须满足 `00A_高质量产出判别标准.md` 中关于判断结构与本体视图阶段的高质量标准。本阶段不大段复制 00A，而是将其转化为以下执行门槛。

### 8.1 最低准入门槛

满足以下条件时，02 可以标记为 `minimum_pass`，并进入 03：

1. 研究问题、对象、范围、时间和用途与 01 一致；
2. 已形成判断脊柱，且没有把 01 的问题偷换为更容易回答的问题；
3. 已形成最小问题树，并说明前置、并列、竞争和停止关系；
4. 已说明对象是否需要拆分，以及拆分依据；
5. 已列出主路径、削弱条件、逻辑失效条件、反面证据和其他可能解释；
6. 每个关键核心问题都有最低验证条件和不满足时的降级规则；
7. 研究逻辑 Markdown 与本体视图 YAML 表达同一套判断；
8. 每个选中关键变量都有传导环节、状态绑定、观测需求和 evidence profile 出口链；
9. 正式本体、任务候选和本体缺口的边界清楚；
10. 已明确传递给 03 的实例、观测、证据和反面证据需求。

`minimum_pass` 只表示可以进入 03，不代表产物已经达到正式交付质量。

### 8.2 高质量语义审阅门槛

满足以下条件时，02 才能标记为 `high_quality_pass`：

1. 判断脊柱足够清晰，研究员读完能知道本次真正要辨别什么；
2. 问题树是“最小可判断结构”，没有无关扩展或框架拼贴；
3. 主逻辑、反面证据逻辑和其他可能解释都能落到可执行验证；
4. 停止条件不是“材料足够多”，而是关键核心问题达到最低验证条件；
5. 降级规则能明确约束 04 不能越权输出；
6. 本体映射服务研究逻辑，而不是用技术字段掩盖逻辑不清；
7. Markdown 对研究员可读，YAML 对 03 可执行；
8. 使用任务候选或存在本体缺口时，已经说明对后续证据准备和推理输出的限制。

### 8.3 本阶段不得出现

02 不得出现以下问题：

1. 只讲行业常识，没有判断脊柱；
2. 只列变量，没有最小问题树；
3. 只有主线，没有反面证据和其他可能解释；
4. 把框架库内容拼贴成分析结构；
5. 为了完整，把所有相关对象、变量和路径都纳入；
6. 每个节点都写“需要证据验证”，但没有最低验证条件；
7. 停止条件是“继续收集更多资料”；
8. 本体视图靠自填字段通过，但关键变量出口链无法被 03 使用；
9. Markdown 和 YAML 的对象、路径、变量或证据要求不一致；
10. 在 02 中提前形成现实结论或业务方向性判断。

### 8.4 返工规则

出现以下任一情况，02 不得进入 03：

| 问题表现 | 处理 |
|---|---|
| 发现 01 的问题、对象、用途或范围仍有关键歧义 | 退回 01 |
| 判断脊柱不清，无法说明本次真正要辨别什么 | 返工 02 |
| 最小问题树缺失或过宽 | 返工 02 |
| 主路径、反面证据和其他可能解释无法区分 | 返工 02 |
| 关键核心问题没有最低验证条件 | 返工 02 |
| 本体无法承接关键机制，且未记录任务候选或本体缺口 | 返工 02 或进入本体优化 |
| 研究逻辑 Markdown 与本体视图 YAML 不一致 | 返工 02 |
| 证据需求无法被 03 执行 | 返工 02 |

返工说明必须写清：问题发生在哪个核心问题、影响哪个路径或变量、需要修改什么，以及修改后如何重新校验。

---

## 9. 输出及与 03 的交接

### 9.1 输出文件

02 每次生成一组同主题、同日期、同序号的配对文件：

```text
02-<核心主题>研究逻辑-<YYYYMMDD>-<当日序号>.md
02-<核心主题>本体视图-<YYYYMMDD>-<当日序号>.yaml
```

例如：

```text
02-存储芯片周期研究逻辑-20260611-1.md
02-存储芯片周期本体视图-20260611-1.yaml
```

如存在需要单独留档的中高等级本体缺口，可附：

```text
02-存储芯片周期本体缺口说明-20260611-1.md
```

命名要求：

1. 正式产出必须以阶段前缀 `02-` 开头；
2. 核心主题使用能够识别研究对象和核心判断的简短业务表述；
3. 日期使用文件实际生成日期；
4. 序号从 `1` 开始，同日同主题依次递增；
5. 配对文件必须使用相同的核心主题、日期和序号；
6. 文件日期只用于审计，不作为滚动时间范围的锚点。

研究逻辑 Markdown 和本体视图 YAML 的 front matter / `quality_control` 应同时保留阶段状态和产出质量。字段与章节以 `输出模板/02_研究逻辑模板.md`、`输出模板/02_判断结构与本体视图模板.yaml` 为准；可选缺口说明见 `输出模板/02_本体缺口说明模板.md`。

研究逻辑 Markdown front matter 至少应包含：

```yaml
---
document_type: research_logic
schema_version: "1.0.0"
task_id: "<任务标识>"
logic_id: "<研究逻辑文档标识>"
generated_at: "<文档生成时间，ISO 8601；仅用于审计>"
source_document: "<01 投研需求说明文件>"
ontology_view_ref: "<同主题、同日期、同序号的本体视图文件名>"
framework_library_ref: 02框架库/README.md
framework_library_version: 1.0.0
stage_status: aligned
quality_status: high_quality_pass
quality_gate_ref: "00A_高质量产出判别标准.md#5.2"
judgment_spine: "<一句话说明本次真正要辨别的主矛盾和核心逻辑>"
---
```

`stage_status` 取值为 `aligned`、`needs_revision` 或 `returned_to_01`。进入 03 前正式交付应达到 `stage_status: aligned` 且 `quality_status: high_quality_pass`；标记为 `high_quality_pass` 时，配对 YAML 的 `quality_control.deterministic_check_status` 必须为 `checked`，`semantic_review_status` 必须为 `reviewed`。

研究逻辑正文必须包含以下章节，结构与 `02_研究逻辑模板.md` 一致：

1. 研究问题与边界；
2. 问题拆解与分析顺序；
3. 主逻辑与其他可能；
4. 细分差异与比较口径；
5. 关键结论与证据要求；
6. 证据准备说明；
7. 进入 03 前质量检查。

正文第 7 章可保留人工审阅表和质量结论，但不得重复抄写完整 YAML 或 `validation.checks`。`validation.checks` 中的检查项名称（如 `judgment_spine_present`）为机器侧命名，不因正文章节改名而改变。

本体视图 YAML 顶层至少应包含：`schema_name`、`schema_version`、`task_context`、`quality_control`、`research_framework`、`ontology_sources`、`judgment_units`、`path_design`、`ontology_bindings`、`instance_requirements`、`evidence_requirements`、`handoff_to_03`、`validation`。`task_context.judgment_landing` 取值与 01/02 模板一致，包括 `假设验证与更新`。

`validation.checks` 至少包含：`scope_consistent_with_01`、`judgment_spine_present`、`minimum_question_tree_present`、`main_counter_competing_paths_present`、`judgment_units_have_minimum_conditions`、`state_variable_chain_complete`、`evidence_requirements_executable_by_03`、`md_yaml_consistent`。进入 03 前 `validation.result` 必须为 `pass`，且上述检查项均为 `true`。


### 9.2 进入 03 前应确认什么

交给 03 前，应确认：

1. 研究问题、对象和边界与 01 一致；
2. 研究逻辑已经讲清核心矛盾、问题拆解、主路径、反面证据和替代解释；
3. 关键对象、关系、变化和路径已在 YAML 中被正式本体、任务候选或缺口记录承接；
4. 每个关键判断都有对应的实例、观测、证据或反面证据需求；
5. 每项证据和反面证据要求都绑定了可解析的 evidence profile；
6. 研究逻辑文件与本体视图文件编号、路径和含义一致；
7. 使用任务候选或存在本体缺口时，相关限制已明确传给 03。
8. `validation.checks.state_variable_chain_complete = true`，不存在选中后未绑定、未观测或未绑定证据 profile 的关键变量；
9. 02 至少达到 `minimum_pass`；如需作为正式交付产物，则必须达到 `high_quality_pass`。

如果 03 发现缺的是数据、实例或证据，由 03 继续处理；如果发现研究路径、本体承接或对象拆分有误，应退回 02；如果问题边界仍不清楚，应退回 01。
